---
phase: 05-evolution-routes
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - services/akasa-server/src/council/performance-judge.ts
  - services/akasa-server/src/council/soul-analyst.ts
  - services/akasa-server/src/council/devils-advocate.ts
  - services/akasa-server/src/council/council-runner.ts
  - services/akasa-server/src/routes/council.ts
  - services/akasa-server/src/routes/evolution-trigger.ts
  - services/akasa-server/src/routes/index.ts
  - services/akasa-server/src/__tests__/council.test.ts
  - services/akasa-server/src/__tests__/evolution-trigger.test.ts
autonomous: true
requirements:
  - EVO-02
  - EVO-04
  - EVO-06

must_haves:
  truths:
    - "Three council judges (Performance Judge, Soul Analyst, Devil's Advocate) each produce a typed verdict output with confidence score"
    - "Devil's Advocate uses a different LLM provider family (OpenAI) than Performance Judge (Anthropic)"
    - "Council runner aggregates three judge outputs into a single weighted verdict stored in council_verdicts table"
    - "Evolution trigger polls heartbeat_runs for completed runs linked to Akasa bots via paperclipAgentId"
    - "Completed heartbeat runs that lack a council_verdicts row trigger automatic council evaluation"
    - "Verdict routes allow GET list by execution, GET by ID"
  artifacts:
    - path: "services/akasa-server/src/council/council-runner.ts"
      provides: "Orchestrates 3 judges via Promise.allSettled, computes weighted verdict, inserts into DB"
      exports: ["runCouncilForBot"]
    - path: "services/akasa-server/src/council/performance-judge.ts"
      provides: "Performance scoring judge using Anthropic Claude"
      exports: ["runPerformanceJudge"]
    - path: "services/akasa-server/src/council/devils-advocate.ts"
      provides: "Contrarian judge using OpenAI (different provider family)"
      exports: ["runDevilsAdvocate"]
    - path: "services/akasa-server/src/routes/evolution-trigger.ts"
      provides: "Polling loop and manual trigger route for council evaluations"
      exports: ["evolutionTriggerRouter", "checkAndTriggerCouncilEvaluations"]
    - path: "services/akasa-server/src/routes/council.ts"
      provides: "Verdict CRUD routes"
      exports: ["councilRouter"]
  key_links:
    - from: "services/akasa-server/src/routes/evolution-trigger.ts"
      to: "heartbeat_runs table (Paperclip DB)"
      via: "polling query on status + finishedAt"
      pattern: "heartbeatRuns.*status.*succeeded|failed"
    - from: "services/akasa-server/src/routes/evolution-trigger.ts"
      to: "services/akasa-server/src/council/council-runner.ts"
      via: "direct async call after finding unprocessed runs"
      pattern: "runCouncilForBot"
    - from: "services/akasa-server/src/council/council-runner.ts"
      to: "@claw/db council_verdicts table"
      via: "drizzle insert"
      pattern: "db\\.insert.*councilVerdicts"
---

<objective>
Port the three council judges from execution-service, create the council runner that aggregates verdicts, build verdict routes, and implement the evolution trigger that polls Paperclip's heartbeat_runs table for completed runs that need council evaluation.

Purpose: This is the intelligence layer -- after every agent run completes, the system automatically evaluates performance through three independent judges and produces a verdict (Promote/Maintain/Monitor/Demote/Retire) that drives the evolutionary loop.

Output: Council evaluation pipeline that fires automatically on heartbeat completion, verdict storage in DB, verdict CRUD routes at `/api/akasa/verdicts`, evolution trigger at `/api/akasa/evolution`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v5.0-phases/05-performance-intelligence-and-dna-capture/05-RESEARCH.md
@.planning/milestones/v5.0-phases/05-performance-intelligence-and-dna-capture/05-01-SUMMARY.md

@services/akasa-server/src/routes/index.ts
@packages/db/src/schema/council-verdicts.ts
@packages/db/src/schema/bots.ts
@packages/db/src/schema/bot-souls.ts

<interfaces>
<!-- Council verdict schema -->
From packages/db/src/schema/council-verdicts.ts:
```typescript
export const verdictTypeEnum = pgEnum('verdict_type', ['Promote', 'Maintain', 'Monitor', 'Demote', 'Retire']);
export const verdictStatusEnum = pgEnum('verdict_status', ['pending', 'confirmed', 'rejected']);

export const councilVerdicts = pgTable('council_verdicts', {
  id: uuid('id').primaryKey().defaultRandom(),
  executionId: uuid('execution_id').notNull().references(() => executions.id, { onDelete: 'cascade' }),
  botId: uuid('bot_id').notNull(),
  soulId: uuid('soul_id'),
  verdictType: verdictTypeEnum('verdict_type').notNull(),
  status: verdictStatusEnum('status').notNull().default('pending'),
  weightedConfidenceScore: numeric('weighted_confidence_score', { precision: 4, scale: 3 }).notNull(),
  requiresHumanConfirmation: boolean('requires_human_confirmation').notNull().default(false),
  hasUnresolvedDevilsAdvocate: boolean('has_unresolved_devils_advocate').notNull().default(false),
  verdictSummary: text('verdict_summary').notNull(),
  performanceJudgeOutput: jsonb('performance_judge_output'),
  soulAnalystOutput: jsonb('soul_analyst_output'),
  devilsAdvocateOutput: jsonb('devils_advocate_output'),
  confirmedAt, confirmedBy, timeOnScreenMs, godLayerProcessedAt, createdAt, updatedAt,
});
export type CouncilVerdict = typeof councilVerdicts.$inferSelect;
```

From packages/db/src/schema/bots.ts (after Plan 01):
```typescript
export const bots = pgTable('bots', {
  id: uuid('id').primaryKey().defaultRandom(),
  executionId: uuid('execution_id').notNull(),
  paperclipAgentId: uuid('paperclip_agent_id'),  // links to Paperclip agents.id
  soulId: uuid('soul_id'),
  compositeScore: numeric('composite_score', { precision: 5, scale: 2 }),
  // ...
});
```

From paperclip/packages/db/src/schema/heartbeat_runs.ts:
```typescript
export const heartbeatRuns = pgTable('heartbeat_runs', {
  id: uuid('id').primaryKey().defaultRandom(),
  companyId: uuid('company_id').notNull(),
  agentId: uuid('agent_id').notNull(),
  status: text('status').notNull().default('queued'),
  finishedAt: timestamp('finished_at', { withTimezone: true }),
  usageJson: jsonb('usage_json'),
  resultJson: jsonb('result_json'),
  contextSnapshot: jsonb('context_snapshot'),
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port council judges and create council runner</name>
  <files>
    services/akasa-server/src/council/performance-judge.ts,
    services/akasa-server/src/council/soul-analyst.ts,
    services/akasa-server/src/council/devils-advocate.ts,
    services/akasa-server/src/council/council-runner.ts,
    services/akasa-server/src/__tests__/council.test.ts
  </files>
  <read_first>
    services/execution-service/src/queue/council-worker.ts,
    services/execution-service/src/services/soul-generator.ts,
    packages/db/src/schema/council-verdicts.ts,
    packages/db/src/schema/bots.ts,
    packages/db/src/schema/bot-souls.ts,
    packages/db/src/schema/decision-traces.ts,
    packages/db/src/schema/executions.ts
  </read_first>
  <behavior>
    - Test: runPerformanceJudge returns { compositeScore: number, taskCompletionRate: number, tokenEfficiency: number, verdict: string, confidence: number, reasoning: string }
    - Test: runSoulAnalyst returns { dimensionAlignment: number, constitutionAdherence: number, verdict: string, confidence: number, reasoning: string }
    - Test: runDevilsAdvocate returns { contraryEvidence: string[], riskFlags: string[], verdict: string, confidence: number, reasoning: string }
    - Test: runCouncilForBot calls all 3 judges via Promise.allSettled, computes weighted verdict, inserts into council_verdicts
    - Test: council runner handles partial judge failures gracefully (one judge failing doesn't block verdict)
    - Test: Promote/Retire verdicts have requiresHumanConfirmation=true
  </behavior>
  <action>
    1. Create `services/akasa-server/src/__tests__/council.test.ts`:
    - Mock Vercel AI SDK `generateObject` to return deterministic judge outputs without real LLM calls.
    - Mock `@claw/db` to capture insert calls.
    - Test each judge function individually with mocked LLM response.
    - Test council-runner calls all 3 judges, handles one failing gracefully, produces correct weighted verdict.
    - Test that Promote and Retire verdicts set `requiresHumanConfirmation: true`.

    2. Port `services/akasa-server/src/council/performance-judge.ts`:
    - Read `services/execution-service/src/queue/council-worker.ts` for the existing judge logic (it contains inline judge implementations).
    - Create standalone module: `export async function runPerformanceJudge(ctx: CouncilContext): Promise<PerformanceJudgeOutput>`
    - Uses `@ai-sdk/anthropic` with model `claude-sonnet-4-20250514` (Anthropic family).
    - `CouncilContext` type: `{ executionId: string, botId: string, soulId: string | null, soulContent: string | null, taskCategory: string | null, taskResults: { completed: number, failed: number, total: number }, tokenUsage: { input: number, output: number }, runDurationMs: number }`.
    - Prompt instructs judge to evaluate: task completion rate, token efficiency, output quality.
    - Uses `generateObject()` with Zod schema for typed output.
    - `PerformanceJudgeOutput`: `{ compositeScore: number (0-1), taskCompletionRate: number (0-1), tokenEfficiency: number (0-1), verdict: 'Promote'|'Maintain'|'Monitor'|'Demote'|'Retire', confidence: number (0-1), reasoning: string }`.

    3. Port `services/akasa-server/src/council/soul-analyst.ts`:
    - `export async function runSoulAnalyst(ctx: CouncilContext): Promise<SoulAnalystOutput>`
    - Uses `@ai-sdk/anthropic` with model `claude-sonnet-4-20250514` (same family as PJ is OK -- Devil's Advocate is the one that MUST differ).
    - Evaluates how well the soul's behavioral dimensions aligned with actual performance.
    - `SoulAnalystOutput`: `{ dimensionAlignment: number (0-1), constitutionAdherence: number (0-1), verdict: 'Promote'|'Maintain'|'Monitor'|'Demote'|'Retire', confidence: number (0-1), reasoning: string, dimensionScores: Record<string, number> }`.

    4. Port `services/akasa-server/src/council/devils-advocate.ts`:
    - `export async function runDevilsAdvocate(ctx: CouncilContext): Promise<DevilsAdvocateOutput>`
    - MUST use `@ai-sdk/openai` with model `gpt-4o-mini` (DIFFERENT provider family from Performance Judge per CLAUDE.md: "Devil's Advocate must always use a different LLM provider family than Performance Judge").
    - Prompt challenges the other judges' likely conclusions, looks for risks and contrary evidence.
    - `DevilsAdvocateOutput`: `{ contraryEvidence: string[], riskFlags: string[], verdict: 'Promote'|'Maintain'|'Monitor'|'Demote'|'Retire', confidence: number (0-1), reasoning: string }`.

    5. Create `services/akasa-server/src/council/council-runner.ts`:
    - `export async function runCouncilForBot(executionId: string, botId: string, soulId: string | null): Promise<CouncilVerdict>`
    - Assembles `CouncilContext` by querying: bot from `bots` table (for compositeScore, tasksClaimed, tasksCompleted), soul from `botSouls` (for soulContent, dimensions), and run data from heartbeat_runs (for usageJson, resultJson).
    - Calls `Promise.allSettled([runPerformanceJudge(ctx), runSoulAnalyst(ctx), runDevilsAdvocate(ctx)])`.
    - For fulfilled judges, extract verdict. For rejected, log error and use null.
    - Compute weighted verdict: Performance Judge weight 0.5, Soul Analyst weight 0.3, Devil's Advocate weight 0.2. Use highest-weighted verdict type. Weighted confidence = sum(weight * confidence) / sum(weights of fulfilled judges).
    - Set `requiresHumanConfirmation: true` if verdict is 'Promote' or 'Retire'.
    - Set `hasUnresolvedDevilsAdvocate: true` if DA's verdict differs from the weighted verdict.
    - Insert into `councilVerdicts` table with all judge outputs as JSONB.
    - Return the inserted verdict row.
    - Import `db, councilVerdicts, bots, botSouls` from `@claw/db`.

    6. Run tests: `pnpm --filter @claw/akasa-server exec vitest run src/__tests__/council.test.ts`
  </action>
  <verify>
    <automated>cd /Users/tarikstafford/Desktop/Projects/claw-army && pnpm --filter @claw/akasa-server exec vitest run src/__tests__/council.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - services/akasa-server/src/council/performance-judge.ts exports `runPerformanceJudge`
    - services/akasa-server/src/council/performance-judge.ts contains `@ai-sdk/anthropic`
    - services/akasa-server/src/council/soul-analyst.ts exports `runSoulAnalyst`
    - services/akasa-server/src/council/devils-advocate.ts exports `runDevilsAdvocate`
    - services/akasa-server/src/council/devils-advocate.ts contains `@ai-sdk/openai` (NOT anthropic)
    - services/akasa-server/src/council/council-runner.ts exports `runCouncilForBot`
    - services/akasa-server/src/council/council-runner.ts contains `Promise.allSettled`
    - services/akasa-server/src/council/council-runner.ts contains `requiresHumanConfirmation`
    - services/akasa-server/src/council/council-runner.ts contains `db.insert(councilVerdicts)`
    - `pnpm --filter @claw/akasa-server exec vitest run src/__tests__/council.test.ts` exits 0
  </acceptance_criteria>
  <done>Three council judges ported (DA uses OpenAI, PJ/SA use Anthropic), council runner aggregates with weighted voting and stores verdict, all council tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create verdict routes, evolution trigger with polling loop, wire into akasaRouter</name>
  <files>
    services/akasa-server/src/routes/council.ts,
    services/akasa-server/src/routes/evolution-trigger.ts,
    services/akasa-server/src/routes/index.ts,
    services/akasa-server/src/__tests__/evolution-trigger.test.ts
  </files>
  <read_first>
    services/execution-service/src/routes/verdicts.ts,
    packages/db/src/schema/council-verdicts.ts,
    packages/db/src/schema/bots.ts,
    paperclip/packages/db/src/schema/heartbeat_runs.ts,
    services/akasa-server/src/routes/index.ts,
    services/akasa-server/src/council/council-runner.ts
  </read_first>
  <behavior>
    - Test: councilRouter GET / with ?executionId= returns verdicts for that execution
    - Test: councilRouter GET /:id returns single verdict or 404
    - Test: checkAndTriggerCouncilEvaluations finds completed heartbeat_runs with matching Akasa bot (via paperclipAgentId) that have no verdict row, and triggers council
    - Test: checkAndTriggerCouncilEvaluations skips runs that already have a verdict
    - Test: checkAndTriggerCouncilEvaluations skips runs for agents with no Akasa bot record
    - Test: evolution trigger POST /api/akasa/evolution/trigger manually triggers a check cycle
  </behavior>
  <action>
    1. Create `services/akasa-server/src/__tests__/evolution-trigger.test.ts`:
    - Mock both Paperclip DB and Akasa DB.
    - Test that `checkAndTriggerCouncilEvaluations` queries heartbeat_runs for status 'succeeded'/'failed' with finishedAt in last 5 minutes.
    - Test that it joins with Akasa `bots` table via `paperclipAgentId` to filter only Akasa-managed agents.
    - Test that it skips runs that already have a `council_verdicts` row (checked via left join or NOT EXISTS subquery).
    - Test the manual trigger route returns `{ triggered: N }`.

    2. Create `services/akasa-server/src/routes/council.ts`:
    - `export function councilRouter(): Router`
    - `GET /` — query params: `executionId` (required). Returns `db.select().from(councilVerdicts).where(eq(councilVerdicts.executionId, executionId)).orderBy(desc(councilVerdicts.createdAt))`.
    - `GET /:id` — lookup by UUID, return 404 if not found.
    - All handlers in try/catch with `next(err)`.

    3. Create `services/akasa-server/src/routes/evolution-trigger.ts`:
    - `export function evolutionTriggerRouter(): Router` — provides manual trigger route.
    - `export async function checkAndTriggerCouncilEvaluations(paperclipDb, akasaDb): Promise<{ triggered: number }>`:
      - Query `paperclipDb.select({ runId: heartbeatRuns.id, agentId: heartbeatRuns.agentId, companyId: heartbeatRuns.companyId, usageJson: heartbeatRuns.usageJson, resultJson: heartbeatRuns.resultJson }).from(heartbeatRuns).where(and(inArray(heartbeatRuns.status, ['succeeded', 'failed']), gt(heartbeatRuns.finishedAt, cutoff)))` where cutoff = 5 minutes ago.
      - For each completed run, check if `agentId` has a corresponding Akasa bot: `akasaDb.select().from(bots).where(eq(bots.paperclipAgentId, run.agentId)).limit(1)`.
      - If bot found, check if verdict already exists: `akasaDb.select().from(councilVerdicts).where(and(eq(councilVerdicts.botId, bot.id), /* run-scoped check */)).limit(1)`.
      - If no verdict exists, call `runCouncilForBot(bot.executionId, bot.id, bot.soulId)` with `.catch(err => console.error('[evolution-trigger] Council failed:', { botId: bot.id, error: (err as Error).message }))` (fire-and-forget per coding conventions).
      - Count triggered evaluations and return `{ triggered }`.
    - `export function startEvolutionPolling(paperclipDb, akasaDb, intervalMs = 60000): NodeJS.Timeout`:
      - Returns `setInterval(() => { checkAndTriggerCouncilEvaluations(paperclipDb, akasaDb).catch(console.error) }, intervalMs)`.
    - Manual trigger route: `POST /trigger` — calls `checkAndTriggerCouncilEvaluations` synchronously and returns `{ triggered: N }`.

    4. Wire into akasaRouter in `services/akasa-server/src/routes/index.ts`:
    - Add imports: `import { councilRouter } from './council.js';` and `import { evolutionTriggerRouter } from './evolution-trigger.js';`
    - Add mounts: `akasaRouter.use('/akasa/verdicts', councilRouter());` and `akasaRouter.use('/akasa/evolution', evolutionTriggerRouter());`

    5. Start polling in `services/akasa-server/src/index.ts`:
    - After server starts listening, import and call `startEvolutionPolling(db, akasaDb)` where `db` is the Paperclip DB instance already created, and `akasaDb` is the `@claw/db` singleton. Import: `import { db as akasaDb } from '@claw/db';` and `import { startEvolutionPolling } from './routes/evolution-trigger.js';`.
    - Log: `console.log('[akasa-server] Evolution polling started (60s interval)')`.

    6. Run all tests: `pnpm --filter @claw/akasa-server exec vitest run`
  </action>
  <verify>
    <automated>cd /Users/tarikstafford/Desktop/Projects/claw-army && pnpm --filter @claw/akasa-server exec vitest run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - services/akasa-server/src/routes/council.ts exports `councilRouter`
    - services/akasa-server/src/routes/council.ts contains `GET` handlers for / and /:id
    - services/akasa-server/src/routes/evolution-trigger.ts exports `evolutionTriggerRouter`, `checkAndTriggerCouncilEvaluations`, `startEvolutionPolling`
    - services/akasa-server/src/routes/evolution-trigger.ts contains `heartbeatRuns` import from `@paperclipai/db`
    - services/akasa-server/src/routes/evolution-trigger.ts contains `paperclipAgentId` query pattern
    - services/akasa-server/src/routes/evolution-trigger.ts contains `setInterval`
    - services/akasa-server/src/routes/index.ts contains `councilRouter` and `evolutionTriggerRouter` imports and `.use` mounts
    - services/akasa-server/src/index.ts contains `startEvolutionPolling`
    - `pnpm --filter @claw/akasa-server exec vitest run` exits 0
  </acceptance_criteria>
  <done>Verdict CRUD routes at /api/akasa/verdicts, evolution trigger polls heartbeat_runs every 60s, completed runs for Akasa-managed agents automatically trigger council evaluation, manual trigger available at POST /api/akasa/evolution/trigger, all tests pass.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @claw/akasa-server exec vitest run` passes all tests (council + evolution-trigger + souls)
- `grep -r "councilRouter\|evolutionTriggerRouter" services/akasa-server/src/routes/index.ts` shows both mounted
- `grep "@ai-sdk/openai" services/akasa-server/src/council/devils-advocate.ts` confirms different provider
- `grep "@ai-sdk/anthropic" services/akasa-server/src/council/performance-judge.ts` confirms Anthropic for PJ
- `grep "startEvolutionPolling" services/akasa-server/src/index.ts` confirms polling started
</verification>

<success_criteria>
- Three council judges produce typed outputs (PJ + SA use Anthropic, DA uses OpenAI per CLAUDE.md constraint)
- Council runner aggregates verdicts with 0.5/0.3/0.2 weighting, stores in council_verdicts table
- Promote/Retire verdicts require human confirmation
- Evolution trigger polls heartbeat_runs for completed Akasa-managed agent runs every 60s
- Unprocessed completed runs trigger council evaluation automatically
- Verdict CRUD routes work at /api/akasa/verdicts
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/milestones/v5.0-phases/05-performance-intelligence-and-dna-capture/05-02-SUMMARY.md`
</output>
