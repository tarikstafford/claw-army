---
phase: 05-evolution-routes
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - services/akasa-server/src/god-layer/class-machine.ts
  - services/akasa-server/src/god-layer/dna-writer.ts
  - services/akasa-server/src/god-layer/negative-register.ts
  - services/akasa-server/src/god-layer/pioneer-tracker.ts
  - services/akasa-server/src/god-layer/god-layer-handler.ts
  - services/akasa-server/src/routes/god-layer.ts
  - services/akasa-server/src/routes/index.ts
  - services/akasa-server/src/__tests__/god-layer.test.ts
autonomous: true
requirements:
  - EVO-03

must_haves:
  truths:
    - "Confirming a Promote verdict triggers class transition (Novice->Understudy->Artisan) and DNA capture"
    - "Confirming a Retire verdict transitions agent to Retired class"
    - "DNA capture writes behavioral patterns from high-performing bots to dna_store with version increment"
    - "Negative signals from Demote/Monitor verdicts are recorded in negative_signal_register"
    - "Pioneer tracking detects first-in-category achievements and marks the soul"
    - "God layer is idempotent -- confirming the same verdict twice does not create duplicate transitions"
  artifacts:
    - path: "services/akasa-server/src/god-layer/god-layer-handler.ts"
      provides: "Main God Layer orchestrator that calls class-machine, dna-writer, negative-register, pioneer-tracker"
      exports: ["executeGodLayer"]
    - path: "services/akasa-server/src/god-layer/class-machine.ts"
      provides: "Pure function for agent class transitions"
      exports: ["computeClassTransition"]
    - path: "services/akasa-server/src/god-layer/dna-writer.ts"
      provides: "Captures high-performing behavioral patterns to dna_store"
      exports: ["captureDna"]
    - path: "services/akasa-server/src/god-layer/negative-register.ts"
      provides: "Records negative signals from poor-performing bots"
      exports: ["recordNegativeSignal"]
    - path: "services/akasa-server/src/god-layer/pioneer-tracker.ts"
      provides: "Tracks first-in-category achievements"
      exports: ["checkAndRecordPioneer"]
    - path: "services/akasa-server/src/routes/god-layer.ts"
      provides: "Confirm/reject verdict routes that trigger God Layer"
      exports: ["godLayerRouter"]
  key_links:
    - from: "services/akasa-server/src/routes/god-layer.ts"
      to: "services/akasa-server/src/god-layer/god-layer-handler.ts"
      via: "PATCH /:id/confirm calls executeGodLayer"
      pattern: "executeGodLayer"
    - from: "services/akasa-server/src/god-layer/god-layer-handler.ts"
      to: "@claw/db agent_classes table"
      via: "class-machine.ts computes transition, handler persists"
      pattern: "agentClasses"
    - from: "services/akasa-server/src/god-layer/dna-writer.ts"
      to: "@claw/db dna_store table"
      via: "drizzle insert with MAX(version)+1"
      pattern: "db\\.insert.*dnaStore"
---

<objective>
Port the God Layer modules from execution-service (class machine, DNA writer, negative register, pioneer tracker), create the God Layer handler that orchestrates post-verdict processing, and build confirm/reject verdict routes that trigger class transitions and DNA capture.

Purpose: The God Layer is the actuator of the evolution engine -- it turns council verdicts into concrete state changes (class promotions, DNA library writes, negative signal tracking) that make agents measurably smarter over time.

Output: Confirm/reject verdict routes at `/api/akasa/verdicts/:id/confirm` and `/reject`, God Layer handler that executes class transitions, DNA capture, negative signals, and pioneer detection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v5.0-phases/05-performance-intelligence-and-dna-capture/05-RESEARCH.md
@.planning/milestones/v5.0-phases/05-performance-intelligence-and-dna-capture/05-01-SUMMARY.md
@.planning/milestones/v5.0-phases/05-performance-intelligence-and-dna-capture/05-02-SUMMARY.md

@packages/db/src/schema/council-verdicts.ts
@packages/db/src/schema/agent-classes.ts
@packages/db/src/schema/dna-store.ts
@packages/db/src/schema/negative-signal-register.ts
@packages/db/src/schema/category-benchmarks.ts
@packages/db/src/schema/bots.ts
@packages/db/src/schema/bot-souls.ts

<interfaces>
<!-- DB schemas the executor needs -->

From packages/db/src/schema/agent-classes.ts:
```typescript
// Agent class progression: Novice -> Understudy -> Artisan -> Retired
export const agentClasses = pgTable('agent_classes', {
  id: uuid('id').primaryKey().defaultRandom(),
  botId: uuid('bot_id').notNull(),
  currentClass: varchar('current_class', { length: 50 }).notNull().default('Novice'),
  previousClass: varchar('previous_class', { length: 50 }),
  transitionReason: text('transition_reason'),
  verdictId: uuid('verdict_id'),
  transitionedAt: timestamp('transitioned_at', { withTimezone: true, precision: 3 }).notNull().defaultNow(),
  createdAt: timestamp('created_at', { withTimezone: true, precision: 3 }).notNull().defaultNow(),
});
```

From packages/db/src/schema/dna-store.ts:
```typescript
export const dnaStore = pgTable('dna_store', {
  id: uuid('id').primaryKey().defaultRandom(),
  taskCategory: varchar('task_category', { length: 255 }).notNull(),
  soulId: uuid('soul_id').notNull(),
  botId: uuid('bot_id').notNull(),
  executionId: uuid('execution_id').notNull(),
  dimensions: jsonb('dimensions').notNull(),
  compositeScore: numeric('composite_score', { precision: 5, scale: 2 }).notNull(),
  version: integer('version').notNull().default(1),
  createdAt: timestamp('created_at', { withTimezone: true, precision: 3 }).notNull().defaultNow(),
});
```

From packages/db/src/schema/negative-signal-register.ts:
```typescript
export const negativeSignalRegister = pgTable('negative_signal_register', {
  id: uuid('id').primaryKey().defaultRandom(),
  botId: uuid('bot_id').notNull(),
  executionId: uuid('execution_id').notNull(),
  soulId: uuid('soul_id'),
  signalType: varchar('signal_type', { length: 100 }).notNull(),
  severity: varchar('severity', { length: 20 }).notNull(),
  description: text('description').notNull(),
  verdictId: uuid('verdict_id'),
  createdAt: timestamp('created_at', { withTimezone: true, precision: 3 }).notNull().defaultNow(),
});
```

From packages/db/src/schema/council-verdicts.ts:
```typescript
// godLayerProcessedAt column = idempotency guard
// Verdict status: pending -> confirmed | rejected
// verdictType: Promote | Maintain | Monitor | Demote | Retire
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port God Layer modules and create God Layer handler</name>
  <files>
    services/akasa-server/src/god-layer/class-machine.ts,
    services/akasa-server/src/god-layer/dna-writer.ts,
    services/akasa-server/src/god-layer/negative-register.ts,
    services/akasa-server/src/god-layer/pioneer-tracker.ts,
    services/akasa-server/src/god-layer/god-layer-handler.ts,
    services/akasa-server/src/__tests__/god-layer.test.ts
  </files>
  <read_first>
    services/execution-service/src/god-layer/class-machine.ts,
    services/execution-service/src/god-layer/dna-writer.ts,
    services/execution-service/src/god-layer/negative-register.ts,
    services/execution-service/src/god-layer/pioneer-tracker.ts,
    services/execution-service/src/queue/god-layer-worker.ts,
    packages/db/src/schema/agent-classes.ts,
    packages/db/src/schema/dna-store.ts,
    packages/db/src/schema/negative-signal-register.ts,
    packages/db/src/schema/category-benchmarks.ts,
    packages/db/src/schema/council-verdicts.ts
  </read_first>
  <behavior>
    - Test: computeClassTransition('Novice', 'Promote') returns { newClass: 'Understudy', transitioned: true }
    - Test: computeClassTransition('Understudy', 'Promote') returns { newClass: 'Artisan', transitioned: true }
    - Test: computeClassTransition('Artisan', 'Promote') returns { newClass: 'Artisan', transitioned: false } (already at max)
    - Test: computeClassTransition('Novice', 'Retire') returns { newClass: 'Retired', transitioned: true }
    - Test: computeClassTransition('Novice', 'Maintain') returns { newClass: 'Novice', transitioned: false }
    - Test: captureDna inserts into dna_store with version = MAX(existing version for same category) + 1
    - Test: recordNegativeSignal inserts signal with correct severity based on verdict type
    - Test: executeGodLayer with Promote verdict calls class transition + DNA capture, sets godLayerProcessedAt
    - Test: executeGodLayer with Retire verdict calls class transition + negative signal, sets godLayerProcessedAt
    - Test: executeGodLayer is idempotent -- if godLayerProcessedAt is already set, returns early without changes
  </behavior>
  <action>
    1. Create `services/akasa-server/src/__tests__/god-layer.test.ts`:
    - Test `computeClassTransition` as a pure function (no mocking needed -- it's pure logic).
    - Mock `@claw/db` for `captureDna`, `recordNegativeSignal`, `checkAndRecordPioneer`, and `executeGodLayer`.
    - Mock `ioredis` for the category lock in dna-writer.
    - Test idempotency: if verdict already has `godLayerProcessedAt` set, `executeGodLayer` returns `{ processed: false, reason: 'already_processed' }`.

    2. Port `services/akasa-server/src/god-layer/class-machine.ts`:
    - Read `services/execution-service/src/god-layer/class-machine.ts` (208 lines) as reference.
    - `export function computeClassTransition(currentClass: AgentClass, verdictType: VerdictType): { newClass: AgentClass, transitioned: boolean }`
    - Class hierarchy: `Novice -> Understudy -> Artisan`. `Retired` is a terminal state reachable from any class via 'Retire' verdict.
    - `Promote`: advance one level (Novice->Understudy, Understudy->Artisan). If already Artisan, no transition.
    - `Demote`: drop one level (Artisan->Understudy, Understudy->Novice). If already Novice, no transition.
    - `Maintain`, `Monitor`: no class change.
    - `Retire`: move to `Retired` from any class.
    - Type: `type AgentClass = 'Novice' | 'Understudy' | 'Artisan' | 'Retired'`.
    - This is a pure function with zero dependencies.

    3. Port `services/akasa-server/src/god-layer/dna-writer.ts`:
    - Read `services/execution-service/src/god-layer/dna-writer.ts` (99 lines) as reference.
    - `export async function captureDna(botId, executionId, soulId, taskCategory, dimensions, compositeScore): Promise<void>`
    - Query `MAX(version)` from `dnaStore` where `taskCategory` matches, then insert with `version: maxVersion + 1`.
    - Uses `ioredis` for category-level lock to prevent concurrent version conflicts: lock key `dna:lock:${taskCategory}`, TTL 10 seconds.
    - If Redis unavailable (fail-open per coding conventions), skip lock and insert directly -- concurrent writes may produce gaps but not duplicates (UUID PK).
    - Import `db, dnaStore` from `@claw/db`.

    4. Port `services/akasa-server/src/god-layer/negative-register.ts`:
    - Read `services/execution-service/src/god-layer/negative-register.ts` (73 lines) as reference.
    - `export async function recordNegativeSignal(botId, executionId, soulId, verdictType, verdictSummary, verdictId): Promise<void>`
    - Maps verdict type to severity: `Demote` -> 'high', `Monitor` -> 'medium', `Retire` -> 'critical'.
    - Inserts into `negativeSignalRegister` with signalType derived from verdict.
    - Import `db, negativeSignalRegister` from `@claw/db`.

    5. Port `services/akasa-server/src/god-layer/pioneer-tracker.ts`:
    - Read `services/execution-service/src/god-layer/pioneer-tracker.ts` (121 lines) as reference.
    - `export async function checkAndRecordPioneer(botId, soulId, taskCategory, compositeScore): Promise<boolean>`
    - Checks `categoryBenchmarks` table for existing benchmark in this category.
    - If none exists and compositeScore > 0, inserts a new benchmark row (this bot is the pioneer).
    - Returns `true` if pioneer was established, `false` otherwise.
    - Import `db, categoryBenchmarks` from `@claw/db`.

    6. Create `services/akasa-server/src/god-layer/god-layer-handler.ts`:
    - Read `services/execution-service/src/queue/god-layer-worker.ts` (583 lines) as the reference for the orchestration logic.
    - `export async function executeGodLayer(verdictId: string): Promise<{ processed: boolean, reason?: string }>`
    - Load verdict from `councilVerdicts` by ID.
    - **Idempotency check**: if `verdict.godLayerProcessedAt` is not null, return `{ processed: false, reason: 'already_processed' }`.
    - Load bot from `bots` table, load soul from `botSouls` if `verdict.soulId` is set.
    - Get current agent class from `agentClasses` (most recent row for this botId, or default 'Novice' if none exists).
    - Call `computeClassTransition(currentClass, verdict.verdictType)`.
    - If `transitioned`: insert new row into `agentClasses` with `{ botId, currentClass: newClass, previousClass: currentClass, transitionReason: verdict.verdictSummary, verdictId }`.
    - If verdict is 'Promote' or 'Maintain' with compositeScore >= 0.7 AND soul exists: call `captureDna(botId, executionId, soulId, taskCategory, soul.dimensions, bot.compositeScore)`.
    - If verdict is 'Demote', 'Monitor', or 'Retire': call `recordNegativeSignal(botId, executionId, soulId, verdictType, verdictSummary, verdictId)`.
    - If verdict is 'Promote' AND taskCategory is set: call `checkAndRecordPioneer(botId, soulId, taskCategory, compositeScore)`.
    - Mark verdict as processed: `db.update(councilVerdicts).set({ godLayerProcessedAt: new Date() }).where(eq(councilVerdicts.id, verdictId))`.
    - Return `{ processed: true }`.
    - All sub-calls wrapped in individual try/catch -- one failure (e.g., pioneer check fails) should not block the whole God Layer. Log errors with `console.error('[god-layer]', ...)`.

    7. Run tests: `pnpm --filter @claw/akasa-server exec vitest run src/__tests__/god-layer.test.ts`
  </action>
  <verify>
    <automated>cd /Users/tarikstafford/Desktop/Projects/claw-army && pnpm --filter @claw/akasa-server exec vitest run src/__tests__/god-layer.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - services/akasa-server/src/god-layer/class-machine.ts exports `computeClassTransition`
    - services/akasa-server/src/god-layer/class-machine.ts contains `Novice`, `Understudy`, `Artisan`, `Retired`
    - services/akasa-server/src/god-layer/dna-writer.ts exports `captureDna`
    - services/akasa-server/src/god-layer/dna-writer.ts contains `dnaStore` import and `MAX` or `max` for version
    - services/akasa-server/src/god-layer/negative-register.ts exports `recordNegativeSignal`
    - services/akasa-server/src/god-layer/pioneer-tracker.ts exports `checkAndRecordPioneer`
    - services/akasa-server/src/god-layer/god-layer-handler.ts exports `executeGodLayer`
    - services/akasa-server/src/god-layer/god-layer-handler.ts contains `godLayerProcessedAt` idempotency check
    - services/akasa-server/src/god-layer/god-layer-handler.ts contains calls to `computeClassTransition`, `captureDna`, `recordNegativeSignal`, `checkAndRecordPioneer`
    - `pnpm --filter @claw/akasa-server exec vitest run src/__tests__/god-layer.test.ts` exits 0
  </acceptance_criteria>
  <done>God Layer modules ported, handler orchestrates class transitions + DNA capture + negative signals + pioneer detection, idempotency guard prevents duplicate processing, all tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create confirm/reject verdict routes, wire God Layer into akasaRouter, run full test suite</name>
  <files>
    services/akasa-server/src/routes/god-layer.ts,
    services/akasa-server/src/routes/index.ts
  </files>
  <read_first>
    services/execution-service/src/routes/verdicts.ts,
    packages/db/src/schema/council-verdicts.ts,
    services/akasa-server/src/god-layer/god-layer-handler.ts,
    services/akasa-server/src/routes/index.ts,
    services/akasa-server/src/routes/council.ts
  </read_first>
  <action>
    1. Create `services/akasa-server/src/routes/god-layer.ts`:
    - `export function godLayerRouter(): Router`
    - `PATCH /:id/confirm` — Confirms a pending verdict and triggers God Layer:
      - Validate `:id` is a UUID.
      - Load verdict from `councilVerdicts` by ID. If not found: `res.status(404).json({ error: 'Verdict not found' })`.
      - If `verdict.status !== 'pending'`: `res.status(409).json({ error: 'Verdict already processed', status: verdict.status })`.
      - Update verdict: `db.update(councilVerdicts).set({ status: 'confirmed', confirmedAt: new Date(), confirmedBy: req.body?.confirmedBy || 'system', updatedAt: new Date() }).where(eq(councilVerdicts.id, id))`.
      - Call `executeGodLayer(id)`. Log result.
      - Return `res.json({ confirmed: true, godLayerResult })`.
      - Wrap in try/catch with `next(err)`.
    - `PATCH /:id/reject` — Rejects a pending verdict (no God Layer triggered):
      - Same validation as confirm.
      - Update verdict: `db.update(councilVerdicts).set({ status: 'rejected', confirmedAt: new Date(), confirmedBy: req.body?.confirmedBy || 'system', updatedAt: new Date() }).where(eq(councilVerdicts.id, id))`.
      - Return `res.json({ rejected: true })`.
    - Import `db, councilVerdicts` from `@claw/db` and `executeGodLayer` from `../god-layer/god-layer-handler.js`.

    2. Wire into akasaRouter in `services/akasa-server/src/routes/index.ts`:
    - Add `import { godLayerRouter } from './god-layer.js';`
    - Add `akasaRouter.use('/akasa/verdicts', godLayerRouter());` — this mounts confirm/reject alongside the existing councilRouter GET routes. NOTE: Since both councilRouter and godLayerRouter use the same mount prefix `/akasa/verdicts`, they can coexist because they handle different HTTP methods on different sub-paths (GET / and GET /:id vs PATCH /:id/confirm and PATCH /:id/reject).

    3. Run full test suite:
    ```bash
    pnpm --filter @claw/akasa-server exec vitest run --reporter=verbose
    ```
    All tests from plans 01, 02, and 03 should pass.
  </action>
  <verify>
    <automated>cd /Users/tarikstafford/Desktop/Projects/claw-army && pnpm --filter @claw/akasa-server exec vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - services/akasa-server/src/routes/god-layer.ts exports `godLayerRouter`
    - services/akasa-server/src/routes/god-layer.ts contains `PATCH` routes for `/:id/confirm` and `/:id/reject`
    - services/akasa-server/src/routes/god-layer.ts contains `executeGodLayer` import
    - services/akasa-server/src/routes/god-layer.ts contains status check `verdict.status !== 'pending'` with 409 response
    - services/akasa-server/src/routes/index.ts contains `godLayerRouter` import and `.use('/akasa/verdicts'` mount
    - services/akasa-server/src/routes/index.ts contains imports for `soulsRouter`, `councilRouter`, `evolutionTriggerRouter`, `godLayerRouter`
    - `pnpm --filter @claw/akasa-server exec vitest run` exits 0 with all test files passing
  </acceptance_criteria>
  <done>Verdict confirm/reject routes live at /api/akasa/verdicts/:id/confirm and /reject, confirming triggers God Layer (class transition, DNA capture, negative signals, pioneer detection), rejecting marks verdict without God Layer, full test suite passes.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @claw/akasa-server exec vitest run --reporter=verbose` passes ALL tests (souls, injection, council, evolution-trigger, god-layer)
- `grep -r "godLayerRouter\|councilRouter\|soulsRouter\|evolutionTriggerRouter" services/akasa-server/src/routes/index.ts` shows all 4 routers mounted
- `grep "godLayerProcessedAt" services/akasa-server/src/god-layer/god-layer-handler.ts` confirms idempotency
- `grep "computeClassTransition" services/akasa-server/src/god-layer/class-machine.ts` confirms pure function
</verification>

<success_criteria>
- Confirming a Promote verdict transitions Novice->Understudy->Artisan and captures DNA
- Confirming a Retire verdict transitions to Retired class
- DNA capture writes to dna_store with incrementing version numbers
- Negative signals recorded for Demote/Monitor/Retire verdicts
- Pioneer detection works for first-in-category achievements
- God Layer is idempotent (duplicate confirm returns already_processed)
- Confirm/reject routes return appropriate status codes (200, 404, 409)
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/milestones/v5.0-phases/05-performance-intelligence-and-dna-capture/05-03-SUMMARY.md`
</output>
