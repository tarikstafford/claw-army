---
phase: 07-google-auth-gate
plan: "06"
type: execute
wave: 4
depends_on: ["07-04", "07-05"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Visiting /new-execution while logged out redirects to /login"
    - "The /login page shows a Sign in with Google button"
    - "Clicking Sign in with Google starts the Google OAuth flow"
    - "After Google OAuth, user is redirected to /new-execution"
    - "The nav shows the user's avatar, name, and Sign out link after login"
    - "Clicking Sign out returns the user to / with nav showing Deploy Crew"
    - "Submitting the /new-execution form creates an execution and redirects to /executions/{id}"
    - "POST /executions without Authorization header returns 401"
  artifacts:
    - path: "services/ui/src/routes/new-execution/+page.server.ts"
      provides: "Auth guard + form action"
    - path: "services/ui/src/routes/login/+page.svelte"
      provides: "Google sign-in page"
    - path: "services/ui/src/routes/+layout.svelte"
      provides: "User nav with avatar, name, sign-out"
    - path: "services/execution-service/src/lib/verify-auth-token.ts"
      provides: "Auth token verification"
  key_links:
    - from: "browser /new-execution (unauthenticated)"
      to: "/login"
      via: "server-side redirect(303)"
      pattern: "redirect.*login"
    - from: "/login button"
      to: "Google OAuth"
      via: "signIn('google')"
      pattern: "signIn.*google"
    - from: "form action"
      to: "POST /executions"
      via: "Authorization: Bearer"
      pattern: "Authorization.*Bearer"
---

<objective>
End-to-end human verification of the complete Google Auth Gate. Confirms the full flow works: unauthenticated redirect → login page → Google OAuth → authenticated nav → protected form submit → execution created.

Purpose: Auth flows are interactive and require a real browser with Google credentials. Automated checks cannot replace clicking through the OAuth popup and verifying the UI state changes.

Output: Verified auth gate working in local dev environment.
</objective>

<execution_context>
@/Users/tarikstafford/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarikstafford/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify complete Google Auth Gate end-to-end</name>
  <action>
Human verifies the full Google Auth Gate flow. The following was automated in Plans 01-05:
- Adapter migration: SvelteKit UI switched from adapter-static to adapter-vercel, server runtime enabled
- Auth.js setup: @auth/sveltekit installed, auth.ts + hooks.server.ts + app.d.ts + catch-all route created
- Backend protection: POST /executions requires valid Auth.js session token (401 without it)
- Login page: /login with brand-consistent dark theme and Sign in with Google button
- Nav user display: avatar (28px circle) + display name + Sign out when authenticated
- Route guard: /new-execution redirects unauthenticated users to /login
- Form action: /new-execution form submits via server action, forwards Bearer token to execution-service

Before testing, ensure these are configured in services/ui/.env:
  AUTH_SECRET=&lt;generated with: openssl rand -base64 32&gt;
  AUTH_GOOGLE_ID=&lt;from Google Cloud Console&gt;
  AUTH_GOOGLE_SECRET=&lt;from Google Cloud Console&gt;
  AUTH_TRUST_HOST=true
  EXECUTION_SERVICE_URL=http://localhost:3001

And in services/execution-service/.env:
  AUTH_SECRET=&lt;same value as services/ui/.env AUTH_SECRET&gt;

Then start both services:
  cd services/execution-service && pnpm dev
  cd services/ui && pnpm dev
  </action>
  <verify>
Run these 7 tests in sequence:

TEST 1 — Unauthenticated redirect:
Open http://localhost:5173/new-execution in a private/incognito browser window.
Expected: Immediately redirected to http://localhost:5173/login

TEST 2 — Login page appearance:
Observe the /login page.
Expected: Dark theme consistent with rest of UI. Claw Army logo, heading "Sign in to deploy", "Sign in with Google" button with Google G icon.

TEST 3 — Google OAuth flow:
Click "Sign in with Google".
Expected: Google OAuth popup/redirect opens. Select your Google account and authorize.
Expected after auth: Redirected to http://localhost:5173/new-execution

TEST 4 — Authenticated nav:
Observe the navigation bar after login.
Expected: Shows your Google profile picture (28px circle), your display name, and a "Sign out" text button. The "Deploy Crew" CTA is NOT shown.

TEST 5 — Form submission:
Fill in the /new-execution form with a test objective (e.g., "Write a hello world program"), click "Launch Mission".
Expected: Redirected to /executions/{id}. No 401 error. Form submission succeeds.

TEST 6 — Backend 401 (curl test):
Run: curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost:3001/executions -H "Content-Type: application/json" -d '{"objective":"test","maxBots":1,"allowedTools":[]}'
Expected output: 401

TEST 7 — Sign out:
Click "Sign out" in the nav.
Expected: Redirected to http://localhost:5173/. Nav shows "Deploy Crew" CTA again (no user info). Visiting /new-execution redirects to /login.

If Google OAuth does not start (404 on /auth/signin/google): check AUTH_TRUST_HOST=true is set and restart pnpm dev after adding .env values.
  </verify>
  <done>
All 7 tests pass:
1. /new-execution redirects to /login when unauthenticated
2. /login page is visually consistent with dark theme and shows Google sign-in button
3. Google OAuth flow completes and redirects to /new-execution
4. Nav shows avatar + name + Sign out after login, no Deploy Crew CTA
5. Form submission creates execution and redirects to /executions/{id}
6. curl without auth returns HTTP 401
7. Sign out clears session and restores Deploy Crew CTA

Type "approved" when all tests pass, or describe which tests failed and what you observed.
  </done>
</task>

</tasks>

<verification>
Human confirms all 7 auth gate flows work correctly in local dev.
</verification>

<success_criteria>
All manual verification flows pass. The complete Google Auth Gate is working end-to-end in local development. The auth gate blocks unauthenticated access, OAuth works, the nav reflects auth state, form submission forwards the token, and the backend rejects unauthenticated calls.
</success_criteria>

<output>
After completion, create `.planning/phases/07-google-auth-gate/07-06-SUMMARY.md`
</output>
