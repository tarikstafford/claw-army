---
phase: 02-design-system-tokens-and-typography
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - services/ui/src/routes/(app)/dashboard/+page.svelte
  - services/ui/src/routes/(app)/objectives/new/+page.svelte
  - services/ui/src/routes/(app)/new-execution/+page.svelte
  - services/ui/src/routes/(app)/negative-signals/+page.svelte
  - services/ui/src/routes/(app)/souls/[id]/+page.svelte
  - services/ui/src/routes/(app)/executions/[id]/bots/[botId]/+page.svelte
  - services/ui/src/routes/(app)/executions/[id]/report/+page.svelte
  - services/ui/src/routes/(app)/executions/[id]/pre-flight/+page.svelte
  - services/ui/src/routes/(app)/executions/[id]/+page.svelte
  - services/ui/src/routes/(app)/category-benchmarks/+page.svelte
  - services/ui/src/routes/(app)/souls/+page.svelte
  - services/ui/src/routes/(app)/guide/+page.svelte
  - services/ui/src/routes/(app)/billing/+page.svelte
  - services/ui/src/routes/(app)/verdicts/+page.svelte
  - services/ui/src/routes/(app)/objectives/+page.svelte
  - services/ui/src/routes/(app)/+layout.svelte
  - services/ui/src/routes/(marketing)/+layout.svelte
  - services/ui/src/routes/(marketing)/login/+page.svelte
  - services/ui/src/routes/(marketing)/+page.svelte
  - services/ui/src/routes/(app)/verdicts/[verdictId]/+page.svelte
  - services/ui/src/routes/(app)/objectives/[id]/+page.svelte
  - services/ui/src/routes/(app)/admin/+page.svelte
  - services/ui/src/lib/components/SoulInspectorPanel.svelte
  - services/ui/src/lib/components/SoulTierBadge.svelte
  - services/ui/src/lib/components/VerdictConfirmPanel.svelte
  - package.json
autonomous: true
requirements: [DS-01, DS-02, DS-04, DS-05, DS-06, DS-07]

must_haves:
  truths:
    - "No component references deprecated v5 token names (--bg-card, --bg-2, --bg-3, --text-faint, --border-mid, --border-hi, --violet-bright, --violet-dim, --violet-glow, --amber-dim, --teal-dim, --rose-dim, --s-*)"
    - "All components compile without CSS undefined values after app.css replacement"
    - "lint:tokens script exists and passes with exit code 0"
    - "No banned token patterns (--h-*, --d-*, --ak-*) exist anywhere in services/ui/src/"
  artifacts:
    - path: "package.json"
      provides: "lint:tokens script at workspace root"
      contains: "lint:tokens"
    - path: "services/ui/src/routes/(app)/dashboard/+page.svelte"
      provides: "Migrated dashboard using v2 tokens"
  key_links:
    - from: "package.json lint:tokens script"
      to: "services/ui/src/**/*.svelte"
      via: "grep for banned patterns"
      pattern: "lint:tokens"
---

<objective>
Migrate all 25 existing Svelte component files from v5 token names to v2 semantic aliases, then add the lint:tokens enforcement script to prevent regression.

Purpose: Without this migration, the app.css clean-slate from Plan 01 leaves components with undefined CSS custom properties — invisible text, missing backgrounds, collapsed spacing. The lint script locks the new state so v5 tokens cannot be reintroduced.
Output: All 25 consumer files updated, `lint:tokens` script passes with exit 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-system-tokens-and-typography/02-CONTEXT.md
@.planning/phases/02-design-system-tokens-and-typography/02-RESEARCH.md
@.planning/phases/02-design-system-tokens-and-typography/02-01-SUMMARY.md

<interfaces>
<!-- V5 to V2 token migration map. Use this for all substitutions. -->
<!-- Source: 02-RESEARCH.md Code Examples section -->

| Old (v5) | New (v2) | Notes |
|----------|----------|-------|
| var(--bg) | var(--bg) | KEPT — auto-switches |
| var(--bg-2) | var(--bg2) | Rename only — remove hyphen |
| var(--bg-3) | var(--bg3) | Rename only |
| var(--bg-card) | var(--card) | Rename |
| var(--bg-card-2) | var(--card) | Collapsed |
| var(--border) | var(--border) | KEPT |
| var(--border-mid) | var(--border) | Use semantic alias |
| var(--border-hi) | var(--bo-bhi) | Back Office contexts only; else var(--border) |
| var(--text) | var(--text) | KEPT |
| var(--text-muted) | var(--text-muted) | KEPT |
| var(--text-faint) | var(--bo-faint) | Back Office only; Front Office has no faint |
| var(--violet) | var(--accent) | Semantic alias |
| var(--violet-bright) | var(--accent-m) | Semantic alias |
| var(--violet-light) | var(--accent-m) | Same mapping |
| var(--violet-dim) | var(--accent-dim) | Semantic alias |
| var(--violet-glow) | rgba(124, 58, 237, 0.08) | Inline value, no named token |
| var(--amber) | var(--karma) | Semantic alias for karma context |
| var(--amber-dim) | rgba(251, 191, 36, 0.10) | Inline |
| var(--teal) | var(--bo-teal) | Back Office only |
| var(--teal-dim) | rgba(45, 212, 191, 0.10) | Inline |
| var(--rose) | var(--bo-rose) | Back Office only |
| var(--rose-dim) | rgba(244, 114, 182, 0.08) | Inline |
| var(--error) | var(--error) | KEPT as utility token |
| var(--font-display) | var(--font-display) | KEPT — new value (Cormorant) |
| var(--font-body) | var(--font-body) | KEPT — new value (DM Sans) |
| var(--font-mono) | var(--font-mono) | KEPT — fallback monospace |
| var(--s-1) | var(--space-xs) | 4px |
| var(--s-2) | var(--space-sm) | 8px |
| var(--s-3) | 12px | No direct v2 token; use explicit 12px |
| var(--s-4) | 16px | No direct v2 token; v2 --space-md is 14px |
| var(--s-5) | var(--space-lg) | 20px |
| var(--s-6) | var(--space-xl) | 28px (v5 was 24px — acceptable per D-01 approximate migration) |
| var(--s-7) | var(--space-xl) | 28px |
| var(--s-8) | var(--space-2xl) | 40px (v5 was 32px) |
| var(--s-9) | var(--space-2xl) | 40px |
| var(--s-10) | var(--space-2xl) | 40px |
| var(--s-11) | var(--space-2xl) | 40px |
| var(--s-12) | var(--space-3xl) | 60px (v5 was 48px) |
| var(--glitch-r) | REMOVE | v5 artifact |
| var(--glitch-b) | REMOVE | v5 artifact |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate all 25 Svelte consumer files from v5 to v2 tokens</name>
  <files>
    services/ui/src/routes/(app)/dashboard/+page.svelte,
    services/ui/src/routes/(app)/objectives/new/+page.svelte,
    services/ui/src/routes/(app)/new-execution/+page.svelte,
    services/ui/src/routes/(app)/negative-signals/+page.svelte,
    services/ui/src/routes/(app)/souls/[id]/+page.svelte,
    services/ui/src/routes/(app)/executions/[id]/bots/[botId]/+page.svelte,
    services/ui/src/routes/(app)/executions/[id]/report/+page.svelte,
    services/ui/src/routes/(app)/executions/[id]/pre-flight/+page.svelte,
    services/ui/src/routes/(app)/executions/[id]/+page.svelte,
    services/ui/src/routes/(app)/category-benchmarks/+page.svelte,
    services/ui/src/routes/(app)/souls/+page.svelte,
    services/ui/src/routes/(app)/guide/+page.svelte,
    services/ui/src/routes/(app)/billing/+page.svelte,
    services/ui/src/routes/(app)/verdicts/+page.svelte,
    services/ui/src/routes/(app)/objectives/+page.svelte,
    services/ui/src/routes/(app)/+layout.svelte,
    services/ui/src/routes/(marketing)/+layout.svelte,
    services/ui/src/routes/(marketing)/login/+page.svelte,
    services/ui/src/routes/(marketing)/+page.svelte,
    services/ui/src/routes/(app)/verdicts/[verdictId]/+page.svelte,
    services/ui/src/routes/(app)/objectives/[id]/+page.svelte,
    services/ui/src/routes/(app)/admin/+page.svelte,
    services/ui/src/lib/components/SoulInspectorPanel.svelte,
    services/ui/src/lib/components/SoulTierBadge.svelte,
    services/ui/src/lib/components/VerdictConfirmPanel.svelte
  </files>
  <read_first>
    - Each of the 25 files listed above (read every file before modifying to see current state)
    - services/ui/src/app.css (the new v2 token system from Plan 01 — to confirm available token names)
    - .planning/phases/02-design-system-tokens-and-typography/02-RESEARCH.md (V5 Token Migration Map table)
  </read_first>
  <action>
For each of the 25 Svelte files, open the file and perform these exact substitutions in the `<style>` blocks (and any inline `style=` attributes):

**Direct renames (search and replace):**
- `var(--bg-card-2)` -> `var(--card)`
- `var(--bg-card)` -> `var(--card)`
- `var(--bg-2)` -> `var(--bg2)`
- `var(--bg-3)` -> `var(--bg3)`
- `var(--border-mid)` -> `var(--border)`
- `var(--border-hi)` -> `var(--bo-bhi)`
- `var(--text-faint)` -> `var(--bo-faint)`
- `var(--violet-bright)` -> `var(--accent-m)`
- `var(--violet-light)` -> `var(--accent-m)`
- `var(--violet-dim)` -> `var(--accent-dim)`
- `var(--violet-glow)` -> `rgba(124, 58, 237, 0.08)`
- `var(--violet)` -> `var(--accent)` (ONLY where NOT already inside another token name like `--bo-violet`)
- `var(--amber-dim)` -> `rgba(251, 191, 36, 0.10)`
- `var(--amber)` -> `var(--karma)` (ONLY standalone, not inside `--bo-amber`)
- `var(--teal-dim)` -> `rgba(45, 212, 191, 0.10)`
- `var(--teal)` -> `var(--bo-teal)` (ONLY standalone)
- `var(--rose-dim)` -> `rgba(244, 114, 182, 0.08)`
- `var(--rose)` -> `var(--bo-rose)` (ONLY standalone)

**Spacing renames:**
- `var(--s-1)` -> `var(--space-xs)` (4px)
- `var(--s-2)` -> `var(--space-sm)` (8px)
- `var(--s-3)` -> `12px` (no direct v2 token)
- `var(--s-4)` -> `16px` (no direct v2 token; --space-md is 14px)
- `var(--s-5)` -> `var(--space-lg)` (20px)
- `var(--s-6)` -> `var(--space-xl)` (28px)
- `var(--s-7)` -> `var(--space-xl)` (28px)
- `var(--s-8)` -> `var(--space-2xl)` (40px)
- `var(--s-9)` -> `var(--space-2xl)`
- `var(--s-10)` -> `var(--space-2xl)`
- `var(--s-11)` -> `var(--space-2xl)`
- `var(--s-12)` -> `var(--space-3xl)` (60px)

**Remove v5 artifacts:** If any component references `var(--glitch-r)`, `var(--glitch-b)`, or uses v5-only CSS classes (`.r`, `.on`, `.d1`-`.d5`, `.w`, `.ws`, `.glitch`, `.sec-label`, `.sec-h2`, `.body-text`, `.btn-primary`, `.btn-ghost`, `.btn-nav`, `.live-dot`), those class references and associated inline styles should be preserved in the markup but the CSS that defined them (in app.css) is gone. If the component has its own scoped `<style>` redefining those styles, leave those scoped styles. The goal is only to replace v5 **token references** (`var(--old-name)`), not to remove component markup.

**Ordering matters:** Apply longer token names first (e.g., `--bg-card-2` before `--bg-card`, `--violet-bright` before `--violet`, `--amber-dim` before `--amber`) to avoid partial matches.

**Verification after each file:** After substitution, confirm no remaining `var(--bg-card)`, `var(--bg-2)`, `var(--text-faint)`, `var(--violet-bright)`, `var(--violet-dim)`, `var(--s-` in the file.
  </action>
  <verify>
    <automated>! grep -rn --include='*.svelte' -e 'var(--bg-card)' -e 'var(--bg-2)' -e 'var(--bg-3)' -e 'var(--text-faint)' -e 'var(--border-mid)' -e 'var(--border-hi)' -e 'var(--violet-bright)' -e 'var(--violet-dim)' -e 'var(--violet-glow)' -e 'var(--amber-dim)' -e 'var(--teal-dim)' -e 'var(--rose-dim)' -e "var(--s-" services/ui/src/</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of `var(--bg-card)` in any .svelte file under services/ui/src/
    - Zero occurrences of `var(--bg-2)` in any .svelte file under services/ui/src/
    - Zero occurrences of `var(--bg-3)` in any .svelte file under services/ui/src/
    - Zero occurrences of `var(--text-faint)` in any .svelte file under services/ui/src/
    - Zero occurrences of `var(--border-mid)` in any .svelte file under services/ui/src/
    - Zero occurrences of `var(--border-hi)` in any .svelte file under services/ui/src/
    - Zero occurrences of `var(--violet-bright)` in any .svelte file under services/ui/src/
    - Zero occurrences of `var(--violet-dim)` in any .svelte file under services/ui/src/
    - Zero occurrences of `var(--violet-glow)` in any .svelte file under services/ui/src/
    - Zero occurrences of `var(--amber-dim)` in any .svelte file under services/ui/src/
    - Zero occurrences of `var(--teal-dim)` in any .svelte file under services/ui/src/
    - Zero occurrences of `var(--rose-dim)` in any .svelte file under services/ui/src/
    - Zero occurrences of `var(--s-` in any .svelte file under services/ui/src/
  </acceptance_criteria>
  <done>All 25 Svelte files use v2 token names exclusively. No v5-specific token references remain in component styles.</done>
</task>

<task type="auto">
  <name>Task 2: Add lint:tokens enforcement script and verify clean build</name>
  <files>package.json</files>
  <read_first>
    - package.json (root — current scripts)
    - .planning/phases/02-design-system-tokens-and-typography/02-RESEARCH.md (Pattern 4: lint:tokens script)
  </read_first>
  <action>
**Step 1: Add lint:tokens script to root package.json** (per D-04)

Add this script to the `"scripts"` section of the root `package.json`:

```json
"lint:tokens": "! grep -rn --include='*.svelte' --include='*.css' --include='*.ts' -e '--h-' -e '--d-' -e '--ak-' -e 'var(--bg-card)' -e 'var(--bg-2)' -e 'var(--bg-3)' -e 'var(--text-faint)' -e 'var(--border-mid)' -e 'var(--border-hi)' -e 'var(--violet-bright)' -e 'var(--violet-dim)' -e 'var(--violet-glow)' -e 'var(--amber-dim)' -e 'var(--teal-dim)' -e 'var(--rose-dim)' -e \"var(--s-\" services/ui/src/"
```

The `!` prefix inverts grep's exit code: grep returns 0 if matches are found, `!` flips to 1 (failure). The script passes (exit 0) when grep finds nothing.

Banned patterns:
- `--h-*` — old v1.0 Screenplay tokens
- `--d-*` — old v1.0 Director's Cut tokens
- `--ak-*` — old v3.0 Akasa tokens
- All v5.0-specific token names that were replaced in Task 1

**Step 2: Run lint:tokens to verify**

```bash
pnpm lint:tokens
```

Must exit 0.

**Step 3: Run Vite build to verify font imports resolve**

```bash
pnpm --filter @claw/ui build
```

Must complete without errors. This confirms:
- All @fontsource imports resolve
- All CSS custom property references in .svelte files are valid syntax (Vite does not validate CSS var values, but it catches import resolution errors)
  </action>
  <verify>
    <automated>grep "lint:tokens" package.json && pnpm lint:tokens && pnpm --filter @claw/ui build</automated>
  </verify>
  <acceptance_criteria>
    - Root package.json contains `"lint:tokens"` in its scripts section
    - `pnpm lint:tokens` exits with code 0 (no banned patterns found)
    - `pnpm --filter @claw/ui build` exits with code 0 (Vite build succeeds)
    - The lint:tokens script checks for `--h-`, `--d-`, `--ak-` prefixes
    - The lint:tokens script checks for all v5-specific token names listed in the banned patterns
  </acceptance_criteria>
  <done>lint:tokens script is in root package.json, passes with exit 0, and the full Vite build succeeds — confirming the token migration is complete with no broken references.</done>
</task>

</tasks>

<verification>
1. `pnpm lint:tokens` exits 0 — no banned token patterns in services/ui/src/
2. `pnpm --filter @claw/ui build` exits 0 — Vite build succeeds
3. `grep -rn "var(--s-" services/ui/src/ --include="*.svelte"` returns nothing
4. `grep -rn "var(--bg-card)" services/ui/src/ --include="*.svelte"` returns nothing
5. `grep -rn "var(--violet-bright)" services/ui/src/ --include="*.svelte"` returns nothing
</verification>

<success_criteria>
- All 25 consumer files updated from v5 to v2 token names
- Zero occurrences of deprecated v5 token names in any .svelte or .css file
- lint:tokens script exists and passes
- Vite build succeeds
- No banned patterns (--h-*, --d-*, --ak-*) found
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system-tokens-and-typography/02-02-SUMMARY.md`
</output>
