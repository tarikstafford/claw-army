---
phase: 03-design-system-components-and-motion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/ui/src/lib/components/NavBar.svelte
  - services/ui/src/routes/(app)/+layout.svelte
autonomous: true
requirements: [DS-08, DS-09, DS-10, DS-12]

must_haves:
  truths:
    - "NavBar renders at 44px fixed height with backdrop blur in both worlds"
    - "4 tabs (INDRA, OFFICE, CHAT, SANCTUM) show with Press Start 2P 6px labels"
    - "Mode toggle switches between Front Office and Back Office via toggleMode()"
    - "Logo gem diamond rotates continuously with gem-spin animation"
    - "Active tab shows accent fill (plum in FO, violet in BO)"
    - "Old sidebar is fully removed from layout"
    - "Main content area has padding-top 44px instead of margin-left 220px"
    - "Old Google Fonts CDN links are removed from svelte:head"
  artifacts:
    - path: "services/ui/src/lib/components/NavBar.svelte"
      provides: "Top navigation bar with mode toggle and gem logo"
      contains: "gem-spin"
    - path: "services/ui/src/routes/(app)/+layout.svelte"
      provides: "Refactored layout with NavBar instead of sidebar"
      contains: "NavBar"
  key_links:
    - from: "services/ui/src/lib/components/NavBar.svelte"
      to: "services/ui/src/lib/mode.ts"
      via: "import toggleMode"
      pattern: "toggleMode"
    - from: "services/ui/src/routes/(app)/+layout.svelte"
      to: "services/ui/src/lib/components/NavBar.svelte"
      via: "import NavBar"
      pattern: "import NavBar"
---

<objective>
Build the NavBar component and refactor the (app) layout to replace the v5 sidebar with the new horizontal top navigation bar.

Purpose: NavBar is the primary navigation surface for Akasa. It introduces the mode toggle (Front Office / Back Office), the 4 canonical tabs per D-02, and the logo gem. Replacing the sidebar shifts the app layout from vertical sidebar to horizontal top nav.

Output: NavBar.svelte component, refactored +layout.svelte with sidebar removed and NavBar integrated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-design-system-components-and-motion/03-CONTEXT.md
@.planning/phases/03-design-system-components-and-motion/03-UI-SPEC.md
@tasks/akasa-design-guide-v2.md (sections 6.1, 8, 9, 10.4)

<interfaces>
From services/ui/src/lib/mode.ts:
```typescript
export type AkasaMode = 'front-office' | 'back-office';
export function setMode(mode: AkasaMode): void;
export function getMode(): AkasaMode;
export function toggleMode(): AkasaMode;
```

From services/ui/src/app.css (relevant tokens):
```css
--fo-plum: #3D3560;
--fo-plum-m: #6B5FA0;
--fo-bg2: #EDE9E0;
--fo-gold: #B8965A;
--bo-violet: #7C3AED;
--bo-vb: #A78BFA;
--bo-faint: rgba(236, 232, 255, 0.24);
--bo-text: #ECE8FF;
--bo-border: rgba(148, 110, 255, 0.13);
```

Semantic aliases (from app.css :root):
```css
--bg, --card, --text, --text-muted, --border, --accent, --accent-m, --karma
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NavBar.svelte component</name>
  <files>services/ui/src/lib/components/NavBar.svelte</files>
  <read_first>
    - services/ui/src/lib/mode.ts (toggleMode API)
    - services/ui/src/app.css (token names available)
    - tasks/akasa-design-guide-v2.md (section 6.1 NavBar spec)
    - .planning/phases/03-design-system-components-and-motion/03-UI-SPEC.md (NavBar visual contract)
  </read_first>
  <action>
Create `services/ui/src/lib/components/NavBar.svelte` with these exact specs per D-01 (props-first) and D-02 (nav structure):

**Script block:**
- Import `page` from `$app/stores`
- Import `toggleMode`, `getMode` from `$lib/mode`
- Import `browser` from `$app/environment`
- Props: `{ activeTab?: 'indra' | 'office' | 'chat' | 'sanctum' }` via `$props()`
- `$state` for `currentMode` initialized to `'front-office'`
- `$effect` that runs on mount (browser check) to set `currentMode = getMode()` and re-reads after toggle
- `isActive(href)` function: if `activeTab` prop provided, use it; otherwise use `$page.url.pathname.startsWith(href)`
- `handleToggle()` function: calls `toggleMode()`, updates `currentMode` state

**Template structure:**
```
<nav class="navbar">
  <div class="navbar-left">
    <a href="/indra" class="logo">
      <span class="logo-gem"></span>
      <span class="logo-text">Akasa</span>
    </a>
    <div class="nav-tabs">
      {#each tabs as tab}
        <a href={tab.href} class="nav-tab" class:active={isActive(tab.href)}>
          {tab.label}
        </a>
      {/each}
    </div>
  </div>
  <div class="navbar-right">
    <div class="mode-toggle">
      <button class="mode-btn" class:active={currentMode === 'front-office'} onclick={...}>FRONT OFFICE</button>
      <button class="mode-btn" class:active={currentMode === 'back-office'} onclick={...}>BACK OFFICE</button>
    </div>
  </div>
</nav>
```

Tabs constant array: `[{href: '/indra', label: 'INDRA'}, {href: '/office', label: 'OFFICE'}, {href: '/chat', label: 'CHAT'}, {href: '/sanctum', label: 'SANCTUM'}]` — per D-02 and DS-12 (SANCTUM not Dashboard).

**Scoped CSS (exact values from UI-SPEC):**

`.navbar`:
- `position: fixed; top: 0; left: 0; right: 0; z-index: 9999`
- `height: 44px; display: flex; align-items: center; justify-content: space-between`
- `padding: 0 20px`
- `backdrop-filter: blur(12px); -webkit-backdrop-filter: blur(12px)`
- Front Office: `background: rgba(245, 242, 236, 0.85); border-bottom: 1px solid var(--fo-rule)`
- Back Office (`:global(body.back-office) .navbar`): `background: rgba(6, 5, 14, 0.85); border-bottom: 1px solid var(--bo-border)`

`.logo-gem`:
- `display: inline-block; width: 10px; height: 10px`
- `background: var(--fo-gold)` (Front Office) / `var(--bo-amber)` (Back Office via `body.back-office` override)
- `clip-path: polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%)`
- `animation: gem-spin 5s linear infinite`
- Define `@keyframes gem-spin { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }`

`.logo-text`:
- `font-family: var(--font-display); font-size: 18px; font-weight: 600; letter-spacing: 0.04em`
- FO: `color: var(--fo-plum)`, BO: `color: var(--bo-vb)`

`.nav-tab`:
- `font-family: var(--font-label); font-size: 6px; letter-spacing: 0.05em; text-decoration: none`
- `padding: 6px 10px; border-radius: 4px; transition: all 0.2s`
- FO inactive: `color: var(--muted); background: transparent`
- FO hover: `color: var(--fo-plum); background: var(--fo-bg2)`
- FO active: `color: #fff; background: var(--fo-plum)`
- BO inactive: `color: var(--bo-faint); background: transparent`
- BO hover: `color: var(--bo-text); background: rgba(124, 58, 237, 0.10)`
- BO active: `color: #fff; background: var(--bo-violet)`

`.mode-btn`:
- `font-family: var(--font-label); font-size: 6px; letter-spacing: 0.05em`
- `padding: 4px 8px; border: none; border-radius: 3px; cursor: pointer; transition: all 0.2s`
- Inactive: `background: transparent; color: var(--text-muted)`
- Active in FO: `background: var(--fo-plum); color: #fff`
- Active in BO: `background: var(--bo-violet); color: #fff`
  </action>
  <verify>
    <automated>grep -c 'gem-spin' services/ui/src/lib/components/NavBar.svelte && grep -c 'toggleMode' services/ui/src/lib/components/NavBar.svelte && grep -c 'SANCTUM' services/ui/src/lib/components/NavBar.svelte && grep -c 'font-label' services/ui/src/lib/components/NavBar.svelte</automated>
  </verify>
  <acceptance_criteria>
    - NavBar.svelte exists at services/ui/src/lib/components/NavBar.svelte
    - File contains `gem-spin 5s linear infinite`
    - File contains `toggleMode` import from `$lib/mode`
    - File contains all 4 tab labels: `INDRA`, `OFFICE`, `CHAT`, `SANCTUM`
    - File contains `height: 44px`
    - File contains `z-index: 9999`
    - File contains `backdrop-filter: blur(12px)`
    - File contains `clip-path: polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%)`
    - File contains `font-family: var(--font-label)` for tabs
    - File contains `font-size: 6px` for tab labels
    - File contains `FRONT OFFICE` and `BACK OFFICE` button text
    - File contains `:global(body.back-office)` scoped overrides
    - File contains `$page.url.pathname` for active tab detection
  </acceptance_criteria>
  <done>NavBar component renders 44px fixed nav with logo gem animation, 4 tabs using product naming (DS-12), mode toggle calling toggleMode(), and dual-world styling via body.back-office overrides</done>
</task>

<task type="auto">
  <name>Task 2: Refactor (app) layout — replace sidebar with NavBar</name>
  <files>services/ui/src/routes/(app)/+layout.svelte</files>
  <read_first>
    - services/ui/src/routes/(app)/+layout.svelte (current sidebar layout — MUST read full file)
    - services/ui/src/lib/components/NavBar.svelte (just created in Task 1)
    - .planning/phases/03-design-system-components-and-motion/03-RESEARCH.md (Pattern 7: NavBar Layout Integration)
  </read_first>
  <action>
Refactor `services/ui/src/routes/(app)/+layout.svelte` with these exact changes:

**Remove completely:**
1. The entire `<svelte:head>` block (lines 69-74) containing Google Fonts CDN links (`fonts.googleapis.com`, `fontshare.com`)
2. The `<ParticleCanvas />` component and its import
3. The entire `<aside class="sidebar">` block (lines 78-133) and all sidebar-related markup
4. The `<button class="sidebar-backdrop">` block
5. The `<button class="hamburger">` block
6. All sidebar-related `$state` (`sidebarOpen`) and `$effect` (close sidebar on navigation)
7. The `import ParticleCanvas` line
8. All sidebar-related CSS: `.sidebar`, `.sidebar-logo`, `.logo-mark`, `.logo-text`, `.btn-deploy`, `.sidebar-nav`, `.nav-group`, `.nav-group-tag`, `.nav-link`, `.sidebar-footer`, `.status-pill`, `.user-email`, `.btn-signout`, `.hamburger`, `.hamburger-bar`, `.sidebar-backdrop`, `@media (max-width: 960px)` sidebar rules, `@keyframes lm-spin`, `@keyframes lm-spin-r`, `@keyframes lm-pulse`

**Add:**
1. `import NavBar from '$lib/components/NavBar.svelte';` in the script block (keep existing imports: `onMount`, `signOut`, `browser`, `page`, `connectLifecycleSSE`, LifecycleNotification type)
2. `<NavBar />` component right before `<main class="main-content">` in the template

**Modify:**
1. `.main-content` CSS: change `margin-left: 220px` to `padding-top: 44px`
2. Mobile media query: remove all sidebar-related rules, update `.main-content` to remove `margin-left: 0` override (no longer needed)
3. Lifecycle toasts: adjust `top: 20px` to `top: 56px` (below the 44px NavBar + 12px gap) — keep all toast markup and logic unchanged

**Keep unchanged:**
- The `isActive` function (NavBar uses its own)
- All lifecycle notification logic (`addNotification`, `dismissNotification`, `getNotificationIcon`, `getNotificationColor`)
- The lifecycle toasts markup and CSS (except top position)
- The `children` prop destructure and `{@render children()}` in main
- The `signOut` import and `session` derived (future use)
- The `connectLifecycleSSE` import and `$effect`

Wait — actually the `isActive` function in the layout is now unused since NavBar has its own. Remove the `isActive` function from the layout script. Also remove the `sidebarOpen` effect that watches `$page.url.pathname` (that was for closing sidebar on nav). Keep the `$page` import if still needed by lifecycle SSE or anything else; otherwise remove it too. Actually, `$page` is NOT used by anything remaining in the layout after sidebar removal. However, keep the `page` import since the lifecycle toasts or future features may need it — no harm in leaving it.

Actually, audit carefully: after removing sidebar, check if `page` store is still referenced anywhere in the template or remaining script. If the only reference was `isActive()`, remove both `isActive` and the `page` import. The NavBar component imports its own `page` store.
  </action>
  <verify>
    <automated>grep -c 'NavBar' services/ui/src/routes/\(app\)/+layout.svelte && grep -c 'sidebar' services/ui/src/routes/\(app\)/+layout.svelte | xargs test 0 -eq && grep -c 'padding-top: 44px' services/ui/src/routes/\(app\)/+layout.svelte && grep -c 'fonts.googleapis.com' services/ui/src/routes/\(app\)/+layout.svelte | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - File contains `import NavBar from '$lib/components/NavBar.svelte'`
    - File contains `<NavBar` in template
    - File does NOT contain `sidebar` anywhere (grep returns 0 matches)
    - File does NOT contain `fonts.googleapis.com` (CDN link removed)
    - File does NOT contain `fontshare.com` (CDN link removed)
    - File does NOT contain `ParticleCanvas` (import and usage removed)
    - File does NOT contain `hamburger` (mobile toggle removed)
    - File does NOT contain `margin-left: 220px` (old sidebar offset gone)
    - File contains `padding-top: 44px` in .main-content
    - File still contains `lifecycle-toasts` (toast system preserved)
    - File still contains `connectLifecycleSSE` (SSE preserved)
    - File still contains `signOut` (auth preserved)
    - `pnpm --filter @claw/ui exec svelte-check --threshold error` exits 0 (no type errors)
  </acceptance_criteria>
  <done>Layout uses NavBar instead of sidebar, main content offset is padding-top 44px, CDN font links removed, lifecycle toasts preserved, no type errors</done>
</task>

</tasks>

<verification>
1. `grep -c 'gem-spin' services/ui/src/lib/components/NavBar.svelte` returns >= 1
2. `grep -c 'SANCTUM' services/ui/src/lib/components/NavBar.svelte` returns >= 1
3. `grep -c 'sidebar' services/ui/src/routes/\(app\)/+layout.svelte` returns 0
4. `grep -c 'NavBar' services/ui/src/routes/\(app\)/+layout.svelte` returns >= 1
5. `grep -c 'padding-top: 44px' services/ui/src/routes/\(app\)/+layout.svelte` returns >= 1
</verification>

<success_criteria>
- NavBar.svelte exists with full dual-world styling, 4 tabs, mode toggle, gem-spin logo animation
- (app)/+layout.svelte uses NavBar, sidebar fully removed, CDN fonts removed, content offset corrected
- Product naming applied: SANCTUM tab, FRONT OFFICE / BACK OFFICE toggle labels (DS-12)
- Tier colour tokens (--tier-junior, --tier-mid, --tier-senior) already exist in app.css from Phase 2 (DS-09 satisfied by token availability)
- Agent identity tokens (--agent-indra, --agent-contr) already exist in app.css from Phase 2 (DS-10 satisfied by token availability)
</success_criteria>

<output>
After completion, create `.planning/phases/03-design-system-components-and-motion/03-01-SUMMARY.md`
</output>
