---
phase: 03-design-system-components-and-motion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - services/ui/src/lib/components/MechanicCard.svelte
  - services/ui/src/lib/components/Accordion.svelte
  - services/ui/src/lib/components/SlidePanel.svelte
  - services/ui/src/lib/components/Modal.svelte
  - services/ui/src/lib/components/KarmaCallout.svelte
autonomous: true
requirements: [DS-08, DS-11]

must_haves:
  truths:
    - "MechanicCard renders with hover translateY(-2px) lift and expandable CTA"
    - "Accordion expands/collapses with max-height 0.3s transition (never display:none)"
    - "SlidePanel slides in from right with transform 0.38s cubic-bezier(0.16, 1, 0.3, 1)"
    - "Modal shows overlay with backdrop-filter blur(8px) and closes on overlay click"
    - "KarmaCallout prefixes text with amber diamond symbol"
    - "All 5 components render without errors in Back Office mode"
  artifacts:
    - path: "services/ui/src/lib/components/MechanicCard.svelte"
      provides: "Expandable Back Office card with hover lift"
      contains: "translateY(-2px)"
    - path: "services/ui/src/lib/components/Accordion.svelte"
      provides: "Colour-coded collapsible section"
      contains: "max-height 0.3s"
    - path: "services/ui/src/lib/components/SlidePanel.svelte"
      provides: "Fixed right slide-in panel"
      contains: "cubic-bezier(0.16, 1, 0.3, 1)"
    - path: "services/ui/src/lib/components/Modal.svelte"
      provides: "Full-screen overlay modal"
      contains: "backdrop-filter: blur(8px)"
    - path: "services/ui/src/lib/components/KarmaCallout.svelte"
      provides: "Amber karma highlight block"
      contains: "--bo-amber"
  key_links: []
---

<objective>
Build the 5 Back Office-native design system components: MechanicCard, Accordion, SlidePanel, Modal, and KarmaCallout.

Purpose: These are the core interaction patterns for the Back Office (Director's Cut) world. Every technical/evolution UI in Phase 5+ and Phase 8 depends on these components existing with correct CSS specs.

Output: 5 Svelte 5 components in services/ui/src/lib/components/, each with scoped CSS matching the design guide exactly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-design-system-components-and-motion/03-CONTEXT.md
@.planning/phases/03-design-system-components-and-motion/03-UI-SPEC.md
@tasks/akasa-design-guide-v2.md (sections 6.2, 6.3, 6.4, 6.5, 6.6, 8, 11)

<interfaces>
From services/ui/src/app.css (Back Office tokens used by these components):
```css
--bo-bg: #06050E;
--bo-card: #100F20;
--bo-border: rgba(148, 110, 255, 0.13);
--bo-bhi: rgba(148, 110, 255, 0.32);
--bo-text: #ECE8FF;
--bo-muted: rgba(236, 232, 255, 0.52);
--bo-caption: rgba(236, 232, 255, 0.42);
--bo-faint: rgba(236, 232, 255, 0.24);
--bo-violet: #7C3AED;
--bo-vb: #A78BFA;
--bo-amber: #FBBF24;
--bo-teal: #2DD4BF;
--bo-rose: #F472B6;
--radius-sm: 3px;
--radius-md: 6px;
--radius-lg: 8px;
--font-display: 'Cormorant Garamond', Georgia, serif;
--font-body: 'DM Sans', system-ui, -apple-system, sans-serif;
--font-label: 'Press Start 2P', monospace;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MechanicCard, Accordion, and KarmaCallout components</name>
  <files>
    services/ui/src/lib/components/MechanicCard.svelte
    services/ui/src/lib/components/Accordion.svelte
    services/ui/src/lib/components/KarmaCallout.svelte
  </files>
  <read_first>
    - services/ui/src/app.css (token names, especially --bo-* and --radius-*)
    - .planning/phases/03-design-system-components-and-motion/03-UI-SPEC.md (MechanicCard, Accordion, KarmaCallout visual contracts)
    - tasks/akasa-design-guide-v2.md (sections 6.2, 6.3, 6.5)
  </read_first>
  <action>
Create 3 components with exact CSS from the UI-SPEC:

**MechanicCard.svelte** (per D-01 props-first, per D-03 Back Office native):

Script: `let { tag, title, summary, ctaLabel = 'CLICK TO EXPAND \u2192', onclick }: { tag: string; title: string; summary: string; ctaLabel?: string; onclick?: () => void } = $props();`

Template:
```svelte
<div class="mcard" role="button" tabindex="0" onclick={onclick} onkeydown={(e) => e.key === 'Enter' && onclick?.()}>
  <span class="mcard-tag">{tag}</span>
  <h3 class="mcard-title">{title}</h3>
  <p class="mcard-summary">{summary}</p>
  <span class="mcard-cta">{ctaLabel}</span>
</div>
```

Scoped CSS exact values:
- `.mcard`: `background: var(--bo-card); border: 1px solid var(--bo-border); border-radius: var(--radius-md); padding: 18px 20px; cursor: pointer; transition: border-color 0.2s, transform 0.15s`
- `.mcard:hover`: `border-color: rgba(148, 110, 255, 0.38); transform: translateY(-2px)`
- `.mcard-tag`: `font-family: var(--font-label); font-size: 6px; color: var(--bo-vb); letter-spacing: 0.10em; display: block; margin-bottom: 9px`
- `.mcard-title`: `font-family: var(--font-display); font-size: 18px; font-weight: 600; color: var(--bo-text); margin-bottom: 6px; line-height: 1.2`
- `.mcard-summary`: `font-family: var(--font-body); font-size: 12px; font-style: italic; color: rgba(236, 232, 255, 0.42); line-height: 1.5`
- `.mcard-cta`: `font-family: var(--font-label); font-size: 5px; color: var(--bo-vb); display: block; margin-top: 10px; opacity: 0.65; letter-spacing: 0.05em`

**Accordion.svelte** (uses Snippet for children per D-01):

Script:
```typescript
import type { Snippet } from 'svelte';
let { label, sublabel, meta, color, open = false, children }: {
  label: string; sublabel?: string; meta?: string; color: string; open?: boolean; children: Snippet;
} = $props();
let isOpen = $state(open);
function toggle() { isOpen = !isOpen; }
```

Template:
```svelte
<div class="accordion" style="--acc-color: {color}">
  <button class="accordion-header" onclick={toggle} aria-expanded={isOpen}>
    <div class="accordion-left">
      <span class="acc-dot" style="background: {color}"></span>
      <div class="acc-labels">
        <span class="acc-label">{label}</span>
        {#if sublabel}<span class="acc-sublabel">{sublabel}</span>{/if}
      </div>
    </div>
    <div class="accordion-right">
      {#if meta}<span class="acc-meta">{meta}</span>{/if}
      <span class="acc-arrow" class:open={isOpen}>&#9660;</span>
    </div>
  </button>
  <div class="accordion-body" class:open={isOpen}>
    <div class="accordion-inner">
      {@render children()}
    </div>
  </div>
</div>
```

Scoped CSS exact values:
- `.accordion`: `background: var(--bo-card); border: 1px solid var(--acc-color); border-radius: var(--radius-md); overflow: hidden`
- `.accordion-header`: `display: flex; align-items: center; justify-content: space-between; padding: 16px 20px; cursor: pointer; background: none; border: none; width: 100%; text-align: left; color: inherit`
- `.acc-dot`: `width: 10px; height: 10px; border-radius: 50%; flex-shrink: 0`
- `.acc-label`: `font-family: var(--font-label); font-size: 7px; color: var(--acc-color); margin-bottom: 5px; display: block`
- `.acc-sublabel`: `font-family: var(--font-body); font-size: 12px; color: rgba(236, 232, 255, 0.40); margin-top: 3px; display: block`
- `.acc-meta`: `font-family: var(--font-label); font-size: 7px; color: rgba(236, 232, 255, 0.40)`
- `.acc-arrow`: `font-size: 11px; color: rgba(236, 232, 255, 0.35); transition: transform 0.2s; display: inline-block`
- `.acc-arrow.open`: `transform: rotate(180deg)`
- `.accordion-body`: `max-height: 0; overflow: hidden; transition: max-height 0.3s ease`
- `.accordion-body.open`: `max-height: 600px`
- `.accordion-inner`: `padding: 0 20px 20px; border-top: 1px solid rgba(148, 110, 255, 0.12)`
- `.accordion-inner` text defaults: `font-family: var(--font-body); font-size: 14px; color: rgba(236, 232, 255, 0.55); line-height: 1.8`

**KarmaCallout.svelte** (per D-01 props-first):

Script: `let { text }: { text: string } = $props();`

Template:
```svelte
<div class="karma-callout">
  <span class="karma-diamond">&#9670;</span>
  <span class="karma-text">{text}</span>
</div>
```

Scoped CSS exact values:
- `.karma-callout`: `display: inline-flex; align-items: flex-start; gap: 6px; background: rgba(251, 191, 36, 0.10); border: 1px solid rgba(251, 191, 36, 0.30); border-radius: var(--radius-sm); padding: 8px 12px; margin-top: 8px`
- `.karma-diamond`: `color: var(--bo-amber); font-size: 12px; line-height: 1.6; flex-shrink: 0`
- `.karma-text`: `font-family: var(--font-body); font-size: 12px; color: var(--bo-amber); line-height: 1.6`
  </action>
  <verify>
    <automated>test -f services/ui/src/lib/components/MechanicCard.svelte && test -f services/ui/src/lib/components/Accordion.svelte && test -f services/ui/src/lib/components/KarmaCallout.svelte && grep -c 'translateY(-2px)' services/ui/src/lib/components/MechanicCard.svelte && grep -c 'max-height 0.3s' services/ui/src/lib/components/Accordion.svelte && grep -c 'bo-amber' services/ui/src/lib/components/KarmaCallout.svelte</automated>
  </verify>
  <acceptance_criteria>
    - MechanicCard.svelte exists and contains `translateY(-2px)` for hover lift
    - MechanicCard.svelte contains `font-size: 5px` for CTA (design guide exception)
    - MechanicCard.svelte contains `padding: 18px 20px` (component constant)
    - MechanicCard.svelte contains `CLICK TO EXPAND` as default CTA
    - Accordion.svelte exists and contains `max-height 0.3s ease` transition
    - Accordion.svelte contains `rotate(180deg)` for arrow rotation
    - Accordion.svelte contains `max-height: 600px` for open state
    - Accordion.svelte uses `{@render children()}` for body content
    - Accordion.svelte contains `--acc-color` CSS custom property
    - KarmaCallout.svelte exists and contains `--bo-amber` for text colour
    - KarmaCallout.svelte contains the diamond symbol entity (&#9670; or the unicode char)
    - KarmaCallout.svelte contains `rgba(251, 191, 36, 0.10)` background
  </acceptance_criteria>
  <done>MechanicCard, Accordion, and KarmaCallout components exist with exact CSS specs from design guide, correct motion transitions (DS-11), and proper Svelte 5 props/snippet patterns (D-01)</done>
</task>

<task type="auto">
  <name>Task 2: Create SlidePanel and Modal components</name>
  <files>
    services/ui/src/lib/components/SlidePanel.svelte
    services/ui/src/lib/components/Modal.svelte
  </files>
  <read_first>
    - services/ui/src/app.css (token names)
    - .planning/phases/03-design-system-components-and-motion/03-UI-SPEC.md (SlidePanel and Modal visual contracts)
    - tasks/akasa-design-guide-v2.md (sections 6.4, 6.6)
    - services/ui/src/lib/components/SoulInspectorPanel.svelte (existing slide panel pattern — reference only, do not copy)
  </read_first>
  <action>
Create 2 overlay components with exact CSS from the UI-SPEC:

**SlidePanel.svelte** (uses Snippet for children per D-01):

Script:
```typescript
import type { Snippet } from 'svelte';
let { open, tag, title, onclose, children }: {
  open: boolean; tag?: string; title: string; onclose: () => void; children: Snippet;
} = $props();
```

Template:
```svelte
<div class="slide-panel" class:open={open}>
  <div class="panel-header">
    <div class="panel-header-text">
      {#if tag}<span class="panel-tag">{tag}</span>{/if}
      <h2 class="panel-title">{title}</h2>
    </div>
    <button class="panel-close" onclick={onclose} aria-label="Close">&#10005;</button>
  </div>
  <div class="panel-body">
    {@render children()}
  </div>
</div>
```

Scoped CSS exact values:
- `.slide-panel`: `position: fixed; top: 44px; right: 0; width: 380px; height: calc(100vh - 44px); background: #080714; border-left: 1px solid rgba(148, 110, 255, 0.30); z-index: 200; display: flex; flex-direction: column; transform: translateX(100%); transition: transform 0.38s cubic-bezier(0.16, 1, 0.3, 1)`
- `.slide-panel.open`: `transform: translateX(0)`
- `.panel-header`: `padding: 20px 22px; border-bottom: 1px solid rgba(148, 110, 255, 0.12); display: flex; align-items: flex-start; justify-content: space-between; flex-shrink: 0`
- `.panel-tag`: `font-family: var(--font-label); font-size: 6px; color: var(--bo-vb); letter-spacing: 0.10em; display: block; margin-bottom: 7px`
- `.panel-title`: `font-family: var(--font-display); font-size: 22px; font-weight: 600; color: var(--bo-text); line-height: 1.2`
- `.panel-close`: `width: 26px; height: 26px; border-radius: 50%; background: rgba(148, 110, 255, 0.12); border: 1px solid rgba(148, 110, 255, 0.25); color: var(--bo-muted); cursor: pointer; display: flex; align-items: center; justify-content: center; font-size: 12px; transition: all 0.2s; flex-shrink: 0`
- `.panel-close:hover`: `background: var(--bo-violet); color: #fff; border-color: var(--bo-violet)`
- `.panel-body`: `flex: 1; overflow-y: auto; padding: 20px 22px; scrollbar-width: thin; scrollbar-color: rgba(148, 110, 255, 0.20) transparent`

**Modal.svelte** (uses Snippet for children per D-01):

Script:
```typescript
import type { Snippet } from 'svelte';
let { open, tag, title, onclose, children }: {
  open: boolean; tag?: string; title: string; onclose: () => void; children: Snippet;
} = $props();
```

Template:
```svelte
{#if open}
<div class="modal-overlay" onclick={onclose} role="dialog" aria-modal="true">
  <div class="modal-box" onclick={(e) => e.stopPropagation()}>
    <div class="modal-header">
      <div class="modal-header-text">
        {#if tag}<span class="modal-tag">{tag}</span>{/if}
        <h2 class="modal-title">{title}</h2>
      </div>
      <button class="modal-close" onclick={onclose} aria-label="Close">&#10005;</button>
    </div>
    <div class="modal-body">
      {@render children()}
    </div>
  </div>
</div>
{/if}
```

Scoped CSS exact values:
- `.modal-overlay`: `position: fixed; inset: 0; z-index: 9000; background: rgba(6, 5, 14, 0.88); backdrop-filter: blur(8px); -webkit-backdrop-filter: blur(8px); display: flex; align-items: center; justify-content: center`
- `.modal-box`: `background: #0D0C1E; border: 1px solid rgba(148, 110, 255, 0.35); border-radius: var(--radius-lg); width: 560px; max-width: calc(100vw - 40px); max-height: 80vh; overflow-y: auto; display: flex; flex-direction: column`
- `.modal-header`: `padding: 22px 26px; border-bottom: 1px solid rgba(148, 110, 255, 0.12); display: flex; align-items: flex-start; justify-content: space-between; position: sticky; top: 0; background: #0D0C1E; z-index: 1; flex-shrink: 0`
- `.modal-tag`: `font-family: var(--font-label); font-size: 6px; color: var(--bo-vb); letter-spacing: 0.10em; display: block; margin-bottom: 7px`
- `.modal-title`: `font-family: var(--font-display); font-size: 22px; font-weight: 600; color: var(--bo-text)`
- `.modal-close`: same as SlidePanel `.panel-close` (26x26px circle, same colours, same hover)
- `.modal-body`: `padding: 22px 26px; font-family: var(--font-body); font-size: 14px; color: var(--bo-muted); line-height: 1.85`
  </action>
  <verify>
    <automated>test -f services/ui/src/lib/components/SlidePanel.svelte && test -f services/ui/src/lib/components/Modal.svelte && grep -c 'cubic-bezier(0.16, 1, 0.3, 1)' services/ui/src/lib/components/SlidePanel.svelte && grep -c 'backdrop-filter: blur(8px)' services/ui/src/lib/components/Modal.svelte</automated>
  </verify>
  <acceptance_criteria>
    - SlidePanel.svelte exists and contains `transform 0.38s cubic-bezier(0.16, 1, 0.3, 1)` transition
    - SlidePanel.svelte contains `width: 380px`
    - SlidePanel.svelte contains `top: 44px` (below NavBar)
    - SlidePanel.svelte contains `z-index: 200`
    - SlidePanel.svelte contains `translateX(100%)` (closed state)
    - SlidePanel.svelte contains `{@render children()}`
    - SlidePanel.svelte contains `aria-label="Close"`
    - Modal.svelte exists and contains `backdrop-filter: blur(8px)`
    - Modal.svelte contains `z-index: 9000`
    - Modal.svelte contains `width: 560px`
    - Modal.svelte contains `max-height: 80vh`
    - Modal.svelte contains `{#if open}` for conditional rendering
    - Modal.svelte contains `stopPropagation` on modal box click
    - Modal.svelte contains `role="dialog"` and `aria-modal="true"`
    - Modal.svelte uses `{@render children()}` for body content
  </acceptance_criteria>
  <done>SlidePanel slides from right with GPU-composited cubic-bezier transition (DS-11), Modal shows overlay with blur and click-to-dismiss. Both use Snippet pattern for body content and share close button spec.</done>
</task>

</tasks>

<verification>
1. All 5 component files exist in `services/ui/src/lib/components/`
2. `grep -r 'translateY(-2px)' services/ui/src/lib/components/MechanicCard.svelte` returns match
3. `grep -r 'max-height 0.3s' services/ui/src/lib/components/Accordion.svelte` returns match
4. `grep -r 'cubic-bezier(0.16, 1, 0.3, 1)' services/ui/src/lib/components/SlidePanel.svelte` returns match
5. `grep -r 'backdrop-filter: blur(8px)' services/ui/src/lib/components/Modal.svelte` returns match
6. `grep -r 'bo-amber' services/ui/src/lib/components/KarmaCallout.svelte` returns match
7. No component uses `display: none` for show/hide (anti-pattern from design guide section 11)
</verification>

<success_criteria>
- 5 Back Office components (MechanicCard, Accordion, SlidePanel, Modal, KarmaCallout) exist and compile
- All GPU-composited transitions match spec: card hover 0.15s translateY, panel 0.38s cubic-bezier, accordion 0.3s max-height (DS-11)
- Components use Back Office tokens directly (--bo-*) per D-03 (world-native only)
- Props-first APIs per D-01, Snippets for nested content where appropriate
- No layout-triggering animations (no animating height, width, top, left, background-color)
</success_criteria>

<output>
After completion, create `.planning/phases/03-design-system-components-and-motion/03-02-SUMMARY.md`
</output>
