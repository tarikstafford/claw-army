---
phase: 03-design-system-components-and-motion
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - services/ui/src/app.css
autonomous: true
gap_closure: true
requirements:
  - DS-08
  - DS-09
  - DS-10
  - DS-11
  - DS-12

must_haves:
  truths:
    - "Mode switch between Front Office and Back Office animates over 0.4 seconds instead of flipping instantly"
    - "The transition uses background and color properties on the body element"
  artifacts:
    - path: "services/ui/src/app.css"
      provides: "0.4s transition on body for mode switch"
      contains: "transition"
  key_links:
    - from: "services/ui/src/app.css"
      to: "services/ui/src/lib/mode.ts"
      via: "body.back-office class toggle triggers CSS transition"
      pattern: "transition.*0\\.4s"
---

<objective>
Close the single verification gap from Phase 03: add the missing 0.4s mode switch transition to the body element in app.css.

Purpose: ROADMAP success criterion #4 requires "mode switch (0.4s)" as part of the motion system. Currently setMode() toggles the body.back-office class instantly with no CSS transition, causing a jarring palette flip. All other motion specs (card hover 0.15s, slide panel 0.38s, gem-spin 5s, typing-bounce) are already implemented.

Output: Updated app.css with transition rule on body element.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@services/ui/src/app.css
@services/ui/src/lib/mode.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 0.4s mode switch transition to body in app.css</name>
  <files>services/ui/src/app.css</files>
  <read_first>
    - services/ui/src/app.css (current body rule at line 109-117, back-office override at line 91-104)
    - services/ui/src/lib/mode.ts (setMode toggles body.back-office class — no changes needed here)
    - .planning/phases/03-design-system-components-and-motion/03-VERIFICATION.md (gap description)
  </read_first>
  <action>
    In `services/ui/src/app.css`, add a `transition` property to the existing `body` rule block (currently at line 109-117).

    Add this line after the existing `overflow-x: hidden;` line (line 116):

    ```css
    transition: background 0.4s ease, color 0.4s ease;
    ```

    This transitions the `background` and `color` properties when the `body.back-office` class is toggled by `setMode()` in mode.ts. The body rule uses `var(--bg)` and `var(--text)` which resolve to different values depending on whether `body.back-office` is present.

    Do NOT use `transition: all 0.4s ease` — that would animate every property including font-size, line-height, etc. Only background and color need the mode switch transition.

    Do NOT modify mode.ts — the class toggle mechanism is correct, only the CSS transition is missing.

    Do NOT add transition to any other element or rule — the body element is the only one that needs it since it holds the semantic alias overrides.
  </action>
  <verify>
    <automated>grep -n 'transition.*0\.4s' services/ui/src/app.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'transition: background 0.4s ease, color 0.4s ease' services/ui/src/app.css` returns 1
    - The transition line appears inside the `body {` rule block (between `body {` and its closing `}`)
    - No other files are modified
    - `grep -c 'transition' services/ui/src/lib/mode.ts` returns 0 (mode.ts unchanged)
  </acceptance_criteria>
  <done>The body rule in app.css includes `transition: background 0.4s ease, color 0.4s ease;` so that toggling body.back-office produces a smooth 0.4s palette cross-fade instead of an instant flip.</done>
</task>

</tasks>

<verification>
1. `grep -n 'transition.*0\.4s' services/ui/src/app.css` — must show the transition line within the body rule
2. `grep -c 'transition: background 0.4s ease, color 0.4s ease' services/ui/src/app.css` — must return 1
3. No changes to mode.ts: `git diff services/ui/src/lib/mode.ts` — must be empty
</verification>

<success_criteria>
- DS-11 mode switch (0.4s) gap is closed
- Body element in app.css has `transition: background 0.4s ease, color 0.4s ease`
- All other existing CSS rules remain unchanged
- Verification criterion #4 from ROADMAP ("mode switch 0.4s") is now satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/03-design-system-components-and-motion/03-04-SUMMARY.md`
</output>
