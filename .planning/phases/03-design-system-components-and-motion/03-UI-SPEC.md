---
phase: 3
slug: design-system-components-and-motion
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-23
---

# Phase 3 — UI Design Contract: Design System Components and Motion

> Visual and interaction contract for the Akasa reusable component library and motion system. Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — pure CSS custom properties |
| Preset | not applicable |
| Component library | none — bespoke Svelte 5 components |
| Icon library | none — Unicode/ASCII symbol set per design guide §9 |
| Fonts | Cormorant Garamond (display), DM Sans (body), Press Start 2P (labels) — self-hosted via @fontsource (installed Phase 2) |

Source: CONTEXT.md D-01, RESEARCH.md Standard Stack, CLAUDE.md frontend specifics.

---

## Spacing Scale

All spacing values come from `app.css` Phase 2 token set. These are the project's canonical spacing tokens.

**Governing rule:** The Akasa spacing scale is a documented custom scale defined in design guide §5.1. It does NOT follow the standard 8-point grid. The values 20, 28, 40, and 60 are multiples of 4 and are part of this custom scale. The value 14px (`--space-md`) is prescribed directly by the design guide §5.1 CSS block — see exception note below.

| Token | Value | Usage |
|-------|-------|-------|
| `--space-xs` | 4px | Icon gaps, badge padding, inline separation |
| `--space-sm` | 8px | Karma callout padding, accordion inner gap |
| `--space-md` | 14px | Card compact padding, form control height rhythm |
| `--space-lg` | 20px | Card default padding, section-tight padding, panel header padding |
| `--space-xl` | 28px | Section default padding, modal header padding |
| `--space-2xl` | 40px | Section horizontal padding, major layout margins |
| `--space-3xl` | 60px | Page-level vertical separation |

**Spacing exceptions (design-guide-prescribed — not arbitrary deviations):**

| Exception | Value | Justification |
|-----------|-------|---------------|
| `--space-md` | 14px | Prescribed by design guide §5.1 CSS block verbatim. This is a documented design-guide exception to the multiples-of-4 rule. Do not change without updating the design guide. |
| Tier badge padding | `3px 7px` | Prescribed by design guide §3.4 `.tier-badge` CSS block verbatim. Component-specific exception — tier badge is a compact UI chrome element where exact pixel values are brand-defined. Do not change without updating the design guide. |
| Logo gem dimensions | `10px × 10px` | Prescribed by design guide §6.1 logo gem spec. Brand mark fixed size — the diamond clip-path at 10×10 is a brand identity constant. Declared as brand mark exception. Do not change without brand review. |
| NavBar: fixed height | 44px | Touch target — not a spacing token |
| SlidePanel: fixed width | 380px | Layout dimension — not a spacing token |
| Modal box: fixed width | 560px, max-height 80vh | Layout constraint |

Source: design guide §5.1, app.css lines 54-61.

---

## Typography

Three typefaces, strictly role-bound. Never mix typefaces outside their assigned roles.

**Governing rule:** The 4-size / 2-weight maximum limits apply **per typeface role**, not across the full system. Each typeface has its own independent size and weight budget. With three distinct typefaces serving distinct roles, the system as a whole will have more than 4 sizes and 2 weights — this is correct and expected. The constraint prevents a single typeface from having an unwieldy scale, not from the system having three typefaces.

**Per-role compliance check:**
- Cormorant Garamond: 4 sizes (18px, 22px, clamp(26-38px), clamp(32-52px)) and 2 weights (300 light, 600 semibold) — **PASS**
- DM Sans: 4 sizes (14px, 13px, 12px, 11px) and 2 weights (400 regular, 500 medium) — **PASS**
- Press Start 2P: 4 sizes (6px, 7px, 8px, 20px) and 1 weight (400) — **PASS** (single weight is within the 2-weight limit; 20px is documented MetricTile display exception)

| Role | Typeface | Size | Weight | Line Height | Notes |
|------|----------|------|--------|-------------|-------|
| Display / heading | Cormorant Garamond | 18px (card title), 22px (panel/modal title), 26-38px (section h1, clamp), 32-52px (hero, clamp) | 300 (hero), 600 (headings) | 1.05 (hero), 1.1 (h1), 1.2 (h2/panel) | 16px minimum enforced — never below |
| Body | DM Sans | 14px (body), 13px (UI/panel), 12px (accordion sub, card summary, callout), 11px (caption) | 400 regular, 500 strong/em | 1.8 (body), 1.65 (base), 1.5 (UI), 1.55 (caption) | All readable text, form labels, descriptions |
| Label / tag / UI chrome | Press Start 2P | 6px (nav tabs, eyebrow, status, mode btn, tier badge), 7px (accordion label, tag), 8px maximum | 400 (single weight) | 1.0 (labels not meant for reading) | 6-8px only — never for readable sentences |
| Metric value (special) | Press Start 2P | 20px (MetricTile value display only) | 400 | 1.0 | Only exception to the 6-8px label rule — large display numbers in MetricTile |

Font variables (from app.css):
- `--font-display: 'Cormorant Garamond', Georgia, serif`
- `--font-body: 'DM Sans', system-ui, -apple-system, sans-serif`
- `--font-label: 'Press Start 2P', monospace`

Source: design guide §4.1–4.2, app.css lines 69-73.

---

## Color

### Two-World Color Contract

Phase 3 builds world-native components. Color is declared per world.

**Focal point:** Each world's primary visual anchor is the accent color. In the Front Office, deep plum (`--fo-plum`) grounds all primary actions and active navigation states against the warm cream field. In the Back Office, coordination violet (`--bo-violet`) anchors interactive elements against the near-black void, with amber (`--bo-amber`) as the exclusive karma signal.

#### Front Office (default — no body class)

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#F5F2EC` (`--fo-bg`) | Page background, default surface |
| Secondary (30%) | `#FDFAF6` (`--fo-card`) | Card surfaces, ChatBubble agent background |
| Alternate surface | `#EDE9E0` (`--fo-bg2`) | Section alternates, ChatBubble hover, NavBar hover states |
| Accent (10%) | `#3D3560` (`--fo-plum`) | NavBar active tab, user chat bubbles, primary CTAs, active mode button |
| Accent mid | `#6B5FA0` (`--fo-plum-m`) | Hover states on plum elements |
| Karma accent | `#B8965A` (`--fo-gold`) | Karma scores, MetricTile karma value, diamond symbol, promoted state |
| Text primary | `#0E0D0B` (`--ink`) | All body text, headings, UI text |
| Text secondary | `#7A766D` (`--muted`) | Chat sender labels, nav inactive tabs, captions |
| Border | `#D9CEBB` (`--fo-border`) | Card borders, dividers |
| Rule | `rgba(14, 13, 11, 0.10)` (`--fo-rule`) | Subtle dividers, NavBar bottom border, MetricTile border |
| Destructive | `#f87171` (`--error`) | Error states only — no destructive actions in this phase |

Accent reserved for: NavBar active tab background, user chat bubble background, primary CTA buttons, active mode toggle button, form focus rings in future phases. Never used for decorative purposes.

#### Back Office (`body.back-office`)

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#06050E` (`--bo-bg`) | Page background — never pure `#000000` |
| Secondary (30%) | `#100F20` (`--bo-card`) | All card surfaces: MechanicCard, Accordion, KarmaCallout container |
| Accent (10%) | `#7C3AED` (`--bo-violet`) | NavBar active tab (Back Office), primary CTAs, SlidePanel close hover |
| Accent bright — soul | `#A78BFA` (`--bo-vb`) | Tags, labels, MechanicCard tag, panel tag, modal tag, accordion label |
| Amber — karma only | `#FBBF24` (`--bo-amber`) | KarmaCallout text, karma scores, diamond symbol, promoted state |
| Teal — execution only | `#2DD4BF` (`--bo-teal`) | Active agent indicators, execution in progress states |
| Rose — tools only | `#F472B6` (`--bo-rose`) | Tool Nexus items, contractor indicators (future phases) |
| Text primary | `#ECE8FF` (`--bo-text`) | Primary text, headings, strong elements |
| Text secondary | `rgba(236, 232, 255, 0.52)` (`--bo-muted`) | Body copy, panel body text, modal body text |
| Text caption | `rgba(236, 232, 255, 0.42)` (`--bo-caption`) | Card summaries, accordion subtitle, italic captions |
| Text faint | `rgba(236, 232, 255, 0.24)` (`--bo-faint`) | Eyebrow labels, nav inactive tabs, system status |
| Border default | `rgba(148, 110, 255, 0.13)` (`--bo-border`) | Card borders, NavBar border, dividers |
| Border hover | `rgba(148, 110, 255, 0.32)` (`--bo-bhi`) | MechanicCard hover border |

Amber (`--bo-amber`) reserved for: KarmaCallout component exclusively, karma score displays, diamond `◆` symbol, promoted agent state. Never used for general emphasis.

Source: design guide §3.1–3.5, app.css lines 6-103.

---

## Component Inventory

Eight components delivered in this phase. All use props-first API (D-01). No Tailwind. Pure CSS with scoped `<style>` blocks.

### NavBar (Shared — both worlds explicitly styled)

**File:** `services/ui/src/lib/components/NavBar.svelte`

**Props:**
```typescript
{ activeTab?: 'indra' | 'office' | 'chat' | 'sanctum' }
```

**Visual contract:**
- Height: 44px fixed, `position: fixed; top: 0; left: 0; right: 0; z-index: 9999`
- Backdrop filter: `blur(12px)` — GPU composited
- Logo: Cormorant Garamond 18px weight 600, letter-spacing 0.04em, `--fo-plum` / `--bo-vb`
- Logo gem: 10×10px diamond shape (`clip-path: polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%)`), `--fo-gold` fill, `gem-spin 5s linear infinite` animation — permanent, cannot be removed
- Tab labels: INDRA, OFFICE, CHAT, SANCTUM — Press Start 2P 6px, letter-spacing 0.05em
- Active tab: Front Office: `--fo-plum` bg, white text. Back Office: `--bo-violet` bg, white text
- Inactive tab: Front Office: `--muted` text, transparent bg. Back Office: `--bo-faint` text, transparent bg
- Tab hover: Front Office: `--fo-plum` text, `--fo-bg2` bg. Back Office: `--bo-text` text, `rgba(124, 58, 237, 0.10)` bg
- Mode toggle: Two buttons ("FRONT OFFICE" / "BACK OFFICE"), Press Start 2P 6px. Active button gets accent fill. Calls `toggleMode()` from `services/ui/src/lib/mode.ts`
- Active tab detection: Use SvelteKit `$page.url.pathname` — no prop needed for detection, but prop allows override

**Nav routes (D-02):**
| Tab | Route | Notes |
|-----|-------|-------|
| INDRA | `/indra` | Placeholder — wired in Phase 4 |
| OFFICE | `/office` | Placeholder — wired in Phase 4 |
| CHAT | `/chat` | Placeholder — wired in Phase 4 |
| SANCTUM | `/sanctum` | Product name: Sanctum, not Dashboard (DS-12) |

**Layout impact:** Replaces v5 sidebar. Main content area shifts from `margin-left: 220px` to `padding-top: 44px`. Remove old CDN font `<svelte:head>` links from `(app)/+layout.svelte`.

---

### MechanicCard (Back Office)

**File:** `services/ui/src/lib/components/MechanicCard.svelte`

**Props:**
```typescript
{ tag: string; title: string; summary: string; ctaLabel?: string; onclick?: () => void }
```
Default ctaLabel: `"CLICK TO EXPAND →"`

**Visual contract:**
- Background: `--bo-card` (#100F20)
- Border: 1px solid `--bo-border`, hover: `rgba(148, 110, 255, 0.38)`
- Border radius: `--radius-md` (6px)
- Padding: 18px 20px
- Hover transform: `translateY(-2px)` — GPU composited
- Tag: Press Start 2P 6px, `--bo-vb`, letter-spacing 0.10em, margin-bottom 9px
- Title: Cormorant Garamond 18px weight 600, `--bo-text`, margin-bottom 6px
- Summary: DM Sans 12px italic, `rgba(236, 232, 255, 0.42)`, line-height 1.5
- CTA: Press Start 2P 5px, `--bo-vb`, margin-top 10px, opacity 0.65

---

### Accordion (Back Office)

**File:** `services/ui/src/lib/components/Accordion.svelte`

**Props:**
```typescript
{ label: string; sublabel?: string; meta?: string; color: string; open?: boolean; children: Snippet }
```
The `children` snippet renders the expanded body content.

**Visual contract:**
- Background: `--bo-card`, border: 1px solid `--acc-color` (passed via CSS custom property from `color` prop)
- Border radius: `--radius-md` (6px), overflow: hidden
- Header padding: 16px 20px
- Color dot: 10×10px circle, background = `color` prop
- Label: Press Start 2P 7px, color = `color` prop, margin-bottom 5px
- Sublabel: DM Sans 12px, `rgba(236, 232, 255, 0.40)`, margin-top 3px
- Meta: Press Start 2P 7px, `rgba(236, 232, 255, 0.40)`
- Arrow: `▼` 11px, `rgba(236, 232, 255, 0.35)`, rotates 180deg when open
- Body transition: `max-height 0.3s ease`, from 0px to 600px
- Inner padding: 0 20px 20px, border-top: 1px solid `rgba(148, 110, 255, 0.12)`
- Body text: DM Sans 14px, `rgba(236, 232, 255, 0.55)`, line-height 1.8
- Open state managed by `$state(open)` — toggled on header click

---

### SlidePanel (Back Office)

**File:** `services/ui/src/lib/components/SlidePanel.svelte`

**Props:**
```typescript
{ open: boolean; tag?: string; title: string; onclose: () => void; children: Snippet }
```

**Visual contract:**
- Position: `fixed; top: 44px; right: 0; width: 380px; height: calc(100vh - 44px)`
- Background: `#080714` (slightly lighter than `--bo-bg`)
- Border-left: 1px solid `rgba(148, 110, 255, 0.30)`
- Z-index: 200
- Transform: `translateX(100%)` when closed, `translateX(0)` when open
- Transition: `transform 0.38s cubic-bezier(0.16, 1, 0.3, 1)` — GPU composited
- Header padding: 20px 22px, border-bottom: 1px solid `rgba(148, 110, 255, 0.12)`
- Tag: Press Start 2P 6px, `--bo-vb`, letter-spacing 0.10em, margin-bottom 7px
- Title: Cormorant Garamond 22px weight 600, `--bo-text`, line-height 1.2
- Close button: 26×26px circle, `rgba(148, 110, 255, 0.12)` bg, 1px solid `rgba(148, 110, 255, 0.25)` border, hover: `--bo-violet` bg white text, transition: all 0.2s
- Body: flex 1, overflow-y auto, padding 20px 22px
- Scrollbar: thin, `rgba(148, 110, 255, 0.20)` color

---

### Modal (Back Office)

**File:** `services/ui/src/lib/components/Modal.svelte`

**Props:**
```typescript
{ open: boolean; tag?: string; title: string; onclose: () => void; children: Snippet }
```

**Visual contract:**
- Overlay: `position: fixed; inset: 0; z-index: 9000; background: rgba(6, 5, 14, 0.88); backdrop-filter: blur(8px)`
- Overlay click: closes modal (stopPropagation on box)
- Box: `background: #0D0C1E; border: 1px solid rgba(148, 110, 255, 0.35); border-radius: --radius-lg (8px); width: 560px; max-height: 80vh; overflow-y: auto`
- Header: padding 22px 26px, border-bottom `rgba(148, 110, 255, 0.12)`, sticky top 0, background `#0D0C1E`, z-index 1
- Tag: Press Start 2P 6px, `--bo-vb`, letter-spacing 0.10em, margin-bottom 7px
- Title: Cormorant Garamond 22px weight 600, `--bo-text`
- Close button: same as SlidePanel close button spec
- Body: padding 22px 26px, DM Sans 14px, `--bo-muted`, line-height 1.85
- Visibility: controlled by `open` prop, use Svelte `{#if open}` — no CSS `display:none` toggling

---

### KarmaCallout (Back Office)

**File:** `services/ui/src/lib/components/KarmaCallout.svelte`

**Props:**
```typescript
{ text: string }
```

**Visual contract:**
- Display: inline-flex, align-items flex-start, gap 6px
- Background: `rgba(251, 191, 36, 0.10)`
- Border: 1px solid `rgba(251, 191, 36, 0.30)`
- Border radius: `--radius-sm` (3px)
- Padding: 8px 12px, margin-top 8px
- Font: DM Sans 12px, `--bo-amber` color, line-height 1.6
- Always prefixed with `◆` diamond symbol in amber — rendered in component template, not passed via prop
- Never used outside karma/moat/IP compounding contexts

---

### ChatBubble (Front Office)

**File:** `services/ui/src/lib/components/ChatBubble.svelte`

**Props:**
```typescript
{ variant: 'user' | 'agent'; sender?: string; text?: string; typing?: boolean; children?: Snippet }
```

**Visual contract:**

Agent bubble:
- Background: `--fo-bg2`, border: 1px solid `--fo-rule`
- Padding: 9px 13px, max-width 76%
- Font: DM Sans 13px, line-height 1.65, color `--ink`

User bubble:
- Background: `--fo-plum`, color `#fff`, border: transparent
- Aligned: flex-end

Sender label (agent variant only, when `sender` provided):
- Press Start 2P 5px, `--muted`, letter-spacing 0.08em, margin-bottom 4px

Typing indicator (when `typing=true`):
- Three dots, 5×5px circles, `--fo-plum` background, border-radius 50%
- `typing-bounce 1.1s infinite` animation, staggered delays: 0s, 0.18s, 0.36s
- `@keyframes typing-bounce: 0%,80%,100% translateY(0); 40% translateY(-5px)`

---

### MetricTile (Front Office)

**File:** `services/ui/src/lib/components/MetricTile.svelte`

**Props:**
```typescript
{ label: string; value: string; sub?: string }
```

**Visual contract:**
- Background: `#fff` (literal white, not `--fo-card`)
- Border: 1px solid `--fo-rule`
- Box shadow: `2px 2px 0 --fo-bg3` (flat offset shadow)
- Padding: 12px 14px
- Label: Press Start 2P 5px, `--muted`, letter-spacing 0.10em, margin-bottom 7px
- Value: Press Start 2P 20px, `--ink`, line-height 1 (note: 20px is the only Press Start 2P exception above 8px)
- Sub: DM Sans 10px, `--muted`, margin-top 3px
- Laid out in `.grid-metrics` (4-column, 8px gap) at parent level

---

## Motion System Contract

Phase 3 delivers all standard transitions from design guide §8. GPU-composited properties only: `transform` and `opacity`. Never animate `height`, `width`, `top`, `left`, or `background-color`.

| Element | Transition | Notes |
|---------|-----------|-------|
| Logo gem | `gem-spin 5s linear infinite` (rotation) | Brand mark — permanent, never remove |
| NavBar mode switch | `opacity 0.4s ease, transform 0.4s ease` on world containers | Applied to page content area, not NavBar itself |
| MechanicCard hover | `border-color 0.2s, transform 0.15s` | border-color is not GPU but spec-prescribed |
| MechanicCard hover lift | `transform: translateY(-2px)` | GPU composited |
| SlidePanel open/close | `transform 0.38s cubic-bezier(0.16, 1, 0.3, 1)` | GPU composited |
| Accordion expand | `max-height 0.3s ease` | Not GPU — spec-prescribed exception for accordion |
| Accordion arrow | `transform 0.2s` (rotate 180deg) | GPU composited |
| Close buttons | `all 0.2s` | Catch-all for bg/color/border on small elements |
| NavBar tabs | `all 0.2s` | Color and background transitions |
| ChatBubble typing dots | `typing-bounce 1.1s infinite` (translateY) | GPU composited |
| Reply chips | `all 0.15s` | Small hover state |

**Motion system implementation:** Global CSS transitions defined within each component's scoped `<style>` block. No JavaScript animation library. No global `app.css` additions — component-local.

**One animation rule:** Never stack simultaneous animations on the same element. Entrance animations under 400ms.

Source: design guide §8.1–8.4, CONTEXT.md DS-11.

---

## Symbol Set

No icon library. Use Unicode/ASCII symbols per design guide §9.

| Symbol | Name | Reserved For |
|--------|------|-------------|
| `◆` | Diamond | Karma, IP moat, compounding value — always amber/gold |
| `◈` | Diamond with dot | Start / new actions |
| `⟳` | Cycle arrow | Connect / existing business |
| `▶` | Play arrow | Action buttons, send, task start |
| `▼` | Down arrow | Accordion closed state |
| `✕` | Cross | Close panel, close modal |
| `→` | Right arrow | CTA follow-on ("CLICK TO EXPAND →") |
| `★` | Star | Task token in office animation |
| `·` | Middle dot | Separator within role/rank strings |

Diamond (`◆`) rules: always amber/gold, never violet/teal/rose, precedes section eyebrows and karma callouts only.

---

## Copywriting Contract

Product naming per DS-12 and design guide §10. Applied to NavBar and all new Phase 3 components only. Existing v5 pages are NOT renamed in this phase (D-04).

| Element | Copy |
|---------|------|
| Nav tab — dashboard | `SANCTUM` (not "Dashboard") |
| Nav tab — CEO/fleet | `INDRA` |
| Nav tab — virtual office | `OFFICE` |
| Nav tab — command channel | `CHAT` |
| Mode toggle — light | `FRONT OFFICE` |
| Mode toggle — dark | `BACK OFFICE` |
| MechanicCard CTA | `CLICK TO EXPAND →` |
| Close button label | `✕` (no visible text, aria-label="Close") |
| KarmaCallout prefix | `◆` (hardcoded in component) |
| Agent working state | "working" not "running" |
| Karma references | "karma" not "score" or "points" |
| Typing indicator | aria-label="Agent is typing" |

**Voice rules (design guide §10.1):**
- Sentence case for all readable copy — not Title Case, not ALL CAPS (Press Start 2P handles caps visually)
- No em dashes — use hyphen with spaces or restructure
- No AI-sounding passive construction ("the task has been executed")
- Short, direct sentences for system copy

**Empty state:** No empty states in Phase 3 — this phase delivers components only, not data-driven pages. Empty state copy is deferred to Phase 4 feature pages.

**Error state:** No user-facing error flows in Phase 3. Destructive actions: none in this phase.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — no shadcn |
| Any third-party | none | not applicable — no third-party registries |

This phase uses zero npm packages beyond what Phase 2 installed. Pure CSS + Svelte 5 components.

---

## Component File Organization Decision

Per Claude's Discretion (CONTEXT.md): Use flat organization within `services/ui/src/lib/components/`. No subdirectories at this stage. The component set is small enough (8 components) that subdirectories add navigation overhead without benefit.

```
services/ui/src/lib/components/
├── NavBar.svelte              # Shared
├── MechanicCard.svelte        # Back Office
├── Accordion.svelte           # Back Office
├── SlidePanel.svelte          # Back Office
├── Modal.svelte               # Back Office
├── KarmaCallout.svelte        # Back Office
├── ChatBubble.svelte          # Front Office
├── MetricTile.svelte          # Front Office
├── ParticleCanvas.svelte      # Keep — canvas animation, not replaced
├── SoulTierBadge.svelte       # Keep — tier badge pattern, Phase 5+ may update
├── SoulInspectorPanel.svelte  # Keep — existing functionality preserved
└── VerdictConfirmPanel.svelte # Keep — existing functionality preserved
```

Existing v5 components are kept. They use old tokens where possible but are not updated in this phase (D-04 scope boundary).

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
