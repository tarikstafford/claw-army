---
phase: 04-sveltekit-frontend-core
plan: 04
type: execute
wave: 3
depends_on: [04-02, 04-03]
files_modified:
  - services/ui/src/routes/(app)/+error.svelte
autonomous: false
requirements: [UI-01, UI-02, UI-03, UI-04, UI-05, UI-06, UI-07]

must_haves:
  truths:
    - "All 4 tabs (INDRA, OFFICE, CHAT, SANCTUM) navigate without errors"
    - "Auth flow works: unauthenticated → /auth → sign in → /indra"
    - "Old v5 pages removed — no dead routes importing deleted modules"
    - "Error page renders correctly for 404 and API failures"
  artifacts:
    - path: "services/ui/src/routes/(app)/+error.svelte"
      provides: "Custom error page for 404 and errors"
      contains: "Nothing here"
  key_links:
    - from: "NavBar tabs"
      to: "/indra, /office, /chat, /sanctum"
      via: "href navigation"
      pattern: "href"
---

<objective>
Clean up old v5 routes, add error page, and verify the complete UI works end-to-end. This is the final polish plan that removes dead code and validates the full navigation flow.

Purpose: Ensures no dead imports break the build, error states are handled, and the full Phase 4 navigation is clean.
Output: Clean codebase with error handling and human-verified navigation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-sveltekit-frontend-core/04-CONTEXT.md
@.planning/phases/04-sveltekit-frontend-core/04-UI-SPEC.md
@.planning/phases/04-sveltekit-frontend-core/04-01-SUMMARY.md
@.planning/phases/04-sveltekit-frontend-core/04-02-SUMMARY.md
@.planning/phases/04-sveltekit-frontend-core/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove old v5 routes and add error page</name>
  <files>
    services/ui/src/routes/(app)/+error.svelte
  </files>
  <read_first>
    services/ui/src/routes/(app)/dashboard/+page.svelte,
    services/ui/src/routes/(app)/executions/[id]/+page.svelte,
    services/ui/src/routes/(app)/verdicts/+page.svelte,
    services/ui/src/routes/(app)/objectives/+page.svelte,
    services/ui/src/routes/(app)/souls/+page.svelte,
    services/ui/src/routes/(app)/billing/+page.svelte,
    services/ui/src/routes/(app)/admin/+page.svelte,
    services/ui/src/routes/(app)/negative-signals/+page.svelte,
    services/ui/src/routes/(app)/category-benchmarks/+page.svelte,
    services/ui/src/routes/(app)/new-execution/+page.svelte,
    services/ui/src/routes/(app)/guide/+page.svelte,
    .planning/phases/04-sveltekit-frontend-core/04-UI-SPEC.md
  </read_first>
  <action>
**Step 1: Remove old v5 routes**

Delete the following directories and files that belong to the old v5 UI (these import from old `$lib/api.ts` functions and `$lib/types.ts` types that no longer exist):

- `services/ui/src/routes/(app)/dashboard/` — replaced by `/indra`
- `services/ui/src/routes/(app)/executions/` — v5 execution views, no Paperclip equivalent yet
- `services/ui/src/routes/(app)/verdicts/` — v5 verdict views, Phase 5 scope
- `services/ui/src/routes/(app)/objectives/` — v5 objective views, replaced by goals/projects in OFFICE
- `services/ui/src/routes/(app)/souls/` — v5 soul inspector, Phase 8 scope
- `services/ui/src/routes/(app)/billing/` — replaced by `/sanctum`
- `services/ui/src/routes/(app)/admin/` — will be rebuilt when needed
- `services/ui/src/routes/(app)/negative-signals/` — Phase 5/8 scope
- `services/ui/src/routes/(app)/category-benchmarks/` — Phase 8 scope
- `services/ui/src/routes/(app)/new-execution/` — v5, will be rebuilt with Paperclip agent dispatch
- `services/ui/src/routes/(app)/guide/` — design guide dev page, can be kept if it doesn't import broken modules (check first). If it imports from old `$lib/types`, delete it.

Before deleting each directory, check if it imports from modules that still exist. If the page ONLY uses Phase 3 design system components (and no old api.ts functions), it can be kept. Otherwise delete.

**Step 2: Remove old marketing login route**
Check `services/ui/src/routes/(marketing)/login/+page.svelte`. If it imports from `@auth/sveltekit`, delete it — the auth page is now at `/auth`.

**Step 3: Create error page** (`services/ui/src/routes/(app)/+error.svelte`)
Per UI-SPEC copywriting contract for 404:
```svelte
<script lang="ts">
  import { page } from '$app/stores';
</script>

<div class="error-page">
  <h1 class="error-code">{$page.status}</h1>
  <p class="error-message">
    {#if $page.status === 404}
      Nothing here. <a href="/indra">Head back to the briefing.</a>
    {:else}
      Something went wrong. Try refreshing - if this keeps happening, check your connection.
    {/if}
  </p>
</div>

<style>
  .error-page {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: calc(100vh - 44px);
    text-align: center;
    padding: var(--space-2xl);
  }

  .error-code {
    font-family: var(--font-display);
    font-size: clamp(48px, 8vw, 80px);
    font-weight: 600;
    color: var(--muted);
    margin: 0 0 var(--space-md) 0;
  }

  .error-message {
    font-family: var(--font-body);
    font-size: 16px;
    color: var(--muted);
    line-height: 1.65;
  }

  .error-message a {
    color: var(--fo-plum);
    text-decoration: underline;
  }

  :global(body.back-office) .error-message a {
    color: var(--bo-violet);
  }
</style>
```

**Step 4: Verify build**
Run `pnpm --filter @claw/ui check` to confirm TypeScript compiles with no errors from broken imports.
  </action>
  <verify>
    <automated>cd /Users/tarikstafford/Desktop/Projects/claw-army && test -f services/ui/src/routes/\(app\)/+error.svelte && grep -q "Nothing here" services/ui/src/routes/\(app\)/+error.svelte && ! test -d services/ui/src/routes/\(app\)/dashboard && ! test -d services/ui/src/routes/\(app\)/verdicts && ! test -d services/ui/src/routes/\(app\)/billing && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `services/ui/src/routes/(app)/+error.svelte` exists and contains "Nothing here"
    - `services/ui/src/routes/(app)/+error.svelte` links to `/indra` for 404
    - `services/ui/src/routes/(app)/dashboard/` directory does NOT exist
    - `services/ui/src/routes/(app)/verdicts/` directory does NOT exist
    - `services/ui/src/routes/(app)/billing/` directory does NOT exist
    - `services/ui/src/routes/(app)/executions/` directory does NOT exist
    - No TypeScript errors from broken imports (`pnpm --filter @claw/ui check` passes)
  </acceptance_criteria>
  <done>Old v5 routes deleted, error page added, TypeScript build clean</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — full Phase 4 navigation walkthrough</name>
  <action>
Human verifies the complete Phase 4 SvelteKit Frontend Core — auth migration, API proxy retarget, all 4 tab routes (INDRA, OFFICE, CHAT, SANCTUM), WebSocket infrastructure, and error handling.

Verification steps:
1. Start Paperclip Express + SvelteKit dev: `pnpm dev`
2. Visit http://localhost:5173 — should redirect to /auth sign-in page
3. Verify /auth page shows Akasa gem + wordmark + "Sign in with Google" button (styled per design guide)
4. Click "Sign in with Google" — complete OAuth flow — should redirect to /indra
5. /indra: Fleet stats row visible (MetricTile components), activity feed section, approvals section
6. Click OFFICE tab — sub-nav sidebar appears with AGENTS, ISSUES, GOALS, PROJECTS
7. /office/agents: Agent list (MechanicCard grid if agents exist, empty state if not)
8. /office/issues: Issues table renders
9. /office/goals and /office/projects: Lists render
10. Click CHAT tab — thread sidebar + message panel layout
11. Click SANCTUM tab — cost metrics, "Coming in Phase 5" evolution placeholder
12. Toggle Front Office / Back Office mode — all pages restyle correctly
13. Navigate to a non-existent route like /indra/bogus — error page shows "Nothing here. Head back to the briefing."
14. Open browser DevTools Network tab — confirm no requests to old Fastify execution-service
  </action>
  <verify>Human confirms all 14 verification steps pass</verify>
  <done>Human approves the full Phase 4 navigation and visual correctness</done>
</task>

</tasks>

<verification>
1. No old v5 routes remain that import deleted modules
2. Error page handles 404 and generic errors
3. All 4 tabs navigate cleanly
4. `pnpm --filter @claw/ui check` passes
5. Human verifies visual/functional correctness
</verification>

<success_criteria>
- No broken imports from old v5 code
- Error page renders for 404 with "Nothing here" message and link to /indra
- Full navigation walkthrough: /auth → /indra → /office (4 sub-sections) → /chat → /sanctum works without errors
- TypeScript build passes cleanly
- Human approves visual and functional correctness
</success_criteria>

<output>
After completion, create `.planning/phases/04-sveltekit-frontend-core/04-04-SUMMARY.md`
</output>
