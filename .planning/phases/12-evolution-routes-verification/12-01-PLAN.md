---
phase: 12-evolution-routes-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12-evolution-routes-verification/12-VERIFICATION.md
autonomous: true
requirements:
  - EVO-01
  - EVO-02
  - EVO-03
  - EVO-04
  - EVO-05
  - EVO-06

must_haves:
  truths:
    - "VERIFICATION.md confirms all 6 EVO requirements (EVO-01 through EVO-06) are satisfied"
    - "Each EVO requirement has evidence citing specific file paths, function names, and route endpoints"
    - "EVO-06 is documented with a note that implementation uses 60s polling rather than push events"
    - "All Vitest tests pass before verification is written"
  artifacts:
    - path: ".planning/phases/12-evolution-routes-verification/12-VERIFICATION.md"
      provides: "Formal verification of all 6 EVO requirements"
      contains: "EVO-01"
  key_links:
    - from: "12-VERIFICATION.md"
      to: "services/akasa-server/src/routes/"
      via: "evidence citations"
      pattern: "services/akasa-server/src/"
---

<objective>
Create formal GSD verification artifacts for Phase 5 (Evolution Routes) by auditing the existing codebase and running the test suite. No code changes.

Purpose: Close 6 orphaned EVO requirements (EVO-01 through EVO-06) that were implemented in Phase 5 but never received verification artifacts per GSD workflow.
Output: `.planning/phases/12-evolution-routes-verification/12-VERIFICATION.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-evolution-routes-verification/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run evolution test suite and confirm all tests pass</name>
  <files>.planning/phases/12-evolution-routes-verification/12-VERIFICATION.md</files>
  <read_first>
    - services/akasa-server/src/routes/index.ts
    - services/akasa-server/src/routes/souls.ts
    - services/akasa-server/src/routes/council.ts
    - services/akasa-server/src/routes/god-layer.ts
    - services/akasa-server/src/routes/evolution-trigger.ts
    - services/akasa-server/src/services/soul-generator.ts
    - services/akasa-server/src/services/soul-injector.ts
    - services/akasa-server/src/council/council-runner.ts
    - services/akasa-server/src/council/performance-judge.ts
    - services/akasa-server/src/council/soul-analyst.ts
    - services/akasa-server/src/council/devils-advocate.ts
    - services/akasa-server/src/god-layer/god-layer-handler.ts
    - services/akasa-server/src/god-layer/class-machine.ts
    - services/akasa-server/src/god-layer/dna-writer.ts
    - services/akasa-server/src/god-layer/negative-register.ts
    - services/akasa-server/src/god-layer/pioneer-tracker.ts
    - services/akasa-server/src/index.ts
    - .planning/REQUIREMENTS.md
    - .planning/phases/12-evolution-routes-verification/12-RESEARCH.md
  </read_first>
  <action>
    1. Run `pnpm --filter @claw/akasa-server exec vitest run --reporter=verbose` and capture the output. All evolution-related tests must pass:
       - `src/__tests__/souls.test.ts` (EVO-01)
       - `src/__tests__/soul-injection.test.ts` (EVO-05)
       - `src/__tests__/council.test.ts` (EVO-02)
       - `src/__tests__/god-layer.test.ts` (EVO-03)
       - `src/__tests__/evolution-trigger.test.ts` (EVO-04, EVO-06)

    2. Read all source files listed in `read_first` above. For each EVO requirement, collect the following evidence:
       - **EVO-01 (Soul System Routes):** Confirm `soulsRouter()` is mounted at `/akasa/souls` in `routes/index.ts`. Confirm 5 routes exist: GET /, GET /:id, POST /generate, POST /:id/mutate, POST /inject. Confirm `generateSoul()` and `generateMutatedSoul()` exist in `soul-generator.ts`. Confirm `injectSoulIntoAgent()` exists in `soul-injector.ts`. Confirm the `akasaRouter` is passed as `extraApiRouter` to Paperclip's `createApp()` in `index.ts`.
       - **EVO-02 (Council Evaluation Routes):** Confirm `councilRouter()` is mounted at `/akasa/verdicts` in `routes/index.ts`. Confirm `runCouncilForBot()` orchestrates 3 judges in `council-runner.ts`. Confirm Performance Judge uses `@ai-sdk/anthropic`, Soul Analyst uses `@ai-sdk/anthropic`, Devil's Advocate uses `@ai-sdk/openai` (different provider family). Confirm weighted aggregation (0.5/0.3/0.2) in `computeWeightedVerdict()`. Confirm `requiresHumanConfirmation=true` for Promote and Retire.
       - **EVO-03 (God Layer Routes):** Confirm `godLayerRouter()` is mounted at `/akasa/verdicts` in `routes/index.ts`. Confirm `executeGodLayer()` in `god-layer-handler.ts` orchestrates class-machine, dna-writer, negative-register, pioneer-tracker. Confirm PATCH /:id/confirm and PATCH /:id/reject routes exist. Confirm class transition rules in `class-machine.ts`: Promote (Novice->Understudy, Understudy->Artisan), Demote (Artisan->Understudy, Understudy->Novice), Retire (any->Retired). Confirm idempotency via `godLayerProcessedAt` check.
       - **EVO-04 (Karpathy Loop):** Confirm `checkAndTriggerCouncilEvaluations()` in `evolution-trigger.ts` polls `heartbeat_runs` WHERE status IN ('succeeded','failed') AND finishedAt > (now - 5 minutes). Confirm it matches against `bots.paperclipAgentId`, deduplicates against `council_verdicts`, and fires `runCouncilForBot()`. Confirm `startEvolutionPolling()` is called in `index.ts`. Confirm POST /api/akasa/evolution/trigger manual endpoint exists.
       - **EVO-05 (Soul Injection):** Confirm `injectSoulIntoAgent()` in `soul-injector.ts` writes SOUL.md to `~/.akasa/souls/{soulId}.md` and patches agent's `adapterConfig.instructionsFilePath` (default) or `adapterConfig.systemPrompt` (openai_compatible). Confirm POST /api/akasa/souls/inject route exists.
       - **EVO-06 (Evolution Event Hooks):** Confirm `startEvolutionPolling()` starts a 60-second interval in `index.ts`. Confirm POST /api/akasa/evolution/trigger exists as manual hook. **IMPORTANT:** Document that the implementation uses 60s DB polling of `heartbeat_runs` rather than Paperclip push events. Note this is functionally equivalent (every heartbeat completion triggers council within 60s) but the mechanism is polling, not event-driven push. Mark as SATISFIED with architectural note.

    3. Write `12-VERIFICATION.md` with this structure:
       ```
       # Phase 12: Evolution Routes Verification

       **Verified:** {date}
       **Verifier:** Claude (automated)
       **Test suite:** All passing (include test count from vitest output)

       ## Requirement Verification

       ### EVO-01: Soul System Routes — SATISFIED
       {evidence table with file paths, function names, route paths}
       {cite specific line numbers or function signatures found}

       ### EVO-02: Council Evaluation Routes — SATISFIED
       {evidence table}

       ### EVO-03: God Layer Routes — SATISFIED
       {evidence table}

       ### EVO-04: Karpathy Loop — SATISFIED
       {evidence table with loop flow diagram}

       ### EVO-05: Soul Injection — SATISFIED
       {evidence table with injection flow}

       ### EVO-06: Evolution Event Hooks — SATISFIED (with architectural note)
       {evidence table}
       **Architectural note:** Implementation uses 60s DB polling of heartbeat_runs, not Paperclip push events. Functionally equivalent — every heartbeat completion triggers council evaluation within 60 seconds. This approach was chosen because Paperclip does not expose a heartbeat completion webhook at this integration depth.

       ## Summary

       | Requirement | Status | Confidence | Key Evidence |
       |-------------|--------|------------|--------------|
       | EVO-01 | SATISFIED | HIGH | soulsRouter() at /akasa/souls, 5 routes, soul-generator.ts |
       | EVO-02 | SATISFIED | HIGH | councilRouter(), 3 judges (2 Anthropic + 1 OpenAI), weighted verdict |
       | EVO-03 | SATISFIED | HIGH | godLayerRouter(), class-machine, dna-writer, pioneer-tracker |
       | EVO-04 | SATISFIED | HIGH | evolution-trigger.ts polling loop, index.ts startEvolutionPolling() |
       | EVO-05 | SATISFIED | HIGH | soul-injector.ts, POST /api/akasa/souls/inject |
       | EVO-06 | SATISFIED | MEDIUM | 60s polling (not push events), POST /api/akasa/evolution/trigger |

       ## Test Evidence
       {paste vitest --reporter=verbose output for evolution test files}
       ```

       Each evidence block MUST include:
       - Exact file path (e.g., `services/akasa-server/src/routes/souls.ts`)
       - Function/export name (e.g., `soulsRouter()`, `generateSoul()`)
       - Route path (e.g., `GET /api/akasa/souls`)
       - Test file that covers it (e.g., `src/__tests__/souls.test.ts`)
  </action>
  <verify>
    <automated>grep -c "SATISFIED" .planning/phases/12-evolution-routes-verification/12-VERIFICATION.md | grep -q "^6$" && grep "EVO-01" .planning/phases/12-evolution-routes-verification/12-VERIFICATION.md && grep "EVO-06" .planning/phases/12-evolution-routes-verification/12-VERIFICATION.md && grep "polling" .planning/phases/12-evolution-routes-verification/12-VERIFICATION.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - 12-VERIFICATION.md exists at `.planning/phases/12-evolution-routes-verification/12-VERIFICATION.md`
    - File contains exactly 6 occurrences of "SATISFIED" (one per EVO requirement)
    - File contains sections for EVO-01, EVO-02, EVO-03, EVO-04, EVO-05, EVO-06
    - EVO-01 section references `soulsRouter()` and `services/akasa-server/src/routes/souls.ts`
    - EVO-02 section references `councilRouter()` and `council-runner.ts`
    - EVO-03 section references `godLayerRouter()` and `god-layer-handler.ts`
    - EVO-04 section references `checkAndTriggerCouncilEvaluations()` and `evolution-trigger.ts`
    - EVO-05 section references `injectSoulIntoAgent()` and `soul-injector.ts`
    - EVO-06 section contains "polling" and "60" documenting the polling mechanism
    - File contains a Summary table with all 6 requirements
    - File contains test evidence section with vitest output
    - `pnpm --filter @claw/akasa-server exec vitest run` exits 0 (all tests pass)
  </acceptance_criteria>
  <done>VERIFICATION.md exists with all 6 EVO requirements marked SATISFIED, each with file-path-level evidence, and the test suite passes</done>
</task>

</tasks>

<verification>
- `grep -c "SATISFIED" .planning/phases/12-evolution-routes-verification/12-VERIFICATION.md` returns 6
- `grep "EVO-0[1-6]" .planning/phases/12-evolution-routes-verification/12-VERIFICATION.md | wc -l` returns at least 12 (header + summary table)
- `grep "polling" .planning/phases/12-evolution-routes-verification/12-VERIFICATION.md` returns matches (EVO-06 architectural note)
- `grep "services/akasa-server/src/" .planning/phases/12-evolution-routes-verification/12-VERIFICATION.md | wc -l` returns 10+ (file path citations)
</verification>

<success_criteria>
1. `.planning/phases/12-evolution-routes-verification/12-VERIFICATION.md` exists and is complete
2. All 6 EVO requirements (EVO-01 through EVO-06) are marked SATISFIED with file-level evidence
3. EVO-06 includes an architectural note about polling vs push events
4. Test suite passes (`pnpm --filter @claw/akasa-server exec vitest run` exits 0)
</success_criteria>

<output>
After completion, create `.planning/phases/12-evolution-routes-verification/12-01-SUMMARY.md`
</output>
