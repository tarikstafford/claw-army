---
phase: 22-v3-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/ui/src/routes/executions/[id]/report/+page.svelte
  - services/ui/src/routes/executions/[id]/+page.svelte
  - .planning/ROADMAP.md
autonomous: true

must_haves:
  truths:
    - "Dead CSS rules (.class-badge, .class-novice, .class-understudy, .class-artisan, .class-retired, .class-none) no longer exist in report/+page.svelte"
    - "VerdictConfirmPanel on the execution detail page receives the authenticated user email as userId instead of hardcoded 'operator'"
    - "ROADMAP.md Phase 15 plan bullets reflect actual completion status: 15-01, 15-02, 15-03 checked, 15-04 unchecked"
  artifacts:
    - path: "services/ui/src/routes/executions/[id]/report/+page.svelte"
      provides: "Report page without orphaned class-badge CSS"
    - path: "services/ui/src/routes/executions/[id]/+page.svelte"
      provides: "Execution detail page with session-derived userId"
    - path: ".planning/ROADMAP.md"
      provides: "Accurate Phase 15 plan completion checkboxes"
  key_links:
    - from: "services/ui/src/routes/executions/[id]/+page.svelte"
      to: "Auth.js session via layout data"
      via: "let { data } = $props() + $derived(data.session?.user?.email)"
      pattern: "data\\.session\\?\\.user\\?\\.email"
---

<objective>
Remove dead CSS, wire real userId into VerdictConfirmPanel, and correct Phase 15 ROADMAP checkboxes.

Purpose: Clear the three non-critical tech debt items surfaced by the v3.0 audit before archiving the milestone.
Output: Three files cleaned up -- zero functional changes beyond userId now reflecting the actual authenticated user.
</objective>

<execution_context>
@/Users/tarikstafford/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarikstafford/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-v3-tech-debt-cleanup/22-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead CSS from report page and wire real userId into execution detail page</name>
  <files>
    services/ui/src/routes/executions/[id]/report/+page.svelte
    services/ui/src/routes/executions/[id]/+page.svelte
  </files>
  <action>
**report/+page.svelte -- Delete dead CSS (lines 376-415):**

Delete the entire block from the `/* Agent class badges */` comment (line 376) through the closing brace of `.class-none` (line 415). This is 40 lines of orphaned CSS that became dead when Phase 18-02 replaced the raw span with the SoulTierBadge component. The line immediately before (line 375, blank line after `.tier-none`) and the line immediately after (line 416, blank line before `/* Pioneer badge */`) should remain -- just remove the 40-line dead block between them.

IMPORTANT: Do NOT touch `.class-badge` / `.class-novice` / etc. CSS in any other file. These class names are still LIVE in `objectives/+page.svelte`, `objectives/[id]/+page.svelte`, and `SoulInspectorPanel.svelte`.

**+page.svelte -- Wire real userId from Auth.js session:**

1. Add `let { data } = $props();` as a new line AFTER the import block (after line 9, the VerdictConfirmPanel import) and BEFORE `const executionId = $derived(page.params.id ?? '');` (currently line 11). This follows the established pattern from `verdicts/+page.svelte`.

2. Add `let userId = $derived(data.session?.user?.email ?? 'operator');` on the line immediately after `let { data } = $props();`. The `?? 'operator'` fallback matches the established pattern in `verdicts/+page.svelte` and `verdicts/[verdictId]/+page.svelte`.

3. On the VerdictConfirmPanel usage (currently line 253), change `userId="operator"` to `userId={userId}`.

Do NOT use `page.data.session` -- this codebase consistently uses `let { data } = $props()` for session access, reserving the `page` store for route params only.
  </action>
  <verify>
1. `grep -c "class-badge\|class-novice\|class-understudy\|class-artisan\|class-retired\|class-none" services/ui/src/routes/executions/[id]/report/+page.svelte` returns 0
2. `grep 'userId="operator"' services/ui/src/routes/executions/[id]/+page.svelte` returns no matches
3. `grep 'data.session?.user?.email' services/ui/src/routes/executions/[id]/+page.svelte` returns 1 match
4. `grep "let { data } = \$props()" services/ui/src/routes/executions/[id]/+page.svelte` returns 1 match
5. `cd services/ui && npx svelte-check --threshold error` passes with no new errors (or the existing error count does not increase)
  </verify>
  <done>
- report/+page.svelte contains zero references to .class-badge, .class-novice, .class-understudy, .class-artisan, .class-retired, or .class-none in either HTML or CSS
- executions/[id]/+page.svelte derives userId from data.session?.user?.email with 'operator' fallback
- VerdictConfirmPanel receives the derived userId variable, not a string literal
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct Phase 15 ROADMAP plan checkboxes</name>
  <files>.planning/ROADMAP.md</files>
  <action>
In `.planning/ROADMAP.md`, locate the Phase 15 "Plans:" section (currently around lines 69-73). Change the first three plan bullets from `[ ]` to `[x]`:

```
- [x] 15-01-PLAN.md — GCE startup script hardening + errorMessage DB column (BOT-01, BOT-02, BOT-04 foundation)
- [x] 15-02-PLAN.md — Ready handler validation + spawn timeout (BOT-03, BOT-04)
- [x] 15-03-PLAN.md — Dispatch round-trip validation + UI error surface (BOT-05, BOT-06)
- [ ] 15-04-PLAN.md — Fix spawn timeout status overwrite (gap closure)
```

15-04 stays unchecked -- no 15-04-SUMMARY.md exists.

Do NOT change the progress table row for Phase 15 -- it already correctly reads `3/4 | In Progress`.
  </action>
  <verify>
1. `grep '\[x\] 15-01' .planning/ROADMAP.md` returns 1 match
2. `grep '\[x\] 15-02' .planning/ROADMAP.md` returns 1 match
3. `grep '\[x\] 15-03' .planning/ROADMAP.md` returns 1 match
4. `grep '\[ \] 15-04' .planning/ROADMAP.md` returns 1 match
  </verify>
  <done>
- ROADMAP.md Phase 15 shows 15-01, 15-02, 15-03 as [x] (completed) and 15-04 as [ ] (incomplete)
- Progress table row unchanged at "3/4 | In Progress"
  </done>
</task>

</tasks>

<verification>
1. No dead CSS classes remain in report/+page.svelte (grep returns 0 matches)
2. userId is derived from Auth.js session in executions/[id]/+page.svelte (grep confirms pattern)
3. ROADMAP Phase 15 bullets accurately reflect 3/4 completion (grep confirms checkboxes)
4. Svelte type-check passes without new errors
5. No other files modified (git diff --name-only shows exactly the 3 target files)
</verification>

<success_criteria>
- Zero orphaned .class-* CSS rules in report/+page.svelte
- VerdictConfirmPanel receives authenticated user email (not "operator") on the execution detail page
- ROADMAP.md Phase 15 plan checkboxes match actual SUMMARY file existence (3 checked, 1 unchecked)
- svelte-check passes
</success_criteria>

<output>
After completion, create `.planning/phases/22-v3-tech-debt-cleanup/22-01-SUMMARY.md`
</output>
