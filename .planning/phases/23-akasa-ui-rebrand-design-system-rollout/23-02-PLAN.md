---
phase: 23-akasa-ui-rebrand-design-system-rollout
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - services/ui/src/routes/new-execution/+page.svelte
  - services/ui/src/routes/executions/[id]/+page.svelte
autonomous: true

must_haves:
  truths:
    - "The new-execution page renders with Akasa dark theme and retains objectiveId URL param pre-fill logic from v3.0"
    - "The execution monitor page renders with Akasa dark theme including live bot cards, SSE updates, and all existing functionality"
    - "Both pages use only Akasa CSS tokens — no old tokens or hardcoded hex colors"
  artifacts:
    - path: "services/ui/src/routes/new-execution/+page.svelte"
      provides: "Akasa-styled execution launch form with objectiveId support"
      contains: "objectiveId"
    - path: "services/ui/src/routes/executions/[id]/+page.svelte"
      provides: "Akasa-styled execution monitor"
      contains: "var(--"
  key_links:
    - from: "services/ui/src/routes/new-execution/+page.svelte"
      to: "services/ui/src/routes/new-execution/+page.server.ts"
      via: "form action (POST)"
      pattern: "objectiveId"
    - from: "services/ui/src/routes/objectives/[id]/+page.svelte"
      to: "services/ui/src/routes/new-execution/+page.svelte"
      via: "URL param ?objectiveId="
      pattern: "objectiveId"
---

<objective>
Merge the two remaining improvement/ui rebranded pages — new-execution and execution monitor — onto the current branch, preserving v3.0-only logic (objectiveId URL params, hidden input).

Purpose: These are the last two files where improvement/ui has a complete rebrand. After this plan, all improvement/ui work is fully integrated and the remaining restyling is net-new work against pages that were never touched in improvement/ui.

Output: new-execution and execution monitor pages fully Akasa-themed with all v3.0 functionality intact.
</objective>

<execution_context>
@/Users/tarikstafford/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarikstafford/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-akasa-ui-rebrand-design-system-rollout/23-RESEARCH.md
@.planning/phases/23-akasa-ui-rebrand-design-system-rollout/23-01-SUMMARY.md
@services/ui/src/routes/new-execution/+page.svelte
@services/ui/src/routes/new-execution/+page.server.ts
@services/ui/src/routes/executions/[id]/+page.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge new-execution page — Akasa styles + objectiveId logic</name>
  <files>services/ui/src/routes/new-execution/+page.svelte</files>
  <action>
This is the most delicate merge — both branches modified this file. Take the improvement/ui version as the base, then add the v3.0 objectiveId logic.

**Step 1:** Get the improvement/ui version:
```bash
git show origin/improvement/ui:services/ui/src/routes/new-execution/+page.svelte
```

**Step 2:** Get the current v3.0 version and identify the objectiveId-specific additions. These are the v3.0 lines that MUST be preserved:

In the `<script>` block, the v3.0 additions are:
- `import { page } from '$app/stores';` (if not already present)
- `let objectiveId = $state($page.url.searchParams.get('objectiveId') || '');`
- `let urlMaxBots = $derived($page.url.searchParams.get('maxBots'));`
- `let urlBudgetCapDollars = $derived($page.url.searchParams.get('budgetCapDollars'));`
- An `$effect` block that pre-fills form fields from URL params when an objectiveId is present
- Any references to `urlMaxBots`, `urlBudgetCapDollars` in the effect or form initialization

In the `<form>` HTML:
- A hidden input: `<input type="hidden" name="objectiveId" value={objectiveId} />`

**Step 3:** Merge by:
1. Start with the improvement/ui version (Akasa styles)
2. Add the objectiveId-related `$state`, `$derived`, and `$effect` declarations to the script block
3. Add the `import { page } from '$app/stores'` if not already imported
4. Add the hidden `<input type="hidden" name="objectiveId" value={objectiveId} />` inside the `<form>` element
5. Ensure the `$effect` that reads URL params and pre-fills `maxBots`, `budgetCapDollars`, `runtimeLimitMinutes`, `toolAllowlist` from the objective is present

**Critical:** Do NOT lose any v3.0 objectiveId logic. Do NOT lose any improvement/ui Akasa styling. Both must coexist.

Also replace any remaining "Claw Army" brand text with "Akasa" in the page title or heading.
  </action>
  <verify>
1. Grep for objectiveId to confirm v3.0 logic is preserved:
```bash
grep 'objectiveId' services/ui/src/routes/new-execution/+page.svelte
```
Should return multiple matches (state var, hidden input, effect).

2. Grep for old tokens to confirm Akasa styling:
```bash
grep -E '\-\-(canvas|signal|surface|text-primary)' services/ui/src/routes/new-execution/+page.svelte
```
Should return 0 matches.

3. Grep for Akasa tokens:
```bash
grep 'var(--' services/ui/src/routes/new-execution/+page.svelte
```
Should return multiple matches.

4. Check brand text:
```bash
grep -i 'claw army' services/ui/src/routes/new-execution/+page.svelte
```
Should return 0 matches.
  </verify>
  <done>new-execution page has full Akasa dark theme styling AND retains all objectiveId URL param reading, hidden input forwarding, and form pre-fill logic from v3.0. No old tokens. No "Claw Army" text.</done>
</task>

<task type="auto">
  <name>Task 2: Replace execution monitor page with improvement/ui version</name>
  <files>services/ui/src/routes/executions/[id]/+page.svelte</files>
  <action>
Get the improvement/ui version and take it wholesale:
```bash
git show origin/improvement/ui:services/ui/src/routes/executions/[id]/+page.svelte
```

This file has NO v3.0-specific additions (the v3.0 changes to the execution monitor were bot card enhancements and VerdictConfirmPanel integration, which are in the script/template, not the CSS — and the improvement/ui version is a full re-skin of the same page structure).

**However**, verify the following v3.0 features are present in the improvement/ui version before replacing. If they are NOT present in improvement/ui (because they were added after the branch diverged), manually add them back:

1. **VerdictConfirmPanel import and usage** — the inline verdict confirmation panel was added in Phase 19. Check if improvement/ui has it. If not, add the import and the `{#if pendingVerdicts.length > 0}` block.
2. **Bot card enrichments** — `currentTaskDescription`, `toolCallCount`, `tokenBurnRate` display on bot cards (Phase 19 additions).
3. **SoulTierBadge import and usage** on bot cards (Phase 18 addition).

If the improvement/ui version is missing these (likely, since it predates Phases 18-19), the approach changes: instead of taking wholesale, take the improvement/ui CSS `<style>` block and HTML template structure, but keep the current v3.0 `<script>` block logic. Then remap old CSS tokens to Akasa tokens in the style block.

The safest approach:
1. Read both versions carefully
2. If improvement/ui has a simpler page (pre-Phase 18/19), keep the current v3.0 `<script>` block and template
3. Replace only the `<style>` block with the improvement/ui version
4. Update any inline style references or class-based token usage in the template to match Akasa tokens
5. Replace any remaining `var(--signal)`, `var(--canvas)`, etc. in the template with Akasa equivalents

Replace any "Claw Army" brand text with "Akasa".
  </action>
  <verify>
1. Grep for old tokens:
```bash
grep -E '\-\-(canvas|signal|surface|text-primary)' services/ui/src/routes/executions/\[id\]/+page.svelte
```
Should return 0 matches.

2. Verify VerdictConfirmPanel integration still present:
```bash
grep 'VerdictConfirmPanel' services/ui/src/routes/executions/\[id\]/+page.svelte
```
Should return matches (import + usage).

3. Verify SoulTierBadge integration:
```bash
grep 'SoulTierBadge' services/ui/src/routes/executions/\[id\]/+page.svelte
```
Should return matches.

4. Verify Akasa tokens used:
```bash
grep 'var(--violet' services/ui/src/routes/executions/\[id\]/+page.svelte
```
Should return matches.

5. Brand check:
```bash
grep -i 'claw army' services/ui/src/routes/executions/\[id\]/+page.svelte
```
Should return 0 matches.
  </verify>
  <done>Execution monitor page has full Akasa dark theme. All v3.0 features (VerdictConfirmPanel inline, SoulTierBadge on bot cards, enriched bot card data) are intact. No old tokens. No "Claw Army" text.</done>
</task>

</tasks>

<verification>
1. `grep 'objectiveId' services/ui/src/routes/new-execution/+page.svelte` returns 3+ matches
2. `grep 'VerdictConfirmPanel' services/ui/src/routes/executions/\[id\]/+page.svelte` returns 1+ match
3. `grep -rE '\-\-(canvas|signal|surface-[0-3]|text-primary)' services/ui/src/routes/new-execution/+page.svelte services/ui/src/routes/executions/\[id\]/+page.svelte` returns 0 matches
4. `grep -i 'claw army' services/ui/src/routes/new-execution/+page.svelte services/ui/src/routes/executions/\[id\]/+page.svelte` returns 0 matches
</verification>

<success_criteria>
- new-execution page is Akasa-themed AND retains objectiveId URL param + hidden input + pre-fill logic
- Execution monitor page is Akasa-themed AND retains VerdictConfirmPanel, SoulTierBadge, enriched bot cards
- Both pages use only Akasa CSS tokens — zero old tokens or hardcoded hex
- Zero "Claw Army" brand references
</success_criteria>

<output>
After completion, create `.planning/phases/23-akasa-ui-rebrand-design-system-rollout/23-02-SUMMARY.md`
</output>
