---
phase: 23-akasa-ui-rebrand-design-system-rollout
plan: 07
type: execute
wave: 4
depends_on: ["23-02", "23-03", "23-04", "23-05", "23-06"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All 13 route pages render with the Akasa dark theme — no page retains old light-mode or v1 styles"
    - "Zero hardcoded hex values remain across all .svelte files (app-wide grep confirms)"
    - "Zero old CSS tokens remain across all .svelte files (app-wide grep confirms)"
    - "Zero 'Claw Army' brand references remain across all .svelte files"
    - "Soul-concept elements use amber accents exclusively"
  artifacts: []
  key_links: []
---

<objective>
Final verification checkpoint — confirm the full Akasa rebrand is visually consistent across every page and that no old design artifacts remain anywhere in the UI codebase.

Purpose: This is the quality gate before marking Phase 23 complete. It catches any pages or components where old tokens, hardcoded hex, or brand copy slipped through the per-plan restyles.

Output: Verified app-wide compliance with the Akasa design system.
</objective>

<execution_context>
@/Users/tarikstafford/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarikstafford/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-akasa-ui-rebrand-design-system-rollout/23-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: App-wide compliance audit — grep for old tokens, hardcoded hex, and brand copy</name>
  <files></files>
  <action>
Run four app-wide audits across the entire `services/ui/src/` directory:

**Audit 1: Old CSS tokens**
```bash
grep -rn --include="*.svelte" -E '\-\-(canvas|signal[^-]|surface-[0-3]|text-primary|text-secondary|active-tint|alert[^-]|critical|border-soft|border-signal)' services/ui/src/
```
Expected: 0 matches. If any found, fix them in-place.

**Audit 2: Hardcoded hex values in style blocks**
```bash
grep -rn --include="*.svelte" -E '#[0-9a-fA-F]{3,8}' services/ui/src/
```
Expected: 0 or near-0 matches. Some exceptions are acceptable:
- `#nav` (element ID selector, not a color)
- Hex values inside SVG `<path>` or `<circle>` elements (decorative)
- References in comments
Any actual color hex values should be replaced with Akasa tokens.

**Audit 3: "Claw Army" brand references**
```bash
grep -rin 'claw army' services/ui/src/
```
Expected: 0 matches. All should say "Akasa".

**Audit 4: Old token definitions still in app.css**
```bash
grep -n -E '\-\-(canvas|signal|surface|text-primary|text-secondary|active|alert|critical):' services/ui/src/app.css
```
Expected: 0 matches (old tokens should not be defined).

If any audit finds violations, fix them directly. For each fix, apply the token mapping from the research:
- `--canvas` → `--bg`
- `--signal` → `--violet`
- `--surface-0/1` → `--bg-2`
- `--surface-2` → `--bg-3`
- `--surface-3` → `--bg-card`
- `--text-primary` → `--text`
- `--text-secondary` → `--text-muted`
- `--active` → `--teal`
- `--alert` → `--amber`
- `--critical` → `--error`
- `--border-soft` → `--border`
- `--border-signal` → `--border-mid`
  </action>
  <verify>
All four audit commands return 0 matches (excluding acceptable exceptions like #nav IDs).
  </verify>
  <done>App-wide audit confirms zero old tokens, zero hardcoded hex colors, zero "Claw Army" references across all .svelte files in services/ui/src/.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all 13 routes</name>
  <files></files>
  <action>
CHECKPOINT: Human visual verification required.

What was built: Complete Akasa UI rebrand across all 13 route pages and 3 shared components — dark violet theme, CSS custom properties, soul-concept amber accents, particle canvas, frosted nav, Clash Display + Inter + JetBrains Mono fonts.

Start the dev server:
```bash
cd services/ui && npm run dev
```

Present the following verification checklist to the user and wait for approval.
  </action>
  <verify>
Visit each route and confirm dark Akasa theme renders correctly:

1. `/` (Landing) — Hero with aurora blobs, glitch text, Akasa brand, particle canvas background
2. `/login` — Akasa-branded login form, dark theme, violet accents
3. `/new-execution` — Dark form with violet controls, "Launch" button
4. `/executions/[id]` — Dark execution monitor, live bot cards, SoulTierBadge visible
5. `/executions/[id]/bots/[botId]` — Dark bot detail, stats panels, soul info
6. `/executions/[id]/report` — Dark report, leaderboard with podium colors
7. `/objectives` — Dark objectives list, card layout, status badges
8. `/objectives/[id]` — Dark objective detail, run history table, stats, Launch button
9. `/verdicts` — Dark verdicts list, verdict type badges (teal/rose/amber)
10. `/verdicts/[verdictId]` — Dark verdict detail, judge summaries, confidence display
11. `/guide` — Dark guide, all sections legible, "Akasa" brand (not "Claw Army")
12. `/admin` — Dark admin panel, danger buttons use red (--error), not rose
13. `/billing` — Dark billing page, usage table, cost display

Key visual checks:
- Nav bar: Frosted glass on scroll, Akasa logo, Objectives/Guide/Verdicts/Billing links
- Fonts: Headings in Clash Display (tight, display weight), body in Inter, tags/mono in JetBrains Mono
- Soul badges: Artisan=amber with breathing glow, Understudy=teal, Novice=muted, Retired=rose
- No bright white panels or cards (everything dark)
- No broken CSS variables (text or backgrounds rendering as default/transparent)

Resume signal: Type "approved" if all 13 routes render correctly, or describe any visual issues to fix.
  </verify>
  <done>Human has confirmed all 13 routes render correctly with the Akasa dark theme. No visual regressions. Phase 23 rebrand is complete.</done>
</task>

</tasks>

<verification>
1. App-wide grep for old tokens returns 0 matches
2. App-wide grep for "Claw Army" returns 0 matches
3. App-wide grep for hardcoded hex returns 0 color-related matches
4. Human confirms all 13 routes render with Akasa dark theme
</verification>

<success_criteria>
- Zero old CSS tokens in the entire services/ui/src/ directory
- Zero hardcoded hex color values in any .svelte file
- Zero "Claw Army" brand references anywhere
- All 13 routes visually confirmed as Akasa dark theme by human review
- Phase 23 success criteria fully met
</success_criteria>

<output>
After completion, create `.planning/phases/23-akasa-ui-rebrand-design-system-rollout/23-07-SUMMARY.md`
</output>
