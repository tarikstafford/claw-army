---
phase: 28-ring-leader-agent-spawning
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/tasks.ts
  - packages/db/migrations/0012_add_ring_leader_task_id.sql
  - packages/db/migrations/meta/_journal.json
  - services/execution-service/src/services/agent-spawner.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "collectUpstreamOutputs returns completed task results when upstream tasks have ring_leader_task_id and status='completed'"
    - "Each agent spawned by the Ring Leader has a corresponding task row with ring_leader_task_id linking back to the mission brief taskId"
    - "Upstream intelligence section appears in agent session prompts when upstream tasks have completed with results"
  artifacts:
    - path: "packages/db/src/schema/tasks.ts"
      provides: "ring_leader_task_id varchar column on tasks table"
      contains: "ringLeaderTaskId"
    - path: "packages/db/migrations/0012_add_ring_leader_task_id.sql"
      provides: "SQL migration adding ring_leader_task_id column and index"
      contains: "ring_leader_task_id"
    - path: "services/execution-service/src/services/agent-spawner.ts"
      provides: "Functional collectUpstreamOutputs and task row creation during spawning"
      contains: "db.insert(tasks)"
  key_links:
    - from: "agent-spawner.ts"
      to: "tasks table"
      via: "db.insert(tasks) with ringLeaderTaskId during agent spawn"
      pattern: "ringLeaderTaskId.*taskId"
    - from: "collectUpstreamOutputs"
      to: "tasks table"
      via: "db.select from tasks WHERE ring_leader_task_id IN upstreamTaskIds AND status=completed"
      pattern: "eq.*ringLeaderTaskId|tasks.*ring_leader_task_id"
---

<objective>
Close the upstream intelligence gap (SPAWN-03 partial) by adding the `ring_leader_task_id` column to the tasks table, creating task rows during Ring Leader agent spawning, and implementing `collectUpstreamOutputs()` to query completed upstream task results.

Purpose: Without this fix, upstream intelligence signals are never injected into agent sessions — `collectUpstreamOutputs()` always returns `[]` because there is no column linking mission brief `taskId` strings to actual task rows. DAG ordering is respected (agents wait for upstream waves) but the intelligence from those upstream completions is lost.

Output: Fully functional upstream intelligence pipeline — when upstream tasks complete with results, downstream agents receive those results in their session prompts via the "## Upstream Intelligence" section already implemented in `agent-session-builder.ts`.
</objective>

<execution_context>
@/Users/tarikstafford/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarikstafford/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/db/src/schema/tasks.ts
@packages/db/migrations/0011_ring_leader_schema.sql
@packages/db/migrations/meta/_journal.json
@services/execution-service/src/services/agent-spawner.ts
@services/execution-service/src/services/agent-session-builder.ts
@services/execution-service/src/queue/openclaw-dispatcher.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ring_leader_task_id column to tasks schema and create SQL migration</name>
  <files>
    packages/db/src/schema/tasks.ts
    packages/db/migrations/0012_add_ring_leader_task_id.sql
    packages/db/migrations/meta/_journal.json
  </files>
  <action>
    1. In `packages/db/src/schema/tasks.ts`, add a `ringLeaderTaskId` column to the tasks table definition:
       - `ringLeaderTaskId: varchar('ring_leader_task_id', { length: 255 })` — nullable, no foreign key (mission brief taskIds are opaque strings like "task-1", "research-phase", etc., not UUIDs referencing another table)
       - Add an index: `index('tasks_ring_leader_task_id_idx').on(t.ringLeaderTaskId)` in the table's index array — this is needed for `collectUpstreamOutputs` which queries by this column

    2. Create `packages/db/migrations/0012_add_ring_leader_task_id.sql` with idempotent SQL:
       ```sql
       ALTER TABLE "tasks" ADD COLUMN IF NOT EXISTS "ring_leader_task_id" varchar(255);--> statement-breakpoint
       CREATE INDEX IF NOT EXISTS "tasks_ring_leader_task_id_idx" ON "tasks" USING btree ("ring_leader_task_id");
       ```
       Follow the exact pattern of prior migrations (e.g., 0011). Use `--> statement-breakpoint` as separator between statements.

    3. Update `packages/db/migrations/meta/_journal.json`: add a new entry at idx 12 with tag `0012_add_ring_leader_task_id`, version `"7"`, breakpoints `true`. Use a timestamp like `1772700000000` (after 0011's `1772600000000`).

    NOTE: Import `varchar` from `drizzle-orm/pg-core` — it is not currently imported in tasks.ts. Add it to the existing import destructuring on line 1.
  </action>
  <verify>
    - `grep 'ringLeaderTaskId' packages/db/src/schema/tasks.ts` finds the column definition
    - `grep 'ring_leader_task_id' packages/db/migrations/0012_add_ring_leader_task_id.sql` finds the migration SQL
    - `grep '0012' packages/db/migrations/meta/_journal.json` finds the journal entry
    - `cd packages/db && npx tsc --noEmit` compiles without errors (or use the workspace typecheck if available)
  </verify>
  <done>
    - tasks schema has `ringLeaderTaskId` varchar(255) nullable column
    - SQL migration file exists and is idempotent (IF NOT EXISTS)
    - Journal entry added so drizzle-kit recognizes the migration
    - TypeScript compiles cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Create task rows during agent spawning and implement collectUpstreamOutputs</name>
  <files>
    services/execution-service/src/services/agent-spawner.ts
  </files>
  <action>
    Two changes to `agent-spawner.ts`:

    **A. Implement `collectUpstreamOutputs()` (replace the stub at lines 149-153):**

    Replace the entire function body. The new implementation should:
    1. Accept `upstreamTaskIds: string[]` (mission brief taskId strings)
    2. Query `tasks` table: `SELECT ring_leader_task_id, result FROM tasks WHERE ring_leader_task_id IN ($upstreamTaskIds) AND status = 'completed' AND result IS NOT NULL`
    3. Use Drizzle: `db.select({ ringLeaderTaskId: tasks.ringLeaderTaskId, result: tasks.result }).from(tasks).where(and(inArray(tasks.ringLeaderTaskId, upstreamTaskIds), eq(tasks.status, 'completed')))`
    4. Filter out rows where `result` is null or empty
    5. Map results to `Array<{ taskId: string; summary: string }>` where `taskId` = `ringLeaderTaskId` and `summary` = `result`
    6. If `upstreamTaskIds` is empty, short-circuit and return `[]`
    7. Remove the TODO comment and update the JSDoc to reflect the real implementation
    8. Wrap in try/catch — log warning on failure and return `[]` (non-fatal: upstream intelligence is a nice-to-have, not a blocker for agent execution)

    **B. Create task rows when spawning agents (inside the wave processing loop):**

    After the agent is spawned (after the `spawnBot` call on line 301) and before registering in the session registry (line 304), insert a task row into the `tasks` table:

    ```typescript
    await db.insert(tasks).values({
      executionId,
      description: taskNode.description,
      status: 'claimed',
      claimedByBotId: botId,
      ringLeaderTaskId: taskId,
      attemptCount: 1,
    });
    ```

    This creates the linkage between the mission brief `taskId` (via `ringLeaderTaskId`) and the actual task row. Status starts as `claimed` because the bot is already assigned. The `openclaw-dispatcher.ts` will later transition these to `completed` or `failed` and populate the `result` field.

    IMPORTANT: The `tasks` import is already present on line 1 (`import { db, botSouls, tasks, ringLeaderRuns } from '@claw/db'`). The `inArray` import needs to be added — line 2 currently has `import { eq, and, inArray } from 'drizzle-orm'` which already includes it. Verify this before adding duplicates.

    Also verify that `tasks.ringLeaderTaskId` is available after Task 1 modifies the schema — the `@claw/db` re-export includes tasks via `packages/db/src/schema/index.ts`.
  </action>
  <verify>
    - `grep -c 'collectUpstreamOutputs' services/execution-service/src/services/agent-spawner.ts` shows the function exists (not just a stub)
    - `grep 'db.insert(tasks)' services/execution-service/src/services/agent-spawner.ts` finds the task row creation
    - `grep 'ringLeaderTaskId' services/execution-service/src/services/agent-spawner.ts` finds usage of the new column
    - `grep 'return \[\]' services/execution-service/src/services/agent-spawner.ts` should NOT match inside collectUpstreamOutputs (the unconditional empty return is gone; the early-return for empty input and the catch fallback are OK)
    - TypeScript compiles cleanly: `cd services/execution-service && npx tsc --noEmit`
  </verify>
  <done>
    - `collectUpstreamOutputs()` queries the tasks table by `ring_leader_task_id` and returns completed task results
    - Each spawned agent creates a task row with `ringLeaderTaskId` linking to the mission brief `taskId`
    - The upstream intelligence pipeline is end-to-end functional: upstream task completes with result -> downstream agent's `collectUpstreamOutputs` finds it -> `buildAgentSessionPrompt` renders the "## Upstream Intelligence" section
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
End-to-end verification of the upstream intelligence pipeline:

1. **Schema:** `packages/db/src/schema/tasks.ts` has `ringLeaderTaskId` column, migration file exists
2. **Task creation:** `agent-spawner.ts` inserts task rows with `ringLeaderTaskId` during agent spawning
3. **Output collection:** `collectUpstreamOutputs()` queries `tasks WHERE ring_leader_task_id IN (...) AND status = 'completed'` and returns `{ taskId, summary }[]`
4. **Integration:** The existing call chain `agent-spawner -> collectUpstreamOutputs -> buildAgentSessionPrompt` now produces the "## Upstream Intelligence" section when upstream tasks have completed with results
5. **TypeScript:** Both `packages/db` and `services/execution-service` compile without errors
</verification>

<success_criteria>
- SPAWN-03 gap is fully closed: upstream intelligence signals are collected from completed task rows and injected into downstream agent sessions
- The `tasks.ring_leader_task_id` column exists in both the Drizzle schema and a SQL migration
- `collectUpstreamOutputs()` is no longer a stub — it queries the database
- Task rows are created during Ring Leader agent spawning with the `ringLeaderTaskId` linkage
- All TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/28-ring-leader-agent-spawning/28-04-SUMMARY.md`
</output>
