---
phase: 32-dashboard-and-reporting
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - services/ui/src/routes/executions/[id]/+page.svelte
autonomous: true

must_haves:
  truths:
    - "Pre-flight view shows population manifest per task with soul assignments, agent classes, sources, and selection rationale"
    - "Live execution view shows Ring Leader run state: budget consumed, task states, drift score, and active anomalies"
  artifacts:
    - path: "services/ui/src/routes/executions/[id]/+page.svelte"
      provides: "Population manifest panel and Ring Leader state panel"
      contains: "getRingLeaderManifest"
  key_links:
    - from: "services/ui/src/routes/executions/[id]/+page.svelte"
      to: "/ring-leader/runs/by-execution/:executionId"
      via: "getRingLeaderManifest and getRingLeaderState API calls"
      pattern: "getRingLeaderManifest|getRingLeaderState"
---

<objective>
Add pre-flight population manifest panel and live Ring Leader state panel to the execution detail page.

Purpose: Satisfies DASH-01 (population manifest with soul assignments, classes, sources, rationale) and DASH-02 (live run state with budget, task states, drift, anomalies).

Output: Two new sections in the execution detail page.
</objective>

<execution_context>
@/Users/tarikstafford/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarikstafford/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-dashboard-and-reporting/32-01-SUMMARY.md
@services/ui/src/routes/executions/[id]/+page.svelte
@services/ui/src/lib/api.ts
@services/ui/src/lib/types.ts
@services/ui/src/app.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add population manifest panel and Ring Leader state panel</name>
  <files>services/ui/src/routes/executions/[id]/+page.svelte</files>
  <action>
Add two new sections to the execution detail page, placed between the metrics panel and the report link.

**1. Population Manifest Panel (DASH-01)**

- Import `getRingLeaderManifest` from `$lib/api` and `RingLeaderManifestResponse`, `PopulationManifest`, `SoulSelectionEntry` from `$lib/types`.
- Add state: `let manifest = $state<RingLeaderManifestResponse | null>(null);`
- Add effect: Fetch `getRingLeaderManifest(executionId)` on mount (same pattern as execution load). Silently catch errors (non-Ring-Leader executions will 404).
- Render a section with heading "Population Manifest" (same `h3` style as existing "Bots" and "Activity Feed" sections — mono, 10px, uppercase, faint).
- If `manifest?.manifests.length > 0`, render one card per task manifest:
  - Card header: task description + pioneer flag badge (amber, same style as report page pioneer badge).
  - Inside each card: a compact table/list of assigned souls:
    - Columns: Soul ID (first 8 chars, mono), Agent Class (use `SoulTierBadge` component), Source (pill: library=teal, generated=violet, mutated=amber), Selection Rationale (truncated 120 chars), Differentiation Score (mono, 2 decimal).
  - If `varianceIntent` is not null, show it as a small italic note below the soul list.
- If manifest is null or empty, show: "No population manifest available." in the same style as the empty feed message.

**2. Ring Leader State Panel (DASH-02)**

- Import `getRingLeaderState` from `$lib/api` and `RingLeaderStateResponse` from `$lib/types`.
- Add state: `let ringLeaderState = $state<RingLeaderStateResponse | null>(null);`
- Add polling effect (same pattern as metrics polling):
  - Fetch `getRingLeaderState(executionId)` immediately and every 5 seconds while non-terminal.
  - Stop polling when execution is completed/failed/stopped.
- Render a section with heading "Ring Leader" between the manifest panel and the report link.
- Show only when `ringLeaderState?.runState` is not null (skip for non-Ring-Leader executions and pre-coordination states).
- Render 4 metric cards in a grid (same `metrics-grid` pattern as existing Live Metrics):
  - **Budget Consumed**: `$X.XX` (runState.budgetConsumedCents / 100, formatted).
  - **Drift Score**: `0.XX` (runState.objectiveDriftScore, 2 decimals). Color: teal if < 0.20, amber if 0.20-0.35, error if > 0.35 (matches DRIFT_REANCHORING_THRESHOLD).
  - **Elapsed**: `Xm Xs` (runState.elapsedTimeSeconds formatted).
  - **Anomalies**: count (runState.anomalies.length). Color: teal if 0, error if > 0.
- Below the grid, render a compact task states summary:
  - For each task in `runState.taskStates` (keyed by taskId):
    - Show: taskId (truncated), status pill (same style as bot status pills — queued=faint, active=teal, complete=teal, failed=error), agent count (`activeAgents.length` active / `completedAgents.length` done / `failedAgents.length` fail).
  - Use a simple list or compact grid — not a full table.
- If anomalies exist, render them as a collapsible list (or just show up to 5 with truncation).

**Styling:**
Follow existing Akasa design system exactly:
- Use CSS variables from app.css (--bg-card, --border, --text-faint, --font-mono, etc.)
- Card pattern: border-radius 12-14px, border 1px solid var(--border), bg var(--bg-card), padding 16-20px.
- Section headers: font-mono 10px uppercase 0.22em spacing, var(--text-faint), with trailing gradient line.
- All new styles scoped in `<style>` block (Svelte component scoping).

**Placement in template:**
```
Status banner
Metrics panel
[NEW] Population Manifest panel
[NEW] Ring Leader panel
Report link (if completed)
Bots section
Activity feed
```
  </action>
  <verify>
Run `cd /Users/tarikstafford/Desktop/Projects/claw-army && npx tsc --noEmit -p services/ui/tsconfig.json` — no type errors. Visually confirm: `grep -c 'getRingLeaderManifest\|getRingLeaderState' services/ui/src/routes/executions/\[id\]/+page.svelte` shows at least 2 matches (import + usage).
  </verify>
  <done>Execution detail page shows population manifest with soul assignments, classes, sources, and rationale per task (DASH-01), and live Ring Leader state with budget consumed, task states, drift score, and anomalies (DASH-02).</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p services/ui/tsconfig.json` passes
- Execution detail page imports and calls getRingLeaderManifest and getRingLeaderState
- Population manifest renders per-task soul assignments with all required fields
- Ring Leader state shows budget, drift, elapsed, anomalies, and per-task status
- Styling matches existing Akasa design system
</verification>

<success_criteria>
DASH-01 and DASH-02 satisfied: Pre-flight population manifest visible with full soul detail, live Ring Leader state panel shows all run metrics and task states during execution.
</success_criteria>

<output>
After completion, create `.planning/phases/32-dashboard-and-reporting/32-02-SUMMARY.md`
</output>
