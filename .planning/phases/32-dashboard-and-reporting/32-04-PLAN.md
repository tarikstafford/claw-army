---
phase: 32-dashboard-and-reporting
plan: 04
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - services/ui/src/routes/executions/[id]/report/+page.svelte
autonomous: true

must_haves:
  truths:
    - "Post-run report includes Ring Leader synthesis with soul selection retrospective and coordination self-assessment"
    - "Post-run report shows Ring Leader fitness scores with coordination and soul selection dimension breakdowns"
  artifacts:
    - path: "services/ui/src/routes/executions/[id]/report/+page.svelte"
      provides: "Ring Leader synthesis panel and fitness score breakdown"
      contains: "getRingLeaderSynthesis"
  key_links:
    - from: "services/ui/src/routes/executions/[id]/report/+page.svelte"
      to: "/ring-leader/runs/by-execution/:executionId/synthesis"
      via: "getRingLeaderSynthesis API call"
      pattern: "getRingLeaderSynthesis"
---

<objective>
Add Ring Leader synthesis and fitness score panels to the post-run report page.

Purpose: Satisfies DASH-04 (synthesis with soul selection retrospective and coordination self-assessment) and DASH-05 (fitness scores with coordination and soul selection dimension breakdowns).

Output: Two new sections on the report page.
</objective>

<execution_context>
@/Users/tarikstafford/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarikstafford/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-dashboard-and-reporting/32-01-SUMMARY.md
@services/ui/src/routes/executions/[id]/report/+page.svelte
@services/ui/src/lib/api.ts
@services/ui/src/lib/types.ts
@packages/shared-types/src/ring-leader.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Ring Leader synthesis panel to report page</name>
  <files>services/ui/src/routes/executions/[id]/report/+page.svelte</files>
  <action>
Add Ring Leader synthesis and fitness score sections to the report page. These render after the existing Soul Tier Distribution section and before the Bot Leaderboard.

**Data fetching:**
- Import `getRingLeaderSynthesis` from `$lib/api` and `RingLeaderSynthesisResponse` from `$lib/types`.
- Add state: `let synthesisData = $state<RingLeaderSynthesisResponse | null>(null);`
- Fetch in the existing `$effect` alongside report and leaderboard: add `getRingLeaderSynthesis(id).catch(() => null)` to the `Promise.all`. Store result in `synthesisData`. The `.catch(() => null)` handles non-Ring-Leader executions gracefully.

**1. Ring Leader Synthesis Section (DASH-04):**
Only render when `synthesisData?.synthesis` is not null.

Section heading: "Ring Leader Synthesis" (same `.section h2` style as other sections).

Layout:
- **Objective Achievement**: Show `objectiveAchieved` as a teal/error badge ("ACHIEVED" / "NOT ACHIEVED"), followed by `achievementRationale` as paragraph text (var(--text-muted), 14px, font-weight 300, line-height 1.65).
- **Run Statistics**: A 4-column metric grid (same style as Execution Summary stats):
  - Intelligence Routing Events: `synthesis.intelligenceRoutingEvents`
  - Reallocation Events: `synthesis.reallocationEvents`
  - Reanchoring Events: `synthesis.reanchoringEvents`
  - Budget Variance: `$X.XX` (synthesis.budgetVarianceCents / 100). Color: teal if negative (under budget), error if positive (over budget).
- **Soul Selection Retrospective** (DASH-04 specific): Render `synthesis.soulSelectionRetrospective` as a styled text block. Use a card (bg-card, border, border-radius 12px, padding 20px) with a small label "Soul Selection Retrospective" in the section-header style (mono 9px uppercase faint). The text itself in body font, 14px, 300 weight, muted color, line-height 1.65. Preserve line breaks with `white-space: pre-line`.
- **Coordination Self-Assessment** (DASH-04 specific): Same card pattern as retrospective, with label "Coordination Self-Assessment" and `synthesis.ringLeaderSelfAssessment` content.
- **Recommended Library Writes**: If `synthesis.recommendedLibraryWrites.length > 0`, show a small list of soul IDs (first 8 chars each, mono, pills with teal border). Otherwise omit.
- **Pioneer Events**: If `synthesis.pioneerEvents.length > 0`, show task IDs as amber pills. Otherwise omit.

**2. Fitness Score Breakdown Section (DASH-05):**
Only render when `synthesisData?.fitness` is not null.

Section heading: "Ring Leader Fitness" (same style).

Layout:
- **Composite Score**: Large display (same as existing stat-value style, 1.5rem mono), e.g., "0.82". Color based on value: >= 0.85 teal, >= 0.68 amber, < 0.68 error (matches promotion thresholds).
- **Coordination Score Breakdown** (60% weight): A card with 4 dimension bars:
  - For each dimension (Collective Outcome 40%, Drift Prevention 25%, Reallocation Effectiveness 20%, Budget Management 15%):
    - Label (mono 10px uppercase faint): dimension name + weight in parens.
    - Value (mono 14px): formatted to 2 decimals.
    - Visual bar: a thin horizontal bar (height 4px, border-radius 2px) with fill width proportional to value (0-1). Fill color: teal if >= 0.7, amber if >= 0.5, error if < 0.5. Background: var(--border).
  - Show weighted subtotal at bottom: `(c.collectiveOutcome * 0.40 + c.driftPrevention * 0.25 + c.reallocationEffectiveness * 0.20 + c.budgetManagement * 0.15).toFixed(2)`.
- **Soul Selection Score Breakdown** (40% weight): Same card pattern, 5 dimensions:
  - Library Search Quality, Differentiation Effectiveness, Mutation Decision Quality, Pioneer Handling, Selection Retrospective Quality.
  - Each dimension: label, value, bar (same style as coordination).
  - Show weighted subtotal (equal weight per dimension, 20% each).

**Styling:**
All new styles follow existing Akasa design system (see existing report page patterns):
- Cards: `.section` margin-bottom 2.5rem, h2 as section headers.
- Stats grid: `.stats-grid` pattern for synthesis run stats.
- Text blocks: bg-card, border, rounded, padded, with small label above.
- Score bars: new CSS for the dimension bar visualization:
  ```css
  .score-bar { height: 4px; border-radius: 2px; background: var(--border); margin-top: 4px; }
  .score-bar-fill { height: 100%; border-radius: 2px; transition: width 0.3s; }
  .score-bar-fill.score-high { background: var(--teal); }
  .score-bar-fill.score-mid { background: var(--amber); }
  .score-bar-fill.score-low { background: var(--error); }
  ```

**Placement in template:**
```
Execution Summary
Soul Tier Distribution
[NEW] Ring Leader Synthesis
[NEW] Ring Leader Fitness
Bot Leaderboard
```
  </action>
  <verify>
Run `cd /Users/tarikstafford/Desktop/Projects/claw-army && npx tsc --noEmit -p services/ui/tsconfig.json` — no type errors. Verify: `grep -c 'getRingLeaderSynthesis\|soulSelectionRetrospective\|coordinationScore\|compositeScore' services/ui/src/routes/executions/\[id\]/report/+page.svelte` shows at least 4 matches.
  </verify>
  <done>Report page shows Ring Leader synthesis (objective achievement, event counts, soul selection retrospective, coordination self-assessment) and fitness scores (composite score, 4-dimension coordination breakdown, 5-dimension soul selection breakdown with visual bars).</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p services/ui/tsconfig.json` passes
- Report page fetches and renders synthesis data
- Soul selection retrospective and coordination self-assessment visible as text blocks
- Fitness score shows composite + 9 dimension breakdowns with visual bars
- Graceful handling for non-Ring-Leader executions (sections simply don't render)
</verification>

<success_criteria>
DASH-04 and DASH-05 satisfied: Post-run report includes Ring Leader synthesis with retrospective and self-assessment, and fitness scores with full coordination (4) and soul selection (5) dimension breakdowns.
</success_criteria>

<output>
After completion, create `.planning/phases/32-dashboard-and-reporting/32-04-SUMMARY.md`
</output>
