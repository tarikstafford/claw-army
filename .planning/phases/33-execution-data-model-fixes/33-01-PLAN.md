---
phase: 33-execution-data-model-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/executions.ts
  - packages/db/migrations/0013_add_llm_provider_allowed_domains.sql
  - packages/db/migrations/meta/_journal.json
  - services/execution-service/src/services/execution.service.ts
  - services/execution-service/src/routes/executions.ts
autonomous: true

must_haves:
  truths:
    - "POST /executions accepts llmProvider (string) and stores it on the executions row"
    - "POST /executions accepts allowedDomains (string array) and stores it on the executions row"
    - "Existing executions without these fields remain unaffected (nullable columns)"
    - "GET /executions/:id returns llmProvider and allowedDomains fields"
  artifacts:
    - path: "packages/db/src/schema/executions.ts"
      provides: "llmProvider and allowedDomains columns"
      contains: "llmProvider"
    - path: "packages/db/migrations/0013_add_llm_provider_allowed_domains.sql"
      provides: "SQL migration adding both columns"
      contains: "ALTER TABLE"
    - path: "services/execution-service/src/services/execution.service.ts"
      provides: "CreateExecutionInput with llmProvider and allowedDomains"
      contains: "llmProvider"
    - path: "services/execution-service/src/routes/executions.ts"
      provides: "Route handler storing both fields"
      contains: "allowedDomains"
  key_links:
    - from: "services/execution-service/src/routes/executions.ts"
      to: "services/execution-service/src/services/execution.service.ts"
      via: "createExecution() call passes llmProvider and allowedDomains"
      pattern: "createExecution.*llmProvider"
    - from: "services/execution-service/src/services/execution.service.ts"
      to: "packages/db/src/schema/executions.ts"
      via: "db.insert(executions).values() includes new columns"
      pattern: "llmProvider.*input\\.llmProvider"
---

<objective>
Add `llmProvider` (varchar, nullable) and `allowedDomains` (text array, nullable) columns to the executions table, wire them through the POST /executions handler and createExecution service, and expose them in the GET response.

Purpose: The backend needs `llmProvider` to route LLM calls to the correct provider per execution, and `allowedDomains` to enforce per-execution domain filtering in the Tool Gateway (wired in plan 33-02). Both columns are nullable for backward compatibility.

Output: Migration SQL, updated Drizzle schema, updated service and route handler.
</objective>

<execution_context>
@/Users/tarikstafford/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarikstafford/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/db/src/schema/executions.ts
@packages/db/migrations/0012_add_ring_leader_task_id.sql
@packages/db/migrations/meta/_journal.json
@services/execution-service/src/services/execution.service.ts
@services/execution-service/src/routes/executions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add llmProvider and allowedDomains to Drizzle schema and create migration</name>
  <files>
    packages/db/src/schema/executions.ts
    packages/db/migrations/0013_add_llm_provider_allowed_domains.sql
    packages/db/migrations/meta/_journal.json
  </files>
  <action>
1. In `packages/db/src/schema/executions.ts`, add two new columns to the `executions` pgTable definition:
   - `llmProvider: varchar('llm_provider', { length: 50 })` — nullable, no default. Valid values will be enforced at app level (anthropic | openai), not DB level, to avoid migration churn when adding providers.
   - `allowedDomains: text('allowed_domains').array()` — nullable text array. When null, Tool Gateway falls back to the global PROXY_DOMAIN_ALLOWLIST env var. When set, per-execution filtering applies.

   Place both columns after `allowedTools` and before `taskCategory` for logical grouping.

2. Create migration file `packages/db/migrations/0013_add_llm_provider_allowed_domains.sql`:
   ```sql
   ALTER TABLE "executions" ADD COLUMN IF NOT EXISTS "llm_provider" varchar(50);--> statement-breakpoint
   ALTER TABLE "executions" ADD COLUMN IF NOT EXISTS "allowed_domains" text[];
   ```
   Use `IF NOT EXISTS` for idempotency (consistent with migrations 0008-0010 pattern).

3. Add journal entry to `packages/db/migrations/meta/_journal.json`:
   - idx: 13
   - version: "7"
   - when: use current epoch milliseconds
   - tag: "0013_add_llm_provider_allowed_domains"
   - breakpoints: true

4. The `Execution` and `NewExecution` inferred types from `$inferSelect` / `$inferInsert` will automatically include the new nullable fields — no manual type changes needed.
  </action>
  <verify>
    - Run `npx tsc --noEmit` from `packages/db` to confirm no type errors.
    - Verify the migration SQL file exists and contains both ALTER TABLE statements.
    - Verify _journal.json has idx 13 entry.
  </verify>
  <done>
    - `executions` Drizzle schema has `llmProvider` (nullable varchar) and `allowedDomains` (nullable text array) columns.
    - Migration 0013 SQL file is idempotent and ready to apply.
    - Journal entry registered.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire llmProvider and allowedDomains through createExecution and POST handler</name>
  <files>
    services/execution-service/src/services/execution.service.ts
    services/execution-service/src/routes/executions.ts
  </files>
  <action>
1. In `services/execution-service/src/services/execution.service.ts`:
   - Add `llmProvider?: string` and `allowedDomains?: string[]` to the `CreateExecutionInput` interface.
   - In the `createExecution` function, include both fields in the `db.insert(executions).values({...})` call:
     ```typescript
     llmProvider: input.llmProvider ?? null,
     allowedDomains: input.allowedDomains ?? null,
     ```

2. In `services/execution-service/src/routes/executions.ts`:
   - The TypeBox schema already accepts `llmProvider` and `allowedDomains` in the POST body (lines 32-33). No change needed there.
   - In the POST handler destructuring (around line 68-75), add `llmProvider` and `allowedDomains` to the destructured body fields:
     ```typescript
     const {
       objective,
       maxBots,
       budgetCapCents,
       runtimeLimitSeconds,
       allowedTools = [],
       llmProvider,
       allowedDomains,
       objectiveId,
     } = request.body;
     ```
   - Pass both fields to `createExecution()` call (around line 112-118):
     ```typescript
     result = await createExecution({
       objective,
       maxBots,
       budgetCapCents: budgetCapCents ?? 0,
       runtimeLimitSeconds: runtimeLimitSeconds ?? 3600,
       allowedTools,
       llmProvider,
       allowedDomains,
       objectiveId,
     });
     ```
   - In the GET /:id response schema (around line 191-208), add:
     ```typescript
     llmProvider: Type.Union([Type.String(), Type.Null()]),
     allowedDomains: Type.Union([Type.Array(Type.String()), Type.Null()]),
     ```
     Place after `allowedTools` for consistency.
  </action>
  <verify>
    - Run `npx tsc --noEmit` from `services/execution-service` to confirm no type errors.
    - Verify the POST handler destructures llmProvider and allowedDomains from body.
    - Verify createExecution passes both fields to db.insert.
    - Verify GET /:id response schema includes both new fields.
  </verify>
  <done>
    - POST /executions stores llmProvider and allowedDomains on the executions row.
    - GET /executions/:id returns both fields (nullable).
    - Existing callers that omit these fields are unaffected (both default to null).
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors across packages/db and services/execution-service.
2. Migration 0013 SQL contains both ALTER TABLE ADD COLUMN statements with IF NOT EXISTS.
3. POST /executions TypeBox schema accepts llmProvider (string, optional) and allowedDomains (string array, optional).
4. createExecution() inserts both fields into the executions table.
5. GET /executions/:id response includes llmProvider and allowedDomains.
6. Omitting both fields from a POST request still succeeds (backward compatible).
</verification>

<success_criteria>
- `llmProvider` and `allowedDomains` columns exist in Drizzle schema and migration.
- POST /executions handler stores both fields.
- GET /executions/:id returns both fields.
- All TypeScript compiles cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/33-execution-data-model-fixes/33-01-SUMMARY.md`
</output>
