---
phase: 35-execution-form-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/ui/src/routes/new-execution/+page.svelte
  - services/ui/src/routes/new-execution/+page.server.ts
autonomous: true

must_haves:
  truths:
    - "New execution form shows a campaign type selector with ad hoc and campaign options"
    - "New execution form shows a multi-select tool allowlist with toggle buttons"
    - "New execution form shows a runtime limit input in minutes"
    - "All three fields are optional with sensible defaults (ad_hoc, empty array, 60 minutes)"
    - "Server action sends allowedTools, runtimeLimitSeconds, and campaignType in the POST body"
  artifacts:
    - path: "services/ui/src/routes/new-execution/+page.svelte"
      provides: "Campaign type selector, tool allowlist multi-select, runtime limit input"
      contains: "campaignType"
    - path: "services/ui/src/routes/new-execution/+page.server.ts"
      provides: "Form data parsing for all three new fields"
      contains: "runtimeLimitSeconds"
  key_links:
    - from: "services/ui/src/routes/new-execution/+page.svelte"
      to: "services/ui/src/routes/new-execution/+page.server.ts"
      via: "FormData with hidden inputs for campaignType and allowedTools, named input for runtimeLimitMinutes"
      pattern: "formData\\.get(All)?\\('(campaignType|allowedTools|runtimeLimitMinutes)'\\)"
    - from: "services/ui/src/routes/new-execution/+page.server.ts"
      to: "POST /executions"
      via: "JSON body with allowedTools, runtimeLimitSeconds, campaignType"
      pattern: "allowedTools.*runtimeLimitSeconds.*campaignType"
---

<objective>
Add campaign type selector, tool allowlist multi-select, and runtime limit input to the new execution form. Wire all three through the server action into the POST /executions request body.

Purpose: Users can configure campaign type, tool access, and time limits when creating executions (FORM-01, FORM-02, FORM-03).
Output: Updated form with three new panels; server action sends all three fields to the backend.
</objective>

<execution_context>
@/Users/tarikstafford/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarikstafford/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-execution-form-enhancements/35-RESEARCH.md
@services/ui/src/routes/new-execution/+page.svelte
@services/ui/src/routes/new-execution/+page.server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add campaign type, tool allowlist, and runtime limit fields to the form</name>
  <files>services/ui/src/routes/new-execution/+page.svelte</files>
  <action>
Add three new form sections to `+page.svelte`, inserting them between the LLM Provider/Egress Perimeter row (panels 04-05) and the Army Analysis panel (06). Renumber Army Analysis to 09 and the new panels to 06, 07, 08.

**Campaign Type Selector (panel 06):**
- Add `$state` variable: `let campaignType = $state<'ad_hoc' | 'campaign'>('ad_hoc');`
- Create a panel with two toggle buttons using the EXACT same `tool-toggle` class and SVG indicator pattern as the existing `llmProvider` selector (panels 04). This is a single-select (radio-style) — clicking one deselects the other.
- Options: `{ id: 'ad_hoc', label: 'Ad Hoc', description: 'One-off execution without a named objective' }` and `{ id: 'campaign', label: 'Campaign', description: 'Part of an ongoing objective with iterating DNA' }`.
- Add hidden input: `<input type="hidden" name="campaignType" value={campaignType} />`
- Define a `CAMPAIGN_TYPES` constant array (same pattern as `LLM_PROVIDERS`) and render with `{#each}`.

**Tool Allowlist Multi-Select (panel 07):**
- Add `$state` variable: `let selectedTools = $state<Set<string>>(new Set());`
- Define constant: `const AVAILABLE_TOOLS: { id: string; label: string; description: string }[] = [ { id: 'bash', label: 'Bash', description: 'Execute shell commands' }, { id: 'file_read', label: 'File Read', description: 'Read files from the filesystem' }, { id: 'file_write', label: 'File Write', description: 'Write files to the filesystem' }, { id: 'web_search', label: 'Web Search', description: 'Search the web' }, { id: 'web_fetch', label: 'Web Fetch', description: 'Fetch content from URLs' } ];`
- Create a panel with toggle buttons using the same `tool-toggle` CSS class. This is multi-select — clicking toggles membership in the Set. Use `onclick={() => { if (selectedTools.has(tool.id)) { selectedTools.delete(tool.id); selectedTools = new Set(selectedTools); } else { selectedTools = new Set([...selectedTools, tool.id]); } }}`.
- Add panel hint: "Select which tools bots can use. Leave all unselected to allow all tools."
- Render hidden inputs for form submission: `{#each [...selectedTools] as tool}<input type="hidden" name="allowedTools" value={tool} />{/each}`
- Show "ENABLED" badge (instead of "SELECTED") on active toggles to differentiate from single-select patterns.

**Runtime Limit Input (panel 08):**
- Add `$state` variable: `let runtimeLimitMinutes = $state(60);`
- Create a panel with a numeric input following the same layout pattern as the Budget Cap panel (03). Use a similar `budget-input-wrap` style wrapper.
- Display unit label "min" next to the input (like "$" prefix on budget).
- Add hint text: "Maximum time before execution is automatically stopped."
- Use `<input type="number" name="runtimeLimitMinutes" bind:value={runtimeLimitMinutes} min="1" step="1" />`
- Add a small `.range-labels` style hint showing "1 min" and "no upper limit" (or similar).

**Styling:** No new CSS classes needed — reuse existing `.panel`, `.tool-toggle`, `.tool-toggle.active`, `.tool-indicator`, `.tool-info`, `.tool-label`, `.tool-desc`, `.tool-status`, `.budget-input-wrap`, `.panel-hint`, and `.tools-grid` classes. The runtime limit panel can reuse the budget-control layout pattern. Place campaign type and tool allowlist in a `.row-panels` grid (side by side), and runtime limit as its own full-width panel below.
  </action>
  <verify>
Open `services/ui/src/routes/new-execution/+page.svelte` and confirm:
1. campaignType state variable with 'ad_hoc' default
2. selectedTools state variable (Set)
3. runtimeLimitMinutes state variable with 60 default
4. Hidden inputs for campaignType and allowedTools
5. Named input for runtimeLimitMinutes
6. All three panels render with tool-toggle pattern
  </verify>
  <done>Form displays campaign type selector (ad hoc / campaign), tool allowlist multi-select with 5 tools, and runtime limit input defaulting to 60 minutes. All use existing CSS patterns. Hidden inputs submit values.</done>
</task>

<task type="auto">
  <name>Task 2: Wire three new fields through the server action</name>
  <files>services/ui/src/routes/new-execution/+page.server.ts</files>
  <action>
Update the `default` action in `+page.server.ts` to extract and forward the three new fields.

After the existing `objectiveId` extraction (line 33), add:

```typescript
const allowedTools = formData.getAll('allowedTools') as string[];
const runtimeLimitMinutes = Number(formData.get('runtimeLimitMinutes') ?? 60);
const runtimeLimitSeconds = runtimeLimitMinutes * 60;
const campaignType = (formData.get('campaignType') as string | null) ?? 'ad_hoc';
```

**IMPORTANT:** Use `formData.getAll('allowedTools')` (not `formData.get()`) because the form sends multiple hidden inputs with the same name.

Update the `JSON.stringify` body in the fetch call to include all three new fields:

```typescript
body: JSON.stringify({
  objective,
  maxBots,
  budgetCapCents,
  llmProvider,
  allowedDomains,
  allowedTools,
  runtimeLimitSeconds,
  campaignType,
  ...(objectiveId ? { objectiveId } : {}),
}),
```

The conversion from minutes to seconds happens here in the server action — the backend expects `runtimeLimitSeconds` (integer, minimum 60). The default of 60 minutes = 3600 seconds matches the existing backend default.
  </action>
  <verify>
Open `services/ui/src/routes/new-execution/+page.server.ts` and confirm:
1. `formData.getAll('allowedTools')` used (not `formData.get`)
2. `runtimeLimitMinutes` parsed and converted to seconds via `* 60`
3. `campaignType` parsed with 'ad_hoc' default
4. All three fields included in the JSON body sent to POST /executions
  </verify>
  <done>Server action extracts allowedTools (array via getAll), converts runtimeLimitMinutes to runtimeLimitSeconds, reads campaignType with fallback, and includes all three in the POST body.</done>
</task>

</tasks>

<verification>
1. Read `+page.svelte` — confirm three new panel sections with correct state variables and hidden inputs
2. Read `+page.server.ts` — confirm `formData.getAll('allowedTools')`, minute-to-second conversion, and campaignType extraction
3. Verify the JSON body includes `allowedTools`, `runtimeLimitSeconds`, and `campaignType`
4. Verify defaults: campaignType='ad_hoc', selectedTools=empty Set (sends []), runtimeLimitMinutes=60
</verification>

<success_criteria>
- Campaign type selector renders with ad_hoc/campaign toggle buttons using tool-toggle pattern
- Tool allowlist renders 5 tools as multi-select toggles
- Runtime limit input shows with 60-minute default
- Server action sends all three fields to POST /executions
- Existing form fields (objective, maxBots, budgetCapDollars, llmProvider, allowedDomains) unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/35-execution-form-enhancements/35-01-SUMMARY.md`
</output>
