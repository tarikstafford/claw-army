---
phase: 39-soul-and-dna-visibility
plan: 03
type: execute
wave: 3
depends_on: [39-01, 39-02]
files_modified: []
autonomous: false
requirements:
  - SOUL-05

must_haves:
  truths:
    - "Execution post-run report shows the Ring Leader fitness detail panel with coordination quality (4 dimensions) and soul selection quality (5 dimensions) each individually scored"
    - "All new Phase 39 pages (Soul Library, Category Benchmarks, Negative Signals) render without errors"
    - "Decision traces section on bot detail page functions correctly"
  artifacts: []
  key_links:
    - from: "services/ui/src/routes/executions/[id]/report/+page.svelte"
      to: "getRingLeaderSynthesis()"
      via: "synthesisData.fitness block"
      pattern: "coordinationScore.*soulSelectionScore"
---

<objective>
Verify the Ring Leader fitness breakdown (SOUL-05) is already fully implemented and perform a visual verification checkpoint for all Phase 39 UI work.

Purpose: SOUL-05 was identified in research as already complete — the fitness panel on the execution report page renders all 4 coordination dimensions and 5 soul selection dimensions with score bars. This plan confirms that finding and provides the human-verify checkpoint for the entire phase.

Output: Verified SOUL-05 requirement, visual confirmation of all Phase 39 pages.
</objective>

<execution_context>
@/Users/tarikstafford/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarikstafford/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-soul-and-dna-visibility/39-RESEARCH.md
@.planning/phases/39-soul-and-dna-visibility/39-01-SUMMARY.md
@.planning/phases/39-soul-and-dna-visibility/39-02-SUMMARY.md

@services/ui/src/routes/executions/[id]/report/+page.svelte
@services/ui/src/lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify SOUL-05 Ring Leader fitness panel completeness</name>
  <files></files>
  <action>
Verify that the Ring Leader fitness breakdown on the execution report page already satisfies SOUL-05 by checking:

1. Read `services/ui/src/routes/executions/[id]/report/+page.svelte` and confirm it renders:
   - Composite score with color class
   - Coordination Score section (60% weight) with 4 dimensions:
     - Collective Outcome (40%)
     - Drift Prevention (25%)
     - Reallocation Effectiveness (20%)
     - Budget Management (15%)
   - Soul Selection Score section (40% weight) with 5 dimensions:
     - Library Search Quality (20%)
     - Differentiation Effectiveness (20%)
     - Mutation Decision Quality (20%)
     - Pioneer Handling (20%)
     - Selection Retrospective Quality (20%)
   - Each dimension has a score bar with percentage fill and numeric value
   - Weighted subtotals for each section

2. Read `services/ui/src/lib/types.ts` and confirm `CoordinationScore` and `SoulSelectionScore` interfaces have all dimension fields.

3. Read `services/ui/src/lib/api.ts` and confirm `getRingLeaderSynthesis()` returns `RingLeaderSynthesisResponse` which includes the `fitness` field.

4. Confirm the backend endpoint `GET /ring-leader/runs/by-execution/:executionId/synthesis` returns the fitness breakdown by reading `services/execution-service/src/routes/ring-leader.ts`.

If any dimension is missing or labels are incorrect, fix them. Based on research, all 9 dimensions are present — this is expected to be a no-op verification.

Output a brief verification statement confirming SOUL-05 is satisfied with line number references.
  </action>
  <verify>
    <automated>grep -c "collectiveOutcome\|driftPrevention\|reallocationEffectiveness\|budgetManagement\|librarySearchQuality\|differentiationEffectiveness\|mutationDecisionQuality\|pioneerHandling\|selectionRetrospectiveQuality" services/ui/src/routes/executions/[id]/report/+page.svelte</automated>
  </verify>
  <done>All 9 fitness dimensions confirmed present in the report page with score bars, weights, and subtotals. SOUL-05 requirement is satisfied by existing implementation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 39 — Soul and DNA Visibility: All five requirements implemented.

**SOUL-01 (Plan 01):** Soul Library browser at /souls — lists all souls with task category, generation, agent class badge, and composite score. Supports filter chips for category and agent class, with Load More pagination.

**SOUL-02 (Plan 02):** Decision Trace Viewer on bot detail page (/executions/:id/bots/:botId) — toggle section showing directive references, attribution confidence percentages, and outcome badges per decision.

**SOUL-03 (Plan 02):** Negative Signal Register at /negative-signals — table listing failed/retired souls with failure type badges, directive failure summaries, and filter chips by failure type.

**SOUL-04 (Plan 01):** Category Benchmarks at /category-benchmarks — table showing pioneer progress, baseline scores, benchmark maturity, thin-data flags, and confirmed run counts per category.

**SOUL-05 (Verified):** Ring Leader Fitness Breakdown on execution report (/executions/:id/report) — coordination quality (4 dimensions) and soul selection quality (5 dimensions) with score bars and weighted subtotals. Already implemented in prior phases.

**Nav links:** Souls, Benchmarks, and Signals added to the main navigation bar.
  </what-built>
  <how-to-verify>
1. Start the dev server: `cd services/ui && pnpm dev`
2. Start the backend: `cd services/execution-service && pnpm dev`

**Soul Library (/souls):**
3. Navigate to /souls in the browser
4. Verify the page loads with a "Soul Library" heading
5. Check that filter chips for categories and agent classes appear
6. Click a category chip — verify it reloads with filtered results
7. Click a class chip — verify it filters by that class
8. If souls exist, verify each card shows: task category, generation badge, agent class badge, score
9. If no souls exist, verify empty state message appears

**Category Benchmarks (/category-benchmarks):**
10. Navigate to /category-benchmarks
11. Verify the page loads with a "Category Benchmarks" heading
12. If benchmarks exist, verify table shows: task category, baseline score, confirmed runs, maturity badge, thin-data indicator
13. If no benchmarks exist, verify empty state message appears

**Negative Signals (/negative-signals):**
14. Navigate to /negative-signals
15. Verify the page loads with a "Negative Signal Register" heading
16. Check that failure type filter chips appear
17. If signals exist, verify table shows: failure type badge, task category, directive summary, date
18. If no signals exist, verify empty state message appears

**Decision Traces (bot detail page):**
19. Navigate to any execution's bot detail page (/executions/:id/bots/:botId)
20. Find and click the "Decision Traces" toggle button
21. If traces exist, verify they show: decision type, directive, confidence %, outcome
22. If no traces exist, verify "No decision traces recorded" message appears

**Ring Leader Fitness (execution report):**
23. Navigate to a completed execution's report page (/executions/:id/report)
24. Scroll to "Ring Leader Fitness" section
25. Verify Coordination Score shows 4 dimensions with score bars
26. Verify Soul Selection Score shows 5 dimensions with score bars
27. Verify weighted subtotals are displayed

**Navigation:**
28. Check the nav bar shows links: Souls, Benchmarks, Signals
29. Click each link and verify navigation works
  </how-to-verify>
  <resume-signal>Type "approved" if all pages render correctly, or describe any visual or functional issues.</resume-signal>
</task>

</tasks>

<verification>
1. SOUL-05 fitness panel has all 9 dimensions with score bars and weighted subtotals
2. All Phase 39 pages load without JavaScript errors
3. Filter chips trigger backend reload (not client-side filtering)
4. Empty states render correctly on all pages
5. Nav links navigate to correct routes
6. Decision traces section toggles on/off on bot detail page
</verification>

<success_criteria>
- All 5 SOUL requirements (SOUL-01 through SOUL-05) are verified as complete
- Human approval confirms visual correctness of all new pages
- No console errors on any Phase 39 page
</success_criteria>

<output>
After completion, create `.planning/phases/39-soul-and-dna-visibility/39-03-SUMMARY.md`
</output>
