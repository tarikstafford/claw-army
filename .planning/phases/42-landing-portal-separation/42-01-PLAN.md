---
phase: 42-landing-portal-separation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/ui/src/lib/components/ParticleCanvas.svelte
  - services/ui/src/routes/+layout.svelte
  - services/ui/src/routes/+layout.js
  - services/ui/src/routes/(marketing)/+layout.svelte
  - services/ui/src/routes/(marketing)/+page.svelte
  - services/ui/src/routes/(marketing)/+page.server.ts
  - services/ui/src/routes/(marketing)/login/+page.svelte
  - services/ui/src/routes/(app)/+layout.svelte
  - services/ui/src/routes/(app)/+layout.server.ts
autonomous: false
requirements:
  - PORTAL-01

must_haves:
  truths:
    - "Landing page at / shows only logo + Login/Signup nav — no portal links (Objectives, Guide, Verdicts, etc.)"
    - "Portal pages (e.g. /objectives) show full nav with all management links + Deploy crew button + SSE toasts"
    - "All existing routes remain accessible at the same URLs — no 404s"
    - "Particle canvas renders on both landing and portal pages"
    - "Session data is only loaded for portal routes, not the landing page"
  artifacts:
    - path: "services/ui/src/lib/components/ParticleCanvas.svelte"
      provides: "Reusable particle canvas animation component"
      contains: "class Particle"
    - path: "services/ui/src/routes/(marketing)/+layout.svelte"
      provides: "Minimal marketing nav layout (logo + Login/Signup)"
      contains: "ParticleCanvas"
    - path: "services/ui/src/routes/(app)/+layout.svelte"
      provides: "Full portal nav layout with SSE toasts"
      contains: "connectLifecycleSSE"
    - path: "services/ui/src/routes/(app)/+layout.server.ts"
      provides: "Session load for portal routes only"
      exports: ["load"]
    - path: "services/ui/src/routes/+layout.svelte"
      provides: "Bare root shell — only renders children"
      min_lines: 3
  key_links:
    - from: "services/ui/src/routes/(app)/+layout.svelte"
      to: "services/ui/src/routes/(app)/+layout.server.ts"
      via: "data.session from layout server load"
      pattern: "data\\.session"
    - from: "services/ui/src/routes/(marketing)/+layout.svelte"
      to: "services/ui/src/lib/components/ParticleCanvas.svelte"
      via: "component import"
      pattern: "ParticleCanvas"
    - from: "services/ui/src/routes/(app)/+layout.svelte"
      to: "$lib/sse"
      via: "SSE lifecycle connection"
      pattern: "connectLifecycleSSE"
---

<objective>
Separate the public landing page from the authenticated crew portal using SvelteKit route groups.

Purpose: Marketing visitors should see a clean, minimal nav (logo + Login/Signup) — not the full portal nav with Objectives, Guide, Verdicts, etc. Portal users should continue to see the full nav with all management links, Deploy crew button, and SSE lifecycle toasts.

Output: Two SvelteKit route groups — `(marketing)` for landing + login, `(app)` for all authenticated portal routes — each with their own layout. Root layout becomes a bare shell.
</objective>

<execution_context>
@/Users/tarikstafford/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarikstafford/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-landing-portal-separation/42-RESEARCH.md

@services/ui/src/routes/+layout.svelte
@services/ui/src/routes/+layout.server.ts
@services/ui/src/routes/+layout.js
@services/ui/src/routes/+page.svelte
@services/ui/src/routes/+page.server.ts

<interfaces>
<!-- Current root layout provides these to all routes — must be split between groups -->

From services/ui/src/routes/+layout.server.ts:
```typescript
import type { LayoutServerLoad } from './$types';
export const load: LayoutServerLoad = async (event) => {
  return { session: await event.locals.auth() };
};
```

From services/ui/src/routes/+layout.svelte (key imports):
```typescript
import '../app.css';
import { signOut } from '@auth/sveltekit/client';
import { connectLifecycleSSE } from '$lib/sse';
import type { LifecycleNotification } from '$lib/types';
```

Current route directories to move into (app)/:
  objectives/, executions/, souls/, guide/, verdicts/, billing/,
  category-benchmarks/, negative-signals/, new-execution/, admin/

Current route files to move into (marketing)/:
  +page.svelte, +page.server.ts

Current route to move into (marketing)/:
  login/

Routes to leave at root level (server-only, no layout needed):
  auth/[...auth]/, api/[...path]/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract ParticleCanvas component and restructure routes into groups</name>
  <files>
    services/ui/src/lib/components/ParticleCanvas.svelte
    services/ui/src/routes/(marketing)/+page.svelte
    services/ui/src/routes/(marketing)/+page.server.ts
    services/ui/src/routes/(marketing)/login/+page.svelte
    services/ui/src/routes/(app)/+layout.server.ts
    services/ui/src/routes/(app)/objectives/ (move)
    services/ui/src/routes/(app)/executions/ (move)
    services/ui/src/routes/(app)/souls/ (move)
    services/ui/src/routes/(app)/guide/ (move)
    services/ui/src/routes/(app)/verdicts/ (move)
    services/ui/src/routes/(app)/billing/ (move)
    services/ui/src/routes/(app)/category-benchmarks/ (move)
    services/ui/src/routes/(app)/negative-signals/ (move)
    services/ui/src/routes/(app)/new-execution/ (move)
    services/ui/src/routes/(app)/admin/ (move)
  </files>
  <action>
    1. **Extract ParticleCanvas component** from the current root `+layout.svelte`:
       - Create `services/ui/src/lib/components/ParticleCanvas.svelte`
       - Move the `<script lang="ts" module>` block (PARTICLE_COLS array + Particle class) into the component's module script
       - Move the particle canvas setup from the `onMount` in root layout (lines 111-138: canvas element lookup, resize handler, particle array creation, animation loop) into the component's instance script `onMount`
       - Include the canvas element: `<canvas id="particles"></canvas>`
       - Include the scoped style for `canvas#particles` (position fixed, inset 0, pointer-events none, z-index 1) — use `:global(canvas#particles)` since the element is in this component
       - The component takes no props — it is fully self-contained

    2. **Create route group directories:**
       - `mkdir -p services/ui/src/routes/\(marketing\)/login`
       - `mkdir -p services/ui/src/routes/\(app\)`

    3. **Move landing page into (marketing)/:**
       - `mv services/ui/src/routes/+page.svelte services/ui/src/routes/\(marketing\)/+page.svelte`
       - `mv services/ui/src/routes/+page.server.ts services/ui/src/routes/\(marketing\)/+page.server.ts`

    4. **Move login into (marketing)/:**
       - `mv services/ui/src/routes/login/+page.svelte services/ui/src/routes/\(marketing\)/login/+page.svelte`
       - `rmdir services/ui/src/routes/login`

    5. **Move all portal routes into (app)/:**
       - Move each directory: objectives, executions, souls, guide, verdicts, billing, category-benchmarks, negative-signals, new-execution, admin
       - Use `mv services/ui/src/routes/{dirname} services/ui/src/routes/\(app\)/{dirname}` for each

    6. **Move +layout.server.ts into (app)/:**
       - `mv services/ui/src/routes/+layout.server.ts services/ui/src/routes/\(app\)/+layout.server.ts`
       - The session load is only needed for portal routes, not the landing page

    7. **Leave at root level (no move):**
       - `auth/[...auth]/` — Auth.js catch-all, server-only route, no UI layout
       - `api/[...path]/` — API proxy, server-only route, no UI layout
       - `+layout.js` — keeps SSR enabled globally (just exports `{}`)
       - `+layout.svelte` — will be stripped to bare shell in Task 2

    8. **Run svelte-kit sync** to regenerate `$types` after the moves:
       - `pnpm --filter @claw/ui exec svelte-kit sync`
  </action>
  <verify>
    <automated>cd /Users/tarikstafford/Desktop/Projects/claw-army && ls services/ui/src/routes/\(marketing\)/+page.svelte services/ui/src/routes/\(marketing\)/+page.server.ts services/ui/src/routes/\(marketing\)/login/+page.svelte services/ui/src/routes/\(app\)/+layout.server.ts services/ui/src/routes/\(app\)/objectives/+page.svelte services/ui/src/lib/components/ParticleCanvas.svelte && echo "All files in correct locations"</automated>
  </verify>
  <done>All route files are in their correct group directories. ParticleCanvas.svelte exists as a standalone component. auth/ and api/ remain at root level. svelte-kit sync completes without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create group-specific layouts and strip root layout</name>
  <files>
    services/ui/src/routes/+layout.svelte
    services/ui/src/routes/(marketing)/+layout.svelte
    services/ui/src/routes/(app)/+layout.svelte
  </files>
  <action>
    1. **Create (marketing)/+layout.svelte** — minimal nav layout:
       - Import `'../../app.css'` (relative path from the group directory)
       - Import `ParticleCanvas` from `$lib/components/ParticleCanvas.svelte`
       - Use `let { children } = $props()` (Svelte 5 runes)
       - Include `<svelte:head>` with the Google Fonts and Fontshare links (copied from current root layout)
       - Render `<ParticleCanvas />` component
       - Create a nav with the same structure as the current root nav BUT with only:
         - Logo (Akasa SVG + text) linking to `/`
         - nav-right section with: `<a href="/login" class="btn-nav">Login</a>` and `<a href="#access" class="btn-nav btn-primary">Sign up</a>`
         - NO `.nav-links` ul (no Objectives, Guide, Verdicts, etc.)
         - NO `.status-pill` (Platform live indicator)
         - NO SSE toast notifications
       - Include the `navEl` bind and scroll handler for sticky nav (`stuck` class toggle)
       - Render `<main>{@render children()}</main>`
       - Copy all relevant nav styles from the current root layout: nav positioning, .stuck, .nav-row, .logo, .logo-mark, .logo-text, .nav-right, .btn-nav, main z-index, logo animations (lm-spin, lm-spin-r, lm-pulse)
       - Also include the `.btn-primary` style for the Sign up button (same as existing `btn-nav` but with violet background)

    2. **Create (app)/+layout.svelte** — full portal nav layout:
       - Import `'../../app.css'` (relative path)
       - Import `ParticleCanvas` from `$lib/components/ParticleCanvas.svelte`
       - Import `{ signOut }` from `'@auth/sveltekit/client'`
       - Import `{ browser }` from `'$app/environment'`
       - Import `{ connectLifecycleSSE }` from `'$lib/sse'`
       - Import `type { LifecycleNotification }` from `'$lib/types'`
       - Use `let { children, data } = $props()` and `let session = $derived(data.session)`
       - Include `<svelte:head>` with the same font links
       - Render `<ParticleCanvas />`
       - Render the FULL nav as it currently exists in root layout:
         - Logo (Akasa SVG + text)
         - `.nav-links` ul with all 7 links: Objectives, Guide, Verdicts, Billing, Souls, Benchmarks, Signals
         - `.nav-right` with status pill, Deploy crew button (when session), Sign out button (when session), Request access link (when no session)
       - Include the SSE lifecycle toast notification system (the full `$effect`, `addNotification`, `dismissNotification`, `getNotificationIcon`, `getNotificationColor` functions + toast HTML)
       - Include scroll handler for sticky nav
       - Render `<main>{@render children()}</main>`
       - Copy ALL styles from the current root layout: nav, stuck, nav-row, logo, logo-mark, logo-text, nav-links, nav-right, btn-deploy, status-pill, main, responsive media query, and ALL lifecycle toast styles (lifecycle-toasts, lifecycle-toast, slideIn animation, toast-icon, toast-content, toast-type, toast-desc, toast-dismiss)

    3. **Strip root +layout.svelte to bare shell:**
       - Remove ALL existing content
       - Replace with:
         ```svelte
         <script lang="ts">
           let { children } = $props();
         </script>

         {@render children()}
         ```
       - No CSS import (each group layout imports app.css independently)
       - No nav, no particles, no SSE — everything is in group layouts now
       - The root layout ONLY passes through to group layouts

    4. **Run type check** to verify everything compiles:
       - `pnpm --filter @claw/ui check`
  </action>
  <verify>
    <automated>cd /Users/tarikstafford/Desktop/Projects/claw-army && pnpm --filter @claw/ui check</automated>
  </verify>
  <done>Root layout is a bare shell (renders children only). Marketing layout has minimal nav (logo + Login/Signup). App layout has full nav + SSE toasts. TypeScript check passes with no errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify visual separation between landing and portal</name>
  <files>none</files>
  <action>
    Human visual verification of the route group separation. Start the dev server with `pnpm --filter @claw/ui dev` and inspect both marketing and portal layouts in the browser.
  </action>
  <verify>
    <automated>echo "Manual visual verification — see how-to-verify steps"</automated>
  </verify>
  <done>User confirms landing page shows minimal nav and portal pages show full nav.</done>
  <what-built>Route group separation: landing page at / uses minimal marketing nav (logo + Login/Signup only), portal pages like /objectives use full nav with all management links + Deploy crew + SSE toasts. Particle canvas renders on both.</what-built>
  <how-to-verify>
    1. Start the dev server: `pnpm --filter @claw/ui dev`
    2. Navigate to http://localhost:5173/ — verify:
       - Minimal nav: logo + Login + Sign up buttons only
       - NO portal links (Objectives, Guide, Verdicts, Billing, Souls, Benchmarks, Signals)
       - NO "Platform live" status pill
       - Particle canvas still animates in background
       - Landing page content renders normally (hero, sections, request access form)
    3. Navigate to http://localhost:5173/login — verify:
       - Same minimal nav as landing page
       - Login card renders normally
    4. Navigate to http://localhost:5173/objectives (requires auth) — verify:
       - Full nav bar with all 7 management links
       - "Deploy crew" button visible (if authenticated)
       - "Platform live" status pill visible
       - Particle canvas still animates
    5. Spot-check other portal routes (/souls, /billing) — verify full nav renders
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter @claw/ui check` passes — no TypeScript/Svelte errors
- Route URLs unchanged: `/` still serves landing, `/objectives` still serves objectives, `/login` still serves login
- Landing page nav shows ONLY logo + Login + Sign up
- Portal pages nav shows ALL management links + Deploy crew + SSE toasts
- Particle canvas renders on both landing and portal pages
- `auth/` and `api/` server-only routes unaffected
</verification>

<success_criteria>
- Landing page at `/` renders with minimal marketing nav (logo + Login/Signup only)
- Portal pages render with full nav bar (Objectives, Guide, Verdicts, Billing, Souls, Benchmarks, Signals + Deploy crew + status pill)
- SSE lifecycle toasts only appear on portal pages, not landing
- Session is only loaded for portal routes (no unnecessary auth check on landing page)
- All existing routes accessible at their original URLs
- `pnpm --filter @claw/ui check` passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/42-landing-portal-separation/42-01-SUMMARY.md`
</output>
